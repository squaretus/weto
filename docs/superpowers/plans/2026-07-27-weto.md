# Weto Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Приложение в менюбаре macOS, которое немедленно завершает по SIGKILL все процессы заданных приложений, как только сетевое положение машины перестаёт быть безопасным (упал корпоративный VPN, гео стало заблокированным, внешний IP попал в чёрный список либо вердикт вообще не получен).

**Architecture:** Один SPM-пакет, семь таргетов. Вся логика решения — чистая функция `GuardPolicy` в `WetoCore`, не знающая ни про сеть, ни про процессы; системные API спрятаны за протоколами в `WetoSystem`; VM-слой `@Observable @MainActor` в `WetoShared`; UI — единственный процесс `MenuBarExtra` плюс окно настроек. Root-демон существует исключительно ради тихой установки обновлений.

**Tech Stack:** Swift 5.9, SwiftUI, Swift Package Manager, `Network.NWPathMonitor`, `SystemConfiguration.SCDynamicStore`, `libproc`, `NSXPCConnection`, XCTest.

**Спека:** `docs/superpowers/specs/2026-07-27-weto-design.md` — читать перед началом.

**Референс паттернов:** `../blik/blik` — рабочее приложение того же автора с той же архитектурой. Пути к конкретным файлам указаны в задачах.

## Global Constraints

- **Git на этапе реализации не используется.** Проект личный, не рабочий: номеров задач нет, репозиторий не инициализируется, шаги «Коммит» в задачах пропускаются. Владелец даст всё для коммитов после реализации и первых тестов — тогда история собирается одним заходом. Вместо коммита каждая задача завершается прогоном `swift test` и проверкой, что весь набор зелёный.
- **Никаких упоминаний Claude/AI в коммитах** — когда до них дойдёт дело. Трейлеры `Co-Authored-By` не добавляются.
- `swift-tools-version: 5.9`, `platforms: [.macOS("26.0")]`.
- Внешних зависимостей нет — только системные фреймворки.
- **Инвариант границ:** `WetoCore` не импортирует `Network`, `SystemConfiguration`, `AppKit`, `SwiftUI` и не использует `URLSession`. Разрешены `Foundation` и `Darwin`.
- Тесты — XCTest, `final class ...: XCTestCase`, имена методов в стиле `test_subject_does_thing`.
- Язык кода, комментариев и документации — русский для текста, английский для идентификаторов.
- Мокаются только границы системы (сеть, процессы, `SCDynamicStore`). Внутренние типы не подменяются никогда.
- Разработка вертикальными срезами: один тест → одна реализация → следующий тест. Не писать пачку тестов заранее.
- `Package.swift` растёт инкрементально: таргет добавляется в манифест в той же задаче, где появляется его первый файл. Это держит `swift build` зелёным на каждом коммите.
- Идентификаторы: bundle ID `com.weto.app`, демон `com.weto.helper`, пакет `com.weto.pkg`, suite настроек `com.weto.shared`.

---

## File Structure

```
weto/
├── Package.swift
├── AGENTS.md                                  Task 20
├── .gitignore                                 Task 1
├── .claude/
│   ├── CLAUDE.md → ../AGENTS.md (симлинк)     Task 20
│   ├── rules/ARCHITECTURE.md                  Task 20
│   └── docs/                                  Task 20
├── docs/superpowers/{specs,plans}/            уже есть
├── Resources/                                 Task 19
│   ├── Weto-Info.plist
│   ├── com.weto.app.plist
│   ├── com.weto.helper.plist
│   ├── uninstall-helper.sh
│   ├── welcome.html, conclusion.html
│   └── AppIcon.icns
├── scripts/{build.sh,preinstall,postinstall}  Task 19
├── .github/workflows/{release.yml,pr-checks.yml}  Task 20
├── Sources/
│   ├── WetoCore/
│   │   ├── Constants.swift                    Task 1
│   │   ├── CountryFlag.swift                  Task 1
│   │   ├── Model/GeoModels.swift              Task 1   VPNStatus, ConfirmSource, GeoReading, GeoOutcome
│   │   ├── Model/NetworkSnapshot.swift        Task 4
│   │   ├── Model/ProcessSnapshot.swift        Task 5
│   │   ├── Model/KillEvent.swift              Task 12  + UnsafeReason.displayText
│   │   ├── Model/UpdateInfo.swift             Task 17
│   │   ├── IPRange.swift                      Task 2
│   │   ├── GuardPolicy.swift                  Task 3   GuardConfig, GuardSignals, UnsafeReason, GuardDecision
│   │   │                                      Task 12  + decideLocal
│   │   ├── VPNStatusResolver.swift            Task 4
│   │   ├── ProcessMatcher.swift               Task 5
│   │   ├── GeoResponses.swift                 Task 6
│   │   ├── SemanticVersion.swift              Task 17
│   │   └── ReleaseParser.swift                Task 17
│   ├── WetoSystem/
│   │   ├── NetworkSnapshotReader.swift        Task 7   SCDynamicStore → NetworkSnapshot
│   │   ├── HTTPFetching.swift                 Task 8
│   │   ├── GeoProbe.swift                     Task 8
│   │   ├── ProcessRegistry.swift              Task 9
│   │   ├── ProcessKiller.swift                Task 9
│   │   ├── NetworkEventSource.swift           Task 10  NWPathMonitor + SCDynamicStore notify
│   │   └── KeychainStore.swift                Task 11  + TokenBox
│   ├── WetoShared/
│   │   ├── SettingsStore.swift                Task 11
│   │   ├── EventLogStore.swift                Task 12
│   │   ├── KillNotifying.swift                Task 13
│   │   ├── GuardVM.swift                      Task 13  GuardState, GuardStatusColor
│   │   ├── AppCoordinator.swift               Task 13
│   │   ├── StatusPresentation.swift           Task 16  BannerTone
│   │   ├── UpdateVM.swift                     Task 18
│   │   └── LaunchAgentController.swift        Task 19
│   ├── WetoDesign/                      Task 14  порт из ../blik/blik/Sources/BlikDesign
│   │   └── Components/MenuBarImageRenderer.swift  Task 15
│   ├── WetoXPC/                         Task 18
│   ├── WetoHelper/                      Task 18
│   └── WetoMenuBar/
│       ├── WetoMenuBarApp.swift         Task 15
│       ├── MenuBarLabel.swift                 Task 15
│       ├── StatusPopupView.swift              Task 16
│       ├── Settings/SettingsWindow.swift      Task 16, дополняется в 18 и 19
│       └── Settings/TargetsSection.swift      Task 16
└── Tests/
    ├── WetoCoreTests/                   Tasks 1–6, 12, 17
    ├── WetoSystemTests/                 Tasks 7–11
    ├── WetoSharedTests/                 Tasks 11–13, 16, 19
    ├── WetoDesignTests/                 Tasks 14, 15
    └── WetoXPCTests/                    Task 18
```

---

## Task 1: Каркас пакета, константы, флаги стран

**Files:**
- Create: `Package.swift`, `.gitignore`
- Create: `Sources/WetoCore/Constants.swift`
- Create: `Sources/WetoCore/CountryFlag.swift`
- Create: `Sources/WetoCore/Model/GeoModels.swift`
- Test: `Tests/WetoCoreTests/CountryFlagTests.swift`

**Interfaces:**
- Produces: `Constants` (caseless enum со всеми интервалами и дефолтами), `CountryFlag.emoji(for:) -> String`, `VPNStatus`, `ConfirmSource`, `GeoReading`, `GeoOutcome`.
- Consumes: ничего.

- [ ] **Step 1: Создать `Package.swift` с одним таргетом и его тестами**

```swift
// swift-tools-version: 5.9
import PackageDescription

let package = Package(
    name: "weto",
    platforms: [.macOS("26.0")],
    products: [
        .library(name: "WetoCore", targets: ["WetoCore"]),
    ],
    targets: [
        .target(name: "WetoCore", path: "Sources/WetoCore"),
        .testTarget(
            name: "WetoCoreTests",
            dependencies: ["WetoCore"],
            path: "Tests/WetoCoreTests"
        ),
    ]
)
```

- [ ] **Step 2: Создать `.gitignore`**

```
.build/
.DS_Store
*.xcodeproj
.swiftpm/
```

- [ ] **Step 3: Написать падающий тест на флаги**

Файл `Tests/WetoCoreTests/CountryFlagTests.swift`:

```swift
import XCTest
@testable import WetoCore

final class CountryFlagTests: XCTestCase {

    func test_valid_code_maps_to_regional_indicator_pair() {
        XCTAssertEqual(CountryFlag.emoji(for: "KZ"), "🇰🇿")
        XCTAssertEqual(CountryFlag.emoji(for: "RU"), "🇷🇺")
    }

    func test_lowercase_code_is_normalized() {
        XCTAssertEqual(CountryFlag.emoji(for: "kz"), "🇰🇿")
    }

    func test_invalid_code_falls_back_to_white_flag() {
        XCTAssertEqual(CountryFlag.emoji(for: ""), "🏳️")
        XCTAssertEqual(CountryFlag.emoji(for: "K"), "🏳️")
        XCTAssertEqual(CountryFlag.emoji(for: "KAZ"), "🏳️")
        XCTAssertEqual(CountryFlag.emoji(for: "K1"), "🏳️")
        XCTAssertEqual(CountryFlag.emoji(for: "--"), "🏳️")
    }
}
```

- [ ] **Step 4: Прогнать тест, убедиться что падает**

Run: `swift test --filter CountryFlagTests`
Expected: FAIL — `cannot find 'CountryFlag' in scope`.

- [ ] **Step 5: Реализовать `CountryFlag`**

Файл `Sources/WetoCore/CountryFlag.swift`:

```swift
import Foundation

/// Перевод ISO 3166-1 alpha-2 в эмодзи-флаг.
///
/// Таблицы стран нет намеренно: каждая буква кода отображается в regional
/// indicator symbol сдвигом на `0x1F1E6 - 'A'`, поэтому новые коды работают
/// без правок. Некорректный вход даёт белый флаг, а не крэш и не пустую строку —
/// лейбл менюбара всегда должен что-то показывать.
public enum CountryFlag {

    /// Белый флаг — страна неизвестна либо код не разобран.
    public static let unknown = "🏳️"

    public static func emoji(for code: String) -> String {
        let scalars = Array(code.uppercased().unicodeScalars)
        guard scalars.count == 2 else { return unknown }

        var view = String.UnicodeScalarView()
        for scalar in scalars {
            guard scalar.value >= 65, scalar.value <= 90,
                  let indicator = UnicodeScalar(0x1F1E6 + scalar.value - 65)
            else { return unknown }
            view.append(indicator)
        }
        return String(view)
    }
}
```

- [ ] **Step 6: Прогнать тест, убедиться что проходит**

Run: `swift test --filter CountryFlagTests`
Expected: PASS, 3 теста.

- [ ] **Step 7: Создать модели гео и VPN**

Файл `Sources/WetoCore/Model/GeoModels.swift`:

```swift
import Foundation

/// Состояние выбранного в настройках VPN-сервиса.
public enum VPNStatus: Equatable, Sendable {
    /// В настройках не выбран сервис.
    case notConfigured
    /// Сервис есть в системе, но адреса на нём нет — туннель не поднят.
    case down
    /// Туннель поднят. `isPrimary` — держит ли он default route.
    case up(isPrimary: Bool)
}

/// Какой сервис подтвердил страну известного IP.
public enum ConfirmSource: String, Equatable, Codable, Sendable {
    case ipwhois
    case geojs
}

/// Снятое показание о внешнем адресе.
///
/// `primaryCountry` всегда приходит от ipinfo — единственного источника, которому
/// доверен сам IP (см. раздел 5 спеки: остальные сервисы при split-routing
/// отвечают про посторонний адрес). `confirmedCountry == nil` означает, что оба
/// подтверждающих сервиса не ответили.
public struct GeoReading: Equatable, Sendable {
    public let ip: String
    public let asn: String?
    public let primaryCountry: String
    public let confirmedCountry: String?
    public let confirmSource: ConfirmSource?

    public init(
        ip: String,
        asn: String?,
        primaryCountry: String,
        confirmedCountry: String?,
        confirmSource: ConfirmSource?
    ) {
        self.ip = ip
        self.asn = asn
        self.primaryCountry = primaryCountry
        self.confirmedCountry = confirmedCountry
        self.confirmSource = confirmSource
    }
}

/// Итог сетевой пробы.
///
/// `.unavailable` означает отказ именно ipinfo: без него нет IP, а значит нет и
/// предмета для разговора. Отказ подтверждающих сервисов при живом ipinfo
/// выражается как `.resolved` с `confirmedCountry == nil`.
public enum GeoOutcome: Equatable, Sendable {
    case resolved(GeoReading)
    case unavailable(String)
}
```

- [ ] **Step 8: Создать константы**

Файл `Sources/WetoCore/Constants.swift`:

```swift
import Foundation

/// Именованные константы проекта. Caseless enum предотвращает создание экземпляров.
public enum Constants {

    // MARK: - Версия

    /// Версия приложения. Подставляется `scripts/build.sh` при сборке релиза.
    public static let appVersion = "0.1.0"

    // MARK: - Опрос

    /// Интервал фонового такта по умолчанию, секунды.
    public static let defaultPollIntervalSeconds: TimeInterval = 5

    /// Доступные интервалы опроса в настройках, секунды.
    public static let pollIntervalOptions: [TimeInterval] = [5, 10, 15, 30]

    /// Окно коалесценции сетевых событий, секунды.
    /// При переключении Wi-Fi система выдаёт пачку из 5–10 уведомлений за секунду.
    public static let networkEventDebounceSeconds: TimeInterval = 0.3

    /// Период добивающего таймера, пока состояние небезопасно, секунды.
    public static let watchdogIntervalSeconds: TimeInterval = 1

    /// Таймаут любого гео-запроса, секунды.
    public static let geoRequestTimeoutSeconds: TimeInterval = 5

    // MARK: - Гео-сервисы

    /// Единственный источник внешнего IP. Хост `v4.` принудительно использует IPv4.
    public static let ipinfoLiteURL = "https://v4.api.ipinfo.io/lite/me"

    /// Основной подтверждающий сервис. Лимит 1000 запросов в сутки, поэтому
    /// вызывается только при смене IP.
    public static func ipwhoisURL(ip: String) -> String { "https://ipwho.is/\(ip)" }

    /// Резервный подтверждающий сервис. Лимит не задокументирован — отсюда роль резерва.
    public static func geojsURL(ip: String) -> String {
        "https://get.geojs.io/v1/ip/country/\(ip).json"
    }

    // MARK: - Дефолты политики

    /// Заблокированные страны по умолчанию.
    public static let defaultBlockedCountries: Set<String> = ["RU"]

    // MARK: - Журнал

    /// Размер кольцевого буфера журнала срабатываний.
    public static let eventLogCapacity = 200

    /// Сколько последних событий показывать в попапе менюбара.
    public static let eventLogPreviewCount = 5

    // MARK: - Хранилище

    /// Suite `UserDefaults`, общий для приложения и демона.
    public static let userDefaultsSuite = "com.weto.shared"

    /// Сервис Keychain для токена ipinfo.
    public static let keychainService = "com.weto.ipinfo"

    // MARK: - Автообновление

    /// GitHub owner. Уточняется владельцем перед первым релизом (Task 16).
    public static let githubOwner = "squaretus"

    /// GitHub repo. Уточняется владельцем перед первым релизом (Task 16).
    public static let githubRepo = "weto"

    /// Интервал проверки обновлений, секунды (6 часов).
    public static let updateCheckInterval: TimeInterval = 21_600

    /// Задержка первой проверки после старта демона, секунды.
    public static let updateCheckInitialDelay: TimeInterval = 10

    /// Каталог для скачанного PKG, root-only 0700.
    public static let updatesDirectory = "/var/db/weto/updates"
}
```

- [ ] **Step 9: Убедиться, что всё собирается и тесты зелёные**

Run: `swift build && swift test`
Expected: сборка без предупреждений, 3 теста PASS.

- [ ] **Step 10: Инициализировать репозиторий и сделать первый коммит**

```bash
git init
git add -A
git commit -m "$TASK_NO - <Название задачи от владельца>"
```

---

## Task 2: IPRange — разбор и матчинг CIDR

**Files:**
- Create: `Sources/WetoCore/IPRange.swift`
- Test: `Tests/WetoCoreTests/IPRangeTests.swift`

**Interfaces:**
- Consumes: ничего.
- Produces: `IPRange` — `init?(_ text: String)`, `contains(_ ip: String) -> Bool`, свойства `text: String`, `prefixLength: Int`, `isIPv6: Bool`. Тип `Equatable, Hashable, Codable, Sendable` — попадёт в `GuardConfig` (Task 3) и в настройки (Task 11).

- [ ] **Step 1: Написать падающий тест на разбор и матчинг IPv4**

Файл `Tests/WetoCoreTests/IPRangeTests.swift`:

```swift
import XCTest
@testable import WetoCore

final class IPRangeTests: XCTestCase {

    func test_bare_ipv4_matches_only_itself() {
        let range = IPRange("203.0.113.28")
        XCTAssertNotNil(range)
        XCTAssertEqual(range?.prefixLength, 32)
        XCTAssertTrue(range!.contains("203.0.113.28"))
        XCTAssertFalse(range!.contains("203.0.113.29"))
    }

    func test_ipv4_prefix_matches_whole_subnet() {
        let range = IPRange("10.0.0.0/8")!
        XCTAssertTrue(range.contains("10.0.0.1"))
        XCTAssertTrue(range.contains("10.255.255.255"))
        XCTAssertFalse(range.contains("11.0.0.1"))
        XCTAssertFalse(range.contains("9.255.255.255"))
    }

    func test_non_byte_aligned_prefix_is_respected() {
        let range = IPRange("192.168.0.0/20")!
        XCTAssertTrue(range.contains("192.168.15.255"))
        XCTAssertFalse(range.contains("192.168.16.0"))
    }

    func test_zero_prefix_matches_every_address_of_same_family() {
        let range = IPRange("0.0.0.0/0")!
        XCTAssertTrue(range.contains("1.2.3.4"))
        XCTAssertTrue(range.contains("255.255.255.255"))
        XCTAssertFalse(range.contains("2606:2040::1"))
    }

    func test_host_bits_outside_prefix_are_ignored() {
        // 10.5.5.5/8 и 10.0.0.0/8 — одна и та же сеть.
        XCTAssertEqual(IPRange("10.5.5.5/8"), IPRange("10.0.0.0/8"))
    }
}
```

- [ ] **Step 2: Прогнать тест, убедиться что падает**

Run: `swift test --filter IPRangeTests`
Expected: FAIL — `cannot find 'IPRange' in scope`.

- [ ] **Step 3: Реализовать `IPRange`**

Файл `Sources/WetoCore/IPRange.swift`:

```swift
import Foundation
import Darwin

/// Диапазон адресов в нотации CIDR. Поддерживает IPv4 и IPv6.
///
/// Адрес хранится побайтово в сетевом порядке и уже обнулён вне префикса,
/// поэтому `10.5.5.5/8` и `10.0.0.0/8` равны как значения. Исходный текст
/// сохраняется отдельно — его показывает UI и он же уходит в настройки.
public struct IPRange: Equatable, Hashable, Codable, Sendable {

    /// Сетевой адрес, обнулённый вне префикса. 4 байта для IPv4, 16 для IPv6.
    public let networkBytes: [UInt8]
    public let prefixLength: Int
    public let isIPv6: Bool
    /// Как пользователь это ввёл — для отображения в настройках.
    public let text: String

    // MARK: - Разбор

    public init?(_ text: String) {
        let trimmed = text.trimmingCharacters(in: .whitespaces)
        guard !trimmed.isEmpty else { return nil }

        let parts = trimmed.split(separator: "/", omittingEmptySubsequences: false)
        guard parts.count == 1 || parts.count == 2 else { return nil }

        guard let (bytes, isV6) = Self.parseAddress(String(parts[0])) else { return nil }

        let maxPrefix = isV6 ? 128 : 32
        var prefix = maxPrefix
        if parts.count == 2 {
            guard let parsed = Int(parts[1]), parsed >= 0, parsed <= maxPrefix else { return nil }
            prefix = parsed
        }

        self.networkBytes = Self.masked(bytes, prefixLength: prefix)
        self.prefixLength = prefix
        self.isIPv6 = isV6
        self.text = trimmed
    }

    // MARK: - Матчинг

    public func contains(_ ip: String) -> Bool {
        guard let (bytes, isV6) = Self.parseAddress(ip.trimmingCharacters(in: .whitespaces)),
              isV6 == isIPv6
        else { return false }
        return Self.masked(bytes, prefixLength: prefixLength) == networkBytes
    }

    // MARK: - Private

    /// Возвращает байты адреса в сетевом порядке и признак семейства.
    private static func parseAddress(_ string: String) -> ([UInt8], Bool)? {
        var v4 = in_addr()
        if inet_pton(AF_INET, string, &v4) == 1 {
            return (withUnsafeBytes(of: v4.s_addr) { Array($0) }, false)
        }
        var v6 = in6_addr()
        if inet_pton(AF_INET6, string, &v6) == 1 {
            return (withUnsafeBytes(of: v6) { Array($0) }, true)
        }
        return nil
    }

    /// Обнуляет биты правее префикса.
    private static func masked(_ bytes: [UInt8], prefixLength: Int) -> [UInt8] {
        var result = bytes
        for index in result.indices {
            let bitsBefore = index * 8
            if prefixLength >= bitsBefore + 8 { continue }
            if prefixLength <= bitsBefore {
                result[index] = 0
            } else {
                let keep = prefixLength - bitsBefore
                result[index] &= UInt8(truncatingIfNeeded: 0xFF << (8 - keep))
            }
        }
        return result
    }
}
```

- [ ] **Step 4: Прогнать тесты, убедиться что проходят**

Run: `swift test --filter IPRangeTests`
Expected: PASS, 5 тестов.

- [ ] **Step 5: Добавить тест на IPv6 и на мусор**

Дописать в тот же файл:

```swift
    func test_ipv6_prefix_matches_subnet() {
        let range = IPRange("2606:2040::/32")!
        XCTAssertTrue(range.isIPv6)
        XCTAssertTrue(range.contains("2606:2040:2800:141::2"))
        XCTAssertFalse(range.contains("2607:2040::1"))
    }

    func test_families_never_match_each_other() {
        XCTAssertFalse(IPRange("10.0.0.0/8")!.contains("2606:2040::1"))
        XCTAssertFalse(IPRange("2606:2040::/32")!.contains("10.0.0.1"))
    }

    func test_malformed_input_returns_nil() {
        XCTAssertNil(IPRange(""))
        XCTAssertNil(IPRange("   "))
        XCTAssertNil(IPRange("не адрес"))
        XCTAssertNil(IPRange("10.0.0.0/33"))
        XCTAssertNil(IPRange("10.0.0.0/-1"))
        XCTAssertNil(IPRange("2606:2040::/129"))
        XCTAssertNil(IPRange("10.0.0.0/8/8"))
        XCTAssertNil(IPRange("999.0.0.1"))
    }

    func test_contains_rejects_malformed_candidate() {
        XCTAssertFalse(IPRange("10.0.0.0/8")!.contains("не адрес"))
    }
```

- [ ] **Step 6: Прогнать, убедиться что проходят**

Run: `swift test --filter IPRangeTests`
Expected: PASS, 9 тестов.

- [ ] **Step 7: Коммит**

```bash
git add Sources/WetoCore/IPRange.swift Tests/WetoCoreTests/IPRangeTests.swift
git commit -m "$TASK_NO - Разбор и матчинг CIDR"
```

---

## Task 3: GuardPolicy — чистое ядро решения

Сердце проекта. Всё, что решает «убивать или нет», живёт здесь и не знает ни про сеть, ни про процессы, ни про SwiftUI. Тесты синхронные, без единого мока.

**Files:**
- Create: `Sources/WetoCore/GuardPolicy.swift`
- Test: `Tests/WetoCoreTests/GuardPolicyTests.swift`

**Interfaces:**
- Consumes: `VPNStatus`, `GeoOutcome`, `GeoReading` (Task 1), `IPRange` (Task 2).
- Produces: `GuardConfig`, `GuardSignals`, `UnsafeReason`, `GuardDecision`, `GuardPolicy.decide(_:) -> GuardDecision`.

- [ ] **Step 1: Написать падающий тест на безопасный путь и на уровень 1**

Файл `Tests/WetoCoreTests/GuardPolicyTests.swift`:

```swift
import XCTest
@testable import WetoCore

final class GuardPolicyTests: XCTestCase {

    // MARK: - Хелперы

    private func config(
        vpn: String? = "Happ",
        blocked: Set<String> = ["RU"],
        ranges: [IPRange] = [],
        targets: [String] = ["com.example.target"]
    ) -> GuardConfig {
        GuardConfig(
            vpnServiceName: vpn,
            blockedCountries: blocked,
            blockedIPRanges: ranges,
            targetBundleIDs: targets
        )
    }

    private func geo(
        ip: String = "203.0.113.28",
        primary: String = "KZ",
        confirmed: String? = "KZ",
        source: ConfirmSource? = .ipwhois
    ) -> GeoOutcome {
        .resolved(GeoReading(
            ip: ip,
            asn: "AS49791",
            primaryCountry: primary,
            confirmedCountry: confirmed,
            confirmSource: source
        ))
    }

    private func signals(
        enabled: Bool = true,
        vpn: VPNStatus = .up(isPrimary: true),
        geo geoOutcome: GeoOutcome? = nil,
        config configuration: GuardConfig? = nil
    ) -> GuardSignals {
        GuardSignals(
            isEnabled: enabled,
            vpn: vpn,
            geo: geoOutcome ?? geo(),
            config: configuration ?? config()
        )
    }

    // MARK: - Безопасный путь

    func test_everything_aligned_is_safe() {
        XCTAssertEqual(GuardPolicy.decide(signals()), .safe)
    }

    // MARK: - Шаг 0: охрана не работает

    func test_disabled_guard_is_always_safe_even_with_blocked_country() {
        let s = signals(enabled: false, vpn: .down, geo: geo(primary: "RU", confirmed: "RU"))
        XCTAssertEqual(GuardPolicy.decide(s), .safe)
    }

    func test_empty_target_list_is_safe() {
        let s = signals(vpn: .down, config: config(targets: []))
        XCTAssertEqual(GuardPolicy.decide(s), .safe)
    }

    // MARK: - Шаги 1–3: локальные проверки VPN

    func test_vpn_not_configured_kills() {
        let s = signals(config: config(vpn: nil))
        XCTAssertEqual(GuardPolicy.decide(s), .kill(.vpnNotConfigured))
    }

    func test_vpn_down_kills() {
        XCTAssertEqual(GuardPolicy.decide(signals(vpn: .down)), .kill(.vpnDown))
    }

    func test_vpn_up_but_not_primary_kills() {
        let s = signals(vpn: .up(isPrimary: false))
        XCTAssertEqual(GuardPolicy.decide(s), .kill(.vpnNotPrimary))
    }

    func test_vpn_check_precedes_geo_check() {
        // VPN упал и гео тоже плохое — причина должна быть про VPN,
        // потому что сетевая проба в этом случае не выполняется вовсе.
        let s = signals(vpn: .down, geo: .unavailable("timeout"))
        XCTAssertEqual(GuardPolicy.decide(s), .kill(.vpnDown))
    }
}
```

- [ ] **Step 2: Прогнать тест, убедиться что падает**

Run: `swift test --filter GuardPolicyTests`
Expected: FAIL — `cannot find 'GuardConfig' in scope`.

- [ ] **Step 3: Реализовать типы и минимальную политику (шаги 0–3)**

Файл `Sources/WetoCore/GuardPolicy.swift`:

```swift
import Foundation

/// Настройки охраны, влияющие на решение.
public struct GuardConfig: Equatable, Sendable {
    /// `UserDefinedName` сервиса из SCDynamicStore, например "Happ".
    public let vpnServiceName: String?
    /// Коды стран ISO alpha-2 в верхнем регистре.
    public let blockedCountries: Set<String>
    public let blockedIPRanges: [IPRange]
    /// Bundle ID приложений под охраной. Целей может быть несколько.
    public let targetBundleIDs: [String]

    public init(
        vpnServiceName: String?,
        blockedCountries: Set<String>,
        blockedIPRanges: [IPRange],
        targetBundleIDs: [String]
    ) {
        self.vpnServiceName = vpnServiceName
        self.blockedCountries = blockedCountries
        self.blockedIPRanges = blockedIPRanges
        self.targetBundleIDs = targetBundleIDs
    }
}

/// Полный снимок мира, на основании которого принимается решение.
public struct GuardSignals: Equatable, Sendable {
    public let isEnabled: Bool
    public let vpn: VPNStatus
    public let geo: GeoOutcome
    public let config: GuardConfig

    public init(isEnabled: Bool, vpn: VPNStatus, geo: GeoOutcome, config: GuardConfig) {
        self.isEnabled = isEnabled
        self.vpn = vpn
        self.geo = geo
        self.config = config
    }
}

/// Причина, по которой цели признаны небезопасными.
///
/// Типизирована намеренно: уходит в журнал, в текст попапа и в цвет индикатора.
/// При ложном срабатывании должно быть видно, какое звено сработало.
public enum UnsafeReason: Equatable, Sendable {
    case vpnNotConfigured
    case vpnDown
    case vpnNotPrimary
    case geoUnavailable(String)
    case blacklistedIP(String)
    case blockedCountry(code: String, source: String)
    case confirmationUnavailable
    case countryConflict(primary: String, confirmed: String)
}

public enum GuardDecision: Equatable, Sendable {
    case safe
    case kill(UnsafeReason)
}

/// Чистая функция принятия решения. Ни одного системного вызова.
public enum GuardPolicy {

    public static func decide(_ signals: GuardSignals) -> GuardDecision {
        // Шаг 0: охранять нечего или незачем.
        guard signals.isEnabled, !signals.config.targetBundleIDs.isEmpty else { return .safe }

        // Шаги 1–3: локально, без сети, за микросекунды.
        guard signals.config.vpnServiceName != nil else { return .kill(.vpnNotConfigured) }

        switch signals.vpn {
        case .notConfigured:
            return .kill(.vpnNotConfigured)
        case .down:
            return .kill(.vpnDown)
        case .up(let isPrimary):
            guard isPrimary else { return .kill(.vpnNotPrimary) }
        }

        return .safe
    }
}
```

- [ ] **Step 4: Прогнать тесты, убедиться что проходят**

Run: `swift test --filter GuardPolicyTests`
Expected: PASS, 7 тестов.

- [ ] **Step 5: Дописать тесты на шаги 4–9 (сетевая часть)**

Дописать в `GuardPolicyTests`:

```swift
    // MARK: - Шаг 4: ipinfo молчит

    func test_geo_unavailable_kills_with_reason_text() {
        let s = signals(geo: .unavailable("timeout"))
        XCTAssertEqual(GuardPolicy.decide(s), .kill(.geoUnavailable("timeout")))
    }

    // MARK: - Шаг 5: чёрный список IP

    func test_blacklisted_ip_kills_even_when_country_is_allowed() {
        let s = signals(
            geo: geo(ip: "198.51.100.231", primary: "KZ", confirmed: "KZ"),
            config: config(ranges: [IPRange("198.51.100.0/22")!])
        )
        XCTAssertEqual(GuardPolicy.decide(s), .kill(.blacklistedIP("198.51.100.231")))
    }

    func test_blacklist_precedes_country_check() {
        // IP в чёрном списке И страна заблокирована — причина должна быть про IP,
        // она точнее.
        let s = signals(
            geo: geo(ip: "198.51.100.231", primary: "RU", confirmed: "RU"),
            config: config(ranges: [IPRange("198.51.100.231")!])
        )
        XCTAssertEqual(GuardPolicy.decide(s), .kill(.blacklistedIP("198.51.100.231")))
    }

    func test_ip_outside_all_ranges_does_not_trigger_blacklist() {
        let s = signals(config: config(ranges: [IPRange("10.0.0.0/8")!]))
        XCTAssertEqual(GuardPolicy.decide(s), .safe)
    }

    // MARK: - Шаги 6 и 8: заблокированная страна

    func test_blocked_primary_country_kills() {
        let s = signals(geo: geo(primary: "RU", confirmed: "RU"))
        XCTAssertEqual(
            GuardPolicy.decide(s),
            .kill(.blockedCountry(code: "RU", source: "ipinfo"))
        )
    }

    func test_blocked_confirmed_country_kills_even_when_primary_is_allowed() {
        let s = signals(geo: geo(primary: "KZ", confirmed: "RU", source: .ipwhois))
        XCTAssertEqual(
            GuardPolicy.decide(s),
            .kill(.blockedCountry(code: "RU", source: "ipwhois"))
        )
    }

    func test_country_comparison_is_case_insensitive() {
        let s = signals(
            geo: geo(primary: "ru", confirmed: "ru"),
            config: config(blocked: ["RU"])
        )
        XCTAssertEqual(
            GuardPolicy.decide(s),
            .kill(.blockedCountry(code: "RU", source: "ipinfo"))
        )
    }

    // MARK: - Шаг 7: строгий fail-closed на подтверждении

    func test_missing_confirmation_kills_even_when_ipinfo_says_allowed_country() {
        // Осознанное решение владельца: одновременное падение ipwho.is и geojs
        // убивает цели, хотя ipinfo уверенно сообщил безопасную страну.
        let s = signals(geo: geo(primary: "KZ", confirmed: nil, source: nil))
        XCTAssertEqual(GuardPolicy.decide(s), .kill(.confirmationUnavailable))
    }

    func test_blocked_primary_country_precedes_missing_confirmation() {
        let s = signals(geo: geo(primary: "RU", confirmed: nil, source: nil))
        XCTAssertEqual(
            GuardPolicy.decide(s),
            .kill(.blockedCountry(code: "RU", source: "ipinfo"))
        )
    }

    // MARK: - Шаг 9: расхождение

    func test_country_conflict_kills() {
        let s = signals(geo: geo(primary: "KZ", confirmed: "DE"))
        XCTAssertEqual(
            GuardPolicy.decide(s),
            .kill(.countryConflict(primary: "KZ", confirmed: "DE"))
        )
    }

    func test_matching_countries_in_different_case_are_not_a_conflict() {
        let s = signals(geo: geo(primary: "KZ", confirmed: "kz"))
        XCTAssertEqual(GuardPolicy.decide(s), .safe)
    }
```

- [ ] **Step 6: Прогнать, убедиться что падают именно новые тесты**

Run: `swift test --filter GuardPolicyTests`
Expected: FAIL — 11 новых тестов падают с `.safe` вместо ожидаемых `.kill(...)`, старые 7 проходят.

- [ ] **Step 7: Дописать сетевую часть политики**

Заменить в `GuardPolicy.decide` строку `return .safe` в конце на:

```swift
        // Шаги 4–9: требуют результата сетевой пробы.
        guard case .resolved(let reading) = signals.geo else {
            if case .unavailable(let detail) = signals.geo {
                return .kill(.geoUnavailable(detail))
            }
            return .kill(.geoUnavailable("нет данных"))
        }

        if let range = signals.config.blockedIPRanges.first(where: { $0.contains(reading.ip) }) {
            _ = range
            return .kill(.blacklistedIP(reading.ip))
        }

        let blocked = Set(signals.config.blockedCountries.map { $0.uppercased() })
        let primary = reading.primaryCountry.uppercased()

        if blocked.contains(primary) {
            return .kill(.blockedCountry(code: primary, source: "ipinfo"))
        }

        guard let confirmedRaw = reading.confirmedCountry else {
            return .kill(.confirmationUnavailable)
        }
        let confirmed = confirmedRaw.uppercased()

        if blocked.contains(confirmed) {
            let source = reading.confirmSource?.rawValue ?? "confirm"
            return .kill(.blockedCountry(code: confirmed, source: source))
        }

        if primary != confirmed {
            return .kill(.countryConflict(primary: primary, confirmed: confirmed))
        }

        return .safe
```

- [ ] **Step 8: Прогнать, убедиться что все проходят**

Run: `swift test --filter GuardPolicyTests`
Expected: PASS, 18 тестов.

- [ ] **Step 9: Прогнать весь набор целиком**

Run: `swift test`
Expected: PASS, 27 тестов.

- [ ] **Step 10: Коммит**

```bash
git add Sources/WetoCore/GuardPolicy.swift Tests/WetoCoreTests/GuardPolicyTests.swift
git commit -m "$TASK_NO - Чистое ядро политики охраны"
```

---

## Task 4: VPNStatusResolver — VPN из снимка сети

`SCDynamicStore` читается в Task 7, здесь — чистое вычисление статуса из снимка как из структуры данных.

**Files:**
- Create: `Sources/WetoCore/Model/NetworkSnapshot.swift`
- Create: `Sources/WetoCore/VPNStatusResolver.swift`
- Test: `Tests/WetoCoreTests/VPNStatusResolverTests.swift`

**Interfaces:**
- Consumes: `VPNStatus` (Task 1).
- Produces: `NetworkServiceSnapshot(uuid:name:activeInterface:)`, `NetworkSnapshot(services:primaryServiceUUID:)`, `NetworkSnapshot.vpnCandidateNames: [String]`, `VPNStatusResolver.status(serviceName:in:) -> VPNStatus`.

- [ ] **Step 1: Написать падающий тест на реальном снимке машины владельца**

Файл `Tests/WetoCoreTests/VPNStatusResolverTests.swift`:

```swift
import XCTest
@testable import WetoCore

final class VPNStatusResolverTests: XCTestCase {

    /// Снимок снят с машины владельца 2026-07-27, см. раздел 13 спеки.
    private let snapshot = NetworkSnapshot(
        services: [
            .init(uuid: "108E2488", name: "Wi-Fi", activeInterface: "en0"),
            .init(uuid: "BC2D1D42", name: "Happ", activeInterface: "utun6"),
            .init(uuid: "F8D44BFF", name: "Tailscale", activeInterface: "utun7"),
            .init(uuid: "AC98ABFF", name: "KARO", activeInterface: nil),
            .init(uuid: "9BA5C511", name: "iPhone", activeInterface: nil),
            .init(uuid: "13102BC0", name: "Thunderbolt Bridge", activeInterface: nil),
        ],
        primaryServiceUUID: "BC2D1D42"
    )

    func test_nil_name_is_not_configured() {
        XCTAssertEqual(VPNStatusResolver.status(serviceName: nil, in: snapshot), .notConfigured)
    }

    func test_unknown_service_name_is_down() {
        // Сервис удалили из системных настроек, а в наших настройках он остался.
        XCTAssertEqual(VPNStatusResolver.status(serviceName: "Ghost", in: snapshot), .down)
    }

    func test_configured_but_not_active_service_is_down() {
        XCTAssertEqual(VPNStatusResolver.status(serviceName: "KARO", in: snapshot), .down)
    }

    func test_active_service_holding_default_route_is_up_and_primary() {
        XCTAssertEqual(
            VPNStatusResolver.status(serviceName: "Happ", in: snapshot),
            .up(isPrimary: true)
        )
    }

    func test_active_service_not_holding_default_route_is_up_but_not_primary() {
        // Tailscale поднят, но default route держит Happ — ровно тот случай,
        // ради которого проверяется PrimaryService.
        XCTAssertEqual(
            VPNStatusResolver.status(serviceName: "Tailscale", in: snapshot),
            .up(isPrimary: false)
        )
    }

    func test_no_primary_service_means_not_primary() {
        let orphan = NetworkSnapshot(services: snapshot.services, primaryServiceUUID: nil)
        XCTAssertEqual(
            VPNStatusResolver.status(serviceName: "Happ", in: orphan),
            .up(isPrimary: false)
        )
    }

    func test_candidate_names_are_sorted_and_unique() {
        XCTAssertEqual(
            snapshot.vpnCandidateNames,
            ["Happ", "KARO", "Thunderbolt Bridge", "Tailscale", "Wi-Fi", "iPhone"]
        )
    }
}
```

- [ ] **Step 2: Прогнать, убедиться что падает**

Run: `swift test --filter VPNStatusResolverTests`
Expected: FAIL — `cannot find 'NetworkSnapshot' in scope`.

- [ ] **Step 3: Создать модель снимка**

Файл `Sources/WetoCore/Model/NetworkSnapshot.swift`:

```swift
import Foundation

/// Один сетевой сервис macOS в том виде, в каком его отдаёт SCDynamicStore.
///
/// `name` — это `UserDefinedName` из `Setup:/Network/Service/<uuid>`. Опора на имя,
/// а не на интерфейс, принципиальна: номера `utunN` плавают между перезагрузками,
/// а имя стабильно и понятно пользователю.
public struct NetworkServiceSnapshot: Equatable, Sendable {
    public let uuid: String
    public let name: String
    /// Интерфейс из `State:/Network/Service/<uuid>/IPv4`.
    /// `nil` означает, что ключа нет, то есть адреса на сервисе нет и туннель не поднят.
    public let activeInterface: String?

    public init(uuid: String, name: String, activeInterface: String?) {
        self.uuid = uuid
        self.name = name
        self.activeInterface = activeInterface
    }
}

/// Полный снимок сетевой конфигурации на момент опроса.
public struct NetworkSnapshot: Equatable, Sendable {
    public let services: [NetworkServiceSnapshot]
    /// `PrimaryService` из `State:/Network/Global/IPv4` — кто держит default route.
    public let primaryServiceUUID: String?

    public init(services: [NetworkServiceSnapshot], primaryServiceUUID: String?) {
        self.services = services
        self.primaryServiceUUID = primaryServiceUUID
    }

    /// Имена сервисов для выпадающего списка в настройках.
    public var vpnCandidateNames: [String] {
        Array(Set(services.map(\.name))).sorted()
    }
}
```

- [ ] **Step 4: Создать резолвер**

Файл `Sources/WetoCore/VPNStatusResolver.swift`:

```swift
import Foundation

/// Вычисление `VPNStatus` из снимка сети. Чистая функция.
public enum VPNStatusResolver {

    public static func status(serviceName: String?, in snapshot: NetworkSnapshot) -> VPNStatus {
        guard let serviceName else { return .notConfigured }

        // Сервис мог исчезнуть из системных настроек, а у нас остаться в конфиге.
        // Это неотличимо от «не поднят» с точки зрения безопасности.
        guard let service = snapshot.services.first(where: { $0.name == serviceName }) else {
            return .down
        }

        guard service.activeInterface != nil else { return .down }

        return .up(isPrimary: snapshot.primaryServiceUUID == service.uuid)
    }
}
```

- [ ] **Step 5: Прогнать, убедиться что проходят**

Run: `swift test --filter VPNStatusResolverTests`
Expected: PASS, 7 тестов.

- [ ] **Step 6: Коммит**

```bash
git add Sources/WetoCore/Model/NetworkSnapshot.swift \
        Sources/WetoCore/VPNStatusResolver.swift \
        Tests/WetoCoreTests/VPNStatusResolverTests.swift
git commit -m "$TASK_NO - Вычисление статуса VPN из снимка сети"
```

---

## Task 5: ProcessMatcher — отбор процессов цели

`NSRunningApplication` даёт только GUI-процессы, а у современных приложений рядом живут хелперы-рендереры и XPC-сервисы. Отбор идёт по пути исполняемого файла внутри бандла. Само перечисление процессов — Task 9, здесь чистая функция отбора.

**Files:**
- Create: `Sources/WetoCore/Model/ProcessSnapshot.swift`
- Create: `Sources/WetoCore/ProcessMatcher.swift`
- Test: `Tests/WetoCoreTests/ProcessMatcherTests.swift`

**Interfaces:**
- Consumes: ничего.
- Produces: `ProcessSnapshot(pid:executablePath:)`, `ProcessMatcher.pids(in:belongingTo:) -> [Int32]`.

- [ ] **Step 1: Написать падающий тест**

Файл `Tests/WetoCoreTests/ProcessMatcherTests.swift`:

```swift
import XCTest
@testable import WetoCore

final class ProcessMatcherTests: XCTestCase {

    private let processes: [ProcessSnapshot] = [
        .init(pid: 100, executablePath: "/Applications/Target.app/Contents/MacOS/Target"),
        .init(pid: 101, executablePath: "/Applications/Target.app/Contents/Frameworks/Target Helper.app/Contents/MacOS/Target Helper"),
        .init(pid: 102, executablePath: "/Applications/Other.app/Contents/MacOS/Other"),
        .init(pid: 103, executablePath: "/usr/bin/ssh"),
        .init(pid: 104, executablePath: "/Applications/TargetExtra.app/Contents/MacOS/TargetExtra"),
    ]

    func test_matches_main_binary_and_nested_helpers() {
        let pids = ProcessMatcher.pids(in: processes, belongingTo: ["/Applications/Target.app"])
        XCTAssertEqual(pids, [100, 101])
    }

    func test_does_not_match_sibling_bundle_with_shared_prefix() {
        // "/Applications/TargetExtra.app" начинается с "/Applications/Target",
        // но целью не является — сравнение обязано учитывать границу пути.
        let pids = ProcessMatcher.pids(in: processes, belongingTo: ["/Applications/Target.app"])
        XCTAssertFalse(pids.contains(104))
    }

    func test_matches_across_several_targets() {
        let pids = ProcessMatcher.pids(
            in: processes,
            belongingTo: ["/Applications/Target.app", "/Applications/Other.app"]
        )
        XCTAssertEqual(pids, [100, 101, 102])
    }

    func test_trailing_slash_in_bundle_path_is_tolerated() {
        let pids = ProcessMatcher.pids(in: processes, belongingTo: ["/Applications/Target.app/"])
        XCTAssertEqual(pids, [100, 101])
    }

    func test_empty_target_list_matches_nothing() {
        XCTAssertTrue(ProcessMatcher.pids(in: processes, belongingTo: []).isEmpty)
    }

    func test_result_is_deduplicated_when_bundles_overlap() {
        let pids = ProcessMatcher.pids(
            in: processes,
            belongingTo: ["/Applications/Target.app", "/Applications/Target.app"]
        )
        XCTAssertEqual(pids, [100, 101])
    }
}
```

- [ ] **Step 2: Прогнать, убедиться что падает**

Run: `swift test --filter ProcessMatcherTests`
Expected: FAIL — `cannot find 'ProcessSnapshot' in scope`.

- [ ] **Step 3: Реализовать модель и матчер**

Файл `Sources/WetoCore/Model/ProcessSnapshot.swift`:

```swift
import Foundation

/// Процесс в том виде, в каком его отдаёт `proc_listallpids` + `proc_pidpath`.
public struct ProcessSnapshot: Equatable, Sendable {
    public let pid: Int32
    public let executablePath: String

    public init(pid: Int32, executablePath: String) {
        self.pid = pid
        self.executablePath = executablePath
    }
}
```

Файл `Sources/WetoCore/ProcessMatcher.swift`:

```swift
import Foundation

/// Отбор процессов, принадлежащих бандлам целей. Чистая функция.
public enum ProcessMatcher {

    /// Возвращает pid всех процессов, чей исполняемый файл лежит внутри одного
    /// из указанных бандлов, в порядке появления во входном массиве.
    ///
    /// Сравнение идёт по префиксу с обязательным разделителем на границе,
    /// иначе `/Applications/TargetExtra.app` ошибочно попал бы в цель
    /// `/Applications/Target.app`.
    public static func pids(
        in processes: [ProcessSnapshot],
        belongingTo bundlePaths: [String]
    ) -> [Int32] {
        let prefixes = Set(bundlePaths.map { path -> String in
            var normalized = path
            while normalized.hasSuffix("/") { normalized.removeLast() }
            return normalized + "/"
        })
        guard !prefixes.isEmpty else { return [] }

        var seen = Set<Int32>()
        var result: [Int32] = []
        for process in processes where prefixes.contains(where: { process.executablePath.hasPrefix($0) }) {
            if seen.insert(process.pid).inserted {
                result.append(process.pid)
            }
        }
        return result
    }
}
```

- [ ] **Step 4: Прогнать, убедиться что проходят**

Run: `swift test --filter ProcessMatcherTests`
Expected: PASS, 6 тестов.

- [ ] **Step 5: Коммит**

```bash
git add Sources/WetoCore/Model/ProcessSnapshot.swift \
        Sources/WetoCore/ProcessMatcher.swift \
        Tests/WetoCoreTests/ProcessMatcherTests.swift
git commit -m "$TASK_NO - Отбор процессов целей по пути бандла"
```

---

## Task 6: Разбор ответов гео-сервисов

Фикстуры — настоящие ответы, снятые с машины владельца 2026-07-27 (раздел 13 спеки). HTTP появится в Task 8, здесь только `Decodable` и сборка `GeoReading`.

**Files:**
- Create: `Sources/WetoCore/GeoResponses.swift`
- Test: `Tests/WetoCoreTests/GeoResponsesTests.swift`

**Interfaces:**
- Consumes: `GeoReading`, `ConfirmSource` (Task 1).
- Produces: `IPInfoLiteResponse`, `IPWhoIsResponse`, `GeoJSCountryResponse` (все `Decodable`), `GeoResponses.decodeIPInfo(_:) throws -> IPInfoLiteResponse`, `GeoResponses.decodeIPWhoIs(_:) throws -> String?`, `GeoResponses.decodeGeoJS(_:) throws -> String?`, `GeoResponses.makeReading(ipinfo:confirmedCountry:source:) -> GeoReading`.

- [ ] **Step 1: Написать падающий тест на реальных ответах**

Файл `Tests/WetoCoreTests/GeoResponsesTests.swift`:

```swift
import XCTest
@testable import WetoCore

final class GeoResponsesTests: XCTestCase {

    // Ответы сняты с машины владельца 2026-07-27.

    private let ipinfoJSON = Data("""
    {"ip":"203.0.113.28","asn":"AS49791","as_name":"Newserverlife LLC",
     "as_domain":"3hcloud.com","country_code":"KZ","country":"Kazakhstan",
     "continent_code":"AS","continent":"Asia"}
    """.utf8)

    private let ipwhoisJSON = Data("""
    {"ip":"203.0.113.28","success":true,"type":"IPv4","continent":"Asia",
     "continent_code":"AS","country":"Kazakhstan","country_code":"KZ",
     "region":"Almaty","city":"Almaty","is_eu":false,
     "connection":{"asn":49791,"org":"3hcloud LLC","isp":"Newserverlife LLC",
     "domain":"3hcloud.com"}}
    """.utf8)

    private let geojsJSON = Data("""
    {"country":"KZ","country_3":"KAZ","ip":"203.0.113.28","name":"Kazakhstan"}
    """.utf8)

    func test_ipinfo_response_is_decoded() throws {
        let response = try GeoResponses.decodeIPInfo(ipinfoJSON)
        XCTAssertEqual(response.ip, "203.0.113.28")
        XCTAssertEqual(response.countryCode, "KZ")
        XCTAssertEqual(response.asn, "AS49791")
    }

    func test_ipwhois_response_yields_country_code() throws {
        XCTAssertEqual(try GeoResponses.decodeIPWhoIs(ipwhoisJSON), "KZ")
    }

    func test_ipwhois_failure_response_yields_nil() throws {
        let failure = Data(#"{"ip":"1.1.1.1","success":false,"message":"Invalid IP"}"#.utf8)
        XCTAssertNil(try GeoResponses.decodeIPWhoIs(failure))
    }

    func test_geojs_response_yields_country_code() throws {
        XCTAssertEqual(try GeoResponses.decodeGeoJS(geojsJSON), "KZ")
    }

    func test_reading_is_assembled_from_ipinfo_and_confirmation() throws {
        let response = try GeoResponses.decodeIPInfo(ipinfoJSON)
        let reading = GeoResponses.makeReading(
            ipinfo: response,
            confirmedCountry: "KZ",
            source: .ipwhois
        )
        XCTAssertEqual(reading.ip, "203.0.113.28")
        XCTAssertEqual(reading.asn, "AS49791")
        XCTAssertEqual(reading.primaryCountry, "KZ")
        XCTAssertEqual(reading.confirmedCountry, "KZ")
        XCTAssertEqual(reading.confirmSource, .ipwhois)
    }

    func test_reading_without_confirmation_keeps_nil_source() throws {
        let response = try GeoResponses.decodeIPInfo(ipinfoJSON)
        let reading = GeoResponses.makeReading(
            ipinfo: response,
            confirmedCountry: nil,
            source: nil
        )
        XCTAssertNil(reading.confirmedCountry)
        XCTAssertNil(reading.confirmSource)
    }

    func test_malformed_json_throws() {
        XCTAssertThrowsError(try GeoResponses.decodeIPInfo(Data("не json".utf8)))
    }
}
```

- [ ] **Step 2: Прогнать, убедиться что падает**

Run: `swift test --filter GeoResponsesTests`
Expected: FAIL — `cannot find 'GeoResponses' in scope`.

- [ ] **Step 3: Реализовать разбор**

Файл `Sources/WetoCore/GeoResponses.swift`:

```swift
import Foundation

/// Ответ `https://v4.api.ipinfo.io/lite/me` — единственный источник внешнего IP.
public struct IPInfoLiteResponse: Decodable, Equatable, Sendable {
    public let ip: String
    public let asn: String?
    public let asName: String?
    public let countryCode: String
    public let country: String?

    enum CodingKeys: String, CodingKey {
        case ip
        case asn
        case asName = "as_name"
        case countryCode = "country_code"
        case country
    }
}

/// Ответ `https://ipwho.is/{ip}`. При ошибке сервис отвечает 200 с `success: false`,
/// поэтому статус-кода недостаточно и флаг надо разбирать явно.
public struct IPWhoIsResponse: Decodable, Equatable, Sendable {
    public let success: Bool
    public let countryCode: String?

    enum CodingKeys: String, CodingKey {
        case success
        case countryCode = "country_code"
    }
}

/// Ответ `https://get.geojs.io/v1/ip/country/{ip}.json`.
public struct GeoJSCountryResponse: Decodable, Equatable, Sendable {
    public let country: String
    public let ip: String
}

/// Разбор ответов гео-сервисов и сборка показания.
public enum GeoResponses {

    private static let decoder = JSONDecoder()

    public static func decodeIPInfo(_ data: Data) throws -> IPInfoLiteResponse {
        try decoder.decode(IPInfoLiteResponse.self, from: data)
    }

    /// Возвращает код страны либо `nil`, если сервис сообщил о неуспехе.
    public static func decodeIPWhoIs(_ data: Data) throws -> String? {
        let response = try decoder.decode(IPWhoIsResponse.self, from: data)
        guard response.success else { return nil }
        return response.countryCode
    }

    public static func decodeGeoJS(_ data: Data) throws -> String? {
        try decoder.decode(GeoJSCountryResponse.self, from: data).country
    }

    public static func makeReading(
        ipinfo: IPInfoLiteResponse,
        confirmedCountry: String?,
        source: ConfirmSource?
    ) -> GeoReading {
        GeoReading(
            ip: ipinfo.ip,
            asn: ipinfo.asn,
            primaryCountry: ipinfo.countryCode,
            confirmedCountry: confirmedCountry,
            confirmSource: source
        )
    }
}
```

- [ ] **Step 4: Прогнать, убедиться что проходят**

Run: `swift test --filter GeoResponsesTests`
Expected: PASS, 7 тестов.

- [ ] **Step 5: Прогнать весь набор**

Run: `swift test`
Expected: PASS, 40 тестов. `WetoCore` закончен целиком.

- [ ] **Step 6: Коммит**

```bash
git add Sources/WetoCore/GeoResponses.swift Tests/WetoCoreTests/GeoResponsesTests.swift
git commit -m "$TASK_NO - Разбор ответов гео-сервисов"
```

---

## Task 7: NetworkSnapshotReader — чтение SCDynamicStore

Первая задача в `WetoSystem`. Здесь появляется таргет и его тесты.

**Files:**
- Modify: `Package.swift` — добавить таргеты `WetoSystem` и `WetoSystemTests`
- Create: `Sources/WetoSystem/NetworkSnapshotReader.swift`
- Test: `Tests/WetoSystemTests/NetworkSnapshotReaderTests.swift`

**Interfaces:**
- Consumes: `NetworkSnapshot`, `NetworkServiceSnapshot` (Task 4).
- Produces: протокол `NetworkSnapshotReading { func snapshot() -> NetworkSnapshot }` и его реализация `NetworkSnapshotReader`.

- [ ] **Step 1: Добавить таргеты в `Package.swift`**

В `products` добавить `.library(name: "WetoSystem", targets: ["WetoSystem"])`, в `targets` — перед тестовым таргетом Core:

```swift
        .target(
            name: "WetoSystem",
            dependencies: ["WetoCore"],
            path: "Sources/WetoSystem"
        ),
```

и после него:

```swift
        .testTarget(
            name: "WetoSystemTests",
            dependencies: ["WetoSystem", "WetoCore"],
            path: "Tests/WetoSystemTests"
        ),
```

- [ ] **Step 2: Написать падающий тест**

Файл `Tests/WetoSystemTests/NetworkSnapshotReaderTests.swift`:

```swift
import XCTest
@testable import WetoSystem
import WetoCore

/// Тест работает с живым SCDynamicStore, поэтому проверяет структурные
/// инварианты, а не конкретные сервисы: на машине разработчика и на CI-раннере
/// набор интерфейсов разный.
final class NetworkSnapshotReaderTests: XCTestCase {

    func test_reader_returns_snapshot_without_crashing() {
        let snapshot = NetworkSnapshotReader().snapshot()
        XCTAssertNotNil(snapshot)
    }

    func test_every_service_has_non_empty_uuid_and_name() {
        for service in NetworkSnapshotReader().snapshot().services {
            XCTAssertFalse(service.uuid.isEmpty, "пустой UUID у сервиса \(service.name)")
            XCTAssertFalse(service.name.isEmpty, "пустое имя у сервиса \(service.uuid)")
        }
    }

    func test_primary_service_when_present_exists_among_services() {
        let snapshot = NetworkSnapshotReader().snapshot()
        guard let primary = snapshot.primaryServiceUUID else { return }
        XCTAssertTrue(
            snapshot.services.contains { $0.uuid == primary },
            "PrimaryService \(primary) отсутствует в списке сервисов"
        )
    }

    func test_active_interface_names_are_non_empty_when_present() {
        for service in NetworkSnapshotReader().snapshot().services {
            guard let iface = service.activeInterface else { continue }
            XCTAssertFalse(iface.isEmpty)
        }
    }

    func test_repeated_reads_are_stable() {
        let reader = NetworkSnapshotReader()
        XCTAssertEqual(reader.snapshot().services.count, reader.snapshot().services.count)
    }
}
```

- [ ] **Step 3: Прогнать, убедиться что падает**

Run: `swift test --filter NetworkSnapshotReaderTests`
Expected: FAIL — `cannot find 'NetworkSnapshotReader' in scope`.

- [ ] **Step 4: Реализовать читатель**

Файл `Sources/WetoSystem/NetworkSnapshotReader.swift`:

```swift
import Foundation
import SystemConfiguration
import WetoCore

/// Граница системы: чтение сетевой конфигурации.
public protocol NetworkSnapshotReading: Sendable {
    func snapshot() -> NetworkSnapshot
}

/// Чтение сетевых сервисов из SCDynamicStore.
///
/// Три группы ключей, проверены на машине владельца:
///   `Setup:/Network/Service/<uuid>`       → `UserDefinedName` (человекочитаемое имя)
///   `State:/Network/Service/<uuid>/IPv4`  → `InterfaceName` (ключа нет = туннель не поднят)
///   `State:/Network/Global/IPv4`          → `PrimaryService` (кто держит default route)
public struct NetworkSnapshotReader: NetworkSnapshotReading {

    private static let servicePattern = "Setup:/Network/Service/[^/]+/IPv4"

    public init() {}

    public func snapshot() -> NetworkSnapshot {
        guard let store = SCDynamicStoreCreate(nil, "com.weto.app" as CFString, nil, nil) else {
            return NetworkSnapshot(services: [], primaryServiceUUID: nil)
        }

        let uuids = serviceUUIDs(store: store)
        let services = uuids.compactMap { uuid -> NetworkServiceSnapshot? in
            guard let name = userDefinedName(store: store, uuid: uuid) else { return nil }
            return NetworkServiceSnapshot(
                uuid: uuid,
                name: name,
                activeInterface: activeInterface(store: store, uuid: uuid)
            )
        }

        return NetworkSnapshot(services: services, primaryServiceUUID: primaryService(store: store))
    }

    // MARK: - Private

    private func serviceUUIDs(store: SCDynamicStore) -> [String] {
        guard let keys = SCDynamicStoreCopyKeyList(store, Self.servicePattern as CFString) as? [String]
        else { return [] }

        // "Setup:/Network/Service/<uuid>/IPv4" → "<uuid>"
        return keys.compactMap { key in
            let parts = key.split(separator: "/")
            guard parts.count >= 2 else { return nil }
            return String(parts[parts.count - 2])
        }
    }

    private func userDefinedName(store: SCDynamicStore, uuid: String) -> String? {
        let key = "Setup:/Network/Service/\(uuid)" as CFString
        guard let dict = SCDynamicStoreCopyValue(store, key) as? [String: Any] else { return nil }
        return dict["UserDefinedName"] as? String
    }

    private func activeInterface(store: SCDynamicStore, uuid: String) -> String? {
        let key = "State:/Network/Service/\(uuid)/IPv4" as CFString
        guard let dict = SCDynamicStoreCopyValue(store, key) as? [String: Any] else { return nil }
        return dict["InterfaceName"] as? String
    }

    private func primaryService(store: SCDynamicStore) -> String? {
        let key = "State:/Network/Global/IPv4" as CFString
        guard let dict = SCDynamicStoreCopyValue(store, key) as? [String: Any] else { return nil }
        return dict["PrimaryService"] as? String
    }
}
```

- [ ] **Step 5: Прогнать, убедиться что проходят**

Run: `swift test --filter NetworkSnapshotReaderTests`
Expected: PASS, 5 тестов.

- [ ] **Step 6: Проверить руками, что данные совпадают с системой**

```bash
swift run --package-path . 2>/dev/null || true
scutil <<< "show State:/Network/Global/IPv4"
```

Сверить `PrimaryService` из вывода `scutil` с тем, что вернул читатель. На машине владельца ожидается сервис с именем `Happ` на интерфейсе `utun6`.

- [ ] **Step 7: Коммит**

```bash
git add Package.swift Sources/WetoSystem/NetworkSnapshotReader.swift \
        Tests/WetoSystemTests/NetworkSnapshotReaderTests.swift
git commit -m "$TASK_NO - Чтение сетевых сервисов из SCDynamicStore"
```

---

## Task 8: GeoProbe — опрос гео-сервисов с кэшем по IP

Ключевое поведение: `ipinfo` опрашивается каждый такт, подтверждающий сервис — только при смене IP. Это и есть то, что укладывает нас в лимит `ipwho.is` в 1000 запросов в сутки.

**Files:**
- Create: `Sources/WetoSystem/HTTPFetching.swift`
- Create: `Sources/WetoSystem/GeoProbe.swift`
- Test: `Tests/WetoSystemTests/GeoProbeTests.swift`

**Interfaces:**
- Consumes: `Constants`, `GeoOutcome`, `GeoReading`, `GeoResponses`, `ConfirmSource` (Tasks 1, 6).
- Produces: протокол `HTTPFetching { func data(from url: URL, headers: [String: String]) async throws -> Data }`, реализация `URLSessionHTTPFetcher`, протокол `GeoProbing { func probe() async -> GeoOutcome }`, актор `GeoProbe`.

- [ ] **Step 1: Написать падающий тест на фейковом фетчере**

Файл `Tests/WetoSystemTests/GeoProbeTests.swift`:

```swift
import XCTest
@testable import WetoSystem
import WetoCore

private struct FetchFailure: Error {}

/// Фейк на границе системы. Отдаёт заранее заготовленные ответы по подстроке URL
/// и считает вызовы, чтобы проверить кэширование.
private actor FakeFetcher: HTTPFetching {
    var responses: [String: Result<Data, Error>]
    private(set) var callCounts: [String: Int] = [:]

    init(responses: [String: Result<Data, Error>]) {
        self.responses = responses
    }

    func data(from url: URL, headers: [String: String]) async throws -> Data {
        let key = responses.keys.first { url.absoluteString.contains($0) } ?? "unmatched"
        callCounts[key, default: 0] += 1
        guard let result = responses[key] else { throw FetchFailure() }
        return try result.get()
    }

    func count(_ key: String) -> Int { callCounts[key] ?? 0 }
    func setResponse(_ key: String, _ result: Result<Data, Error>) { responses[key] = result }
}

final class GeoProbeTests: XCTestCase {

    private func ipinfoData(ip: String, country: String) -> Data {
        Data("""
        {"ip":"\(ip)","asn":"AS49791","as_name":"Newserverlife LLC",
         "country_code":"\(country)","country":"Kazakhstan"}
        """.utf8)
    }

    private let ipwhoisKZ = Data(#"{"ip":"203.0.113.28","success":true,"country_code":"KZ"}"#.utf8)
    private let geojsKZ = Data(#"{"country":"KZ","country_3":"KAZ","ip":"203.0.113.28","name":"Kazakhstan"}"#.utf8)

    func test_ipinfo_failure_yields_unavailable() async {
        let fetcher = FakeFetcher(responses: ["ipinfo.io": .failure(FetchFailure())])
        let probe = GeoProbe(fetcher: fetcher, token: { "t" })

        guard case .unavailable = await probe.probe() else {
            return XCTFail("ожидался .unavailable")
        }
    }

    func test_missing_token_yields_unavailable_without_network_call() async {
        let fetcher = FakeFetcher(responses: [:])
        let probe = GeoProbe(fetcher: fetcher, token: { nil })

        guard case .unavailable = await probe.probe() else {
            return XCTFail("ожидался .unavailable")
        }
        let calls = await fetcher.count("ipinfo.io")
        XCTAssertEqual(calls, 0)
    }

    func test_successful_probe_uses_ipwhois_as_confirmation() async {
        let fetcher = FakeFetcher(responses: [
            "ipinfo.io": .success(ipinfoData(ip: "203.0.113.28", country: "KZ")),
            "ipwho.is": .success(ipwhoisKZ),
        ])
        let probe = GeoProbe(fetcher: fetcher, token: { "t" })

        guard case .resolved(let reading) = await probe.probe() else {
            return XCTFail("ожидался .resolved")
        }
        XCTAssertEqual(reading.ip, "203.0.113.28")
        XCTAssertEqual(reading.primaryCountry, "KZ")
        XCTAssertEqual(reading.confirmedCountry, "KZ")
        XCTAssertEqual(reading.confirmSource, .ipwhois)
    }

    func test_geojs_is_used_when_ipwhois_fails() async {
        let fetcher = FakeFetcher(responses: [
            "ipinfo.io": .success(ipinfoData(ip: "203.0.113.28", country: "KZ")),
            "ipwho.is": .failure(FetchFailure()),
            "geojs.io": .success(geojsKZ),
        ])
        let probe = GeoProbe(fetcher: fetcher, token: { "t" })

        guard case .resolved(let reading) = await probe.probe() else {
            return XCTFail("ожидался .resolved")
        }
        XCTAssertEqual(reading.confirmedCountry, "KZ")
        XCTAssertEqual(reading.confirmSource, .geojs)
    }

    func test_both_confirmations_failing_yields_resolved_without_confirmation() async {
        let fetcher = FakeFetcher(responses: [
            "ipinfo.io": .success(ipinfoData(ip: "203.0.113.28", country: "KZ")),
            "ipwho.is": .failure(FetchFailure()),
            "geojs.io": .failure(FetchFailure()),
        ])
        let probe = GeoProbe(fetcher: fetcher, token: { "t" })

        guard case .resolved(let reading) = await probe.probe() else {
            return XCTFail("ожидался .resolved, а не .unavailable — IP-то мы получили")
        }
        XCTAssertNil(reading.confirmedCountry)
        XCTAssertNil(reading.confirmSource)
    }

    func test_confirmation_is_cached_while_ip_is_unchanged() async {
        let fetcher = FakeFetcher(responses: [
            "ipinfo.io": .success(ipinfoData(ip: "203.0.113.28", country: "KZ")),
            "ipwho.is": .success(ipwhoisKZ),
        ])
        let probe = GeoProbe(fetcher: fetcher, token: { "t" })

        for _ in 0..<5 { _ = await probe.probe() }

        let ipinfoCalls = await fetcher.count("ipinfo.io")
        let confirmCalls = await fetcher.count("ipwho.is")
        XCTAssertEqual(ipinfoCalls, 5, "ipinfo опрашивается каждый такт")
        XCTAssertEqual(confirmCalls, 1, "подтверждение — только при смене IP")
    }

    func test_ip_change_invalidates_confirmation_cache() async {
        let fetcher = FakeFetcher(responses: [
            "ipinfo.io": .success(ipinfoData(ip: "203.0.113.28", country: "KZ")),
            "ipwho.is": .success(ipwhoisKZ),
        ])
        let probe = GeoProbe(fetcher: fetcher, token: { "t" })
        _ = await probe.probe()

        await fetcher.setResponse("ipinfo.io", .success(ipinfoData(ip: "198.51.100.231", country: "RU")))
        await fetcher.setResponse("ipwho.is", .success(Data(#"{"success":true,"country_code":"RU"}"#.utf8)))

        guard case .resolved(let reading) = await probe.probe() else {
            return XCTFail("ожидался .resolved")
        }
        XCTAssertEqual(reading.confirmedCountry, "RU")
        let confirmCalls = await fetcher.count("ipwho.is")
        XCTAssertEqual(confirmCalls, 2)
    }
}
```

- [ ] **Step 2: Прогнать, убедиться что падает**

Run: `swift test --filter GeoProbeTests`
Expected: FAIL — `cannot find 'HTTPFetching' in scope`.

- [ ] **Step 3: Реализовать границу HTTP**

Файл `Sources/WetoSystem/HTTPFetching.swift`:

```swift
import Foundation
import WetoCore

/// Граница системы: единственный способ, которым проект ходит в сеть.
public protocol HTTPFetching: Sendable {
    func data(from url: URL, headers: [String: String]) async throws -> Data
}

public enum HTTPFetchError: LocalizedError {
    case badStatus(Int)

    public var errorDescription: String? {
        switch self {
        case .badStatus(let code): return "HTTP \(code)"
        }
    }
}

/// Реальная реализация. Кэш URLSession отключён намеренно: устаревший ответ
/// гео-сервиса — это ровно та ситуация, от которой защищает всё приложение.
public struct URLSessionHTTPFetcher: HTTPFetching {

    private let session: URLSession

    public init(timeout: TimeInterval = Constants.geoRequestTimeoutSeconds) {
        let configuration = URLSessionConfiguration.ephemeral
        configuration.requestCachePolicy = .reloadIgnoringLocalAndRemoteCacheData
        configuration.urlCache = nil
        configuration.timeoutIntervalForRequest = timeout
        self.session = URLSession(configuration: configuration)
    }

    public func data(from url: URL, headers: [String: String]) async throws -> Data {
        var request = URLRequest(url: url)
        request.cachePolicy = .reloadIgnoringLocalAndRemoteCacheData
        for (key, value) in headers {
            request.setValue(value, forHTTPHeaderField: key)
        }

        let (data, response) = try await session.data(for: request)
        if let http = response as? HTTPURLResponse, !(200..<300).contains(http.statusCode) {
            throw HTTPFetchError.badStatus(http.statusCode)
        }
        return data
    }
}
```

- [ ] **Step 4: Реализовать `GeoProbe`**

Файл `Sources/WetoSystem/GeoProbe.swift`:

```swift
import Foundation
import WetoCore

/// Граница системы: получение вердикта о внешнем положении машины.
public protocol GeoProbing: Sendable {
    func probe() async -> GeoOutcome
}

/// Опрос гео-сервисов.
///
/// `ipinfo` — единственный источник IP: при split-routing остальные сервисы,
/// отвечая на вопрос «какой у меня адрес», возвращают посторонний IPv6
/// (проверено на машине владельца). Подтверждающий сервис спрашивается про уже
/// известный IP и потому кэшируется: страна фиксированного адреса между
/// опросами не меняется. Это укладывает нас в лимит ipwho.is в 1000 запросов
/// в сутки при опросе раз в 5 секунд.
///
/// Актор, потому что кэш мутируется из фонового цикла и из обработчиков
/// сетевых событий одновременно.
public actor GeoProbe: GeoProbing {

    private let fetcher: HTTPFetching
    private let token: @Sendable () -> String?

    private var cachedIP: String?
    private var cachedCountry: String?
    private var cachedSource: ConfirmSource?

    public init(fetcher: HTTPFetching, token: @escaping @Sendable () -> String?) {
        self.fetcher = fetcher
        self.token = token
    }

    public func probe() async -> GeoOutcome {
        guard let token = token(), !token.isEmpty else {
            return .unavailable("не задан токен ipinfo")
        }
        guard let url = URL(string: Constants.ipinfoLiteURL) else {
            return .unavailable("некорректный URL ipinfo")
        }

        let ipinfo: IPInfoLiteResponse
        do {
            let data = try await fetcher.data(from: url, headers: ["Authorization": "Bearer \(token)"])
            ipinfo = try GeoResponses.decodeIPInfo(data)
        } catch {
            return .unavailable(error.localizedDescription)
        }

        if ipinfo.ip != cachedIP {
            let confirmation = await confirm(ip: ipinfo.ip)
            cachedIP = ipinfo.ip
            cachedCountry = confirmation?.country
            cachedSource = confirmation?.source
        }

        return .resolved(GeoResponses.makeReading(
            ipinfo: ipinfo,
            confirmedCountry: cachedCountry,
            source: cachedSource
        ))
    }

    // MARK: - Private

    /// Основной подтверждающий — ipwho.is, резервный — geojs.
    /// geojs именно резервный: его лимит нигде не задокументирован,
    /// а неизвестное ограничение хуже известного.
    private func confirm(ip: String) async -> (country: String, source: ConfirmSource)? {
        if let country = await fetchCountry(
            urlString: Constants.ipwhoisURL(ip: ip),
            decode: GeoResponses.decodeIPWhoIs
        ) {
            return (country, .ipwhois)
        }
        if let country = await fetchCountry(
            urlString: Constants.geojsURL(ip: ip),
            decode: GeoResponses.decodeGeoJS
        ) {
            return (country, .geojs)
        }
        return nil
    }

    private func fetchCountry(
        urlString: String,
        decode: (Data) throws -> String?
    ) async -> String? {
        guard let url = URL(string: urlString) else { return nil }
        do {
            return try decode(try await fetcher.data(from: url, headers: [:]))
        } catch {
            return nil
        }
    }
}
```

- [ ] **Step 5: Прогнать, убедиться что проходят**

Run: `swift test --filter GeoProbeTests`
Expected: PASS, 7 тестов.

- [ ] **Step 6: Прогнать весь набор**

Run: `swift test`
Expected: PASS, 52 теста.

- [ ] **Step 7: Коммит**

```bash
git add Sources/WetoSystem/HTTPFetching.swift Sources/WetoSystem/GeoProbe.swift \
        Tests/WetoSystemTests/GeoProbeTests.swift
git commit -m "$TASK_NO - Опрос гео-сервисов с кэшем подтверждения по IP"
```

---

## Task 9: ProcessRegistry и ProcessKiller

Тесты здесь настоящие: поднимаем реальный `/bin/sleep`, находим его и убиваем. Это граница системы, и подменять её фейком означало бы не проверить ровно то, ради чего всё написано.

**Files:**
- Create: `Sources/WetoSystem/ProcessRegistry.swift`
- Create: `Sources/WetoSystem/ProcessKiller.swift`
- Test: `Tests/WetoSystemTests/ProcessRegistryTests.swift`
- Test: `Tests/WetoSystemTests/ProcessKillerTests.swift`

**Interfaces:**
- Consumes: `ProcessSnapshot`, `ProcessMatcher` (Task 5).
- Produces:
  - `ProcessLocating { func bundlePath(forBundleID: String) -> String?; func allProcesses() -> [ProcessSnapshot] }`, реализация `ProcessRegistry`
  - `ProcessKilling { func kill(pids: [Int32]) -> [KillResult] }`, реализация `ProcessKiller`
  - `KillResult(pid: Int32, errorCode: Int32?)` — `errorCode == nil` означает успех, `EPERM` означает чужого владельца процесса

- [ ] **Step 1: Написать падающий тест на реестр процессов**

Файл `Tests/WetoSystemTests/ProcessRegistryTests.swift`:

```swift
import XCTest
@testable import WetoSystem
import WetoCore

final class ProcessRegistryTests: XCTestCase {

    func test_all_processes_includes_the_test_runner_itself() {
        let processes = ProcessRegistry().allProcesses()
        let selfPID = ProcessInfo.processInfo.processIdentifier
        XCTAssertTrue(
            processes.contains { $0.pid == selfPID },
            "собственный процесс не найден среди \(processes.count) перечисленных"
        )
    }

    func test_all_processes_have_non_empty_paths() {
        for process in ProcessRegistry().allProcesses() {
            XCTAssertFalse(process.executablePath.isEmpty)
        }
    }

    func test_spawned_process_is_discovered() throws {
        let task = Process()
        task.executableURL = URL(fileURLWithPath: "/bin/sleep")
        task.arguments = ["30"]
        try task.run()
        defer { task.terminate() }

        let processes = ProcessRegistry().allProcesses()
        let found = processes.first { $0.pid == task.processIdentifier }
        XCTAssertNotNil(found, "порождённый /bin/sleep не найден")
        XCTAssertEqual(found?.executablePath, "/bin/sleep")
    }

    func test_bundle_path_resolves_for_a_system_application() {
        // Finder присутствует на любой машине и на CI-раннере.
        let path = ProcessRegistry().bundlePath(forBundleID: "com.apple.finder")
        XCTAssertNotNil(path)
        XCTAssertTrue(path?.hasSuffix(".app") == true, "получено: \(path ?? "nil")")
    }

    func test_bundle_path_is_nil_for_unknown_bundle_id() {
        XCTAssertNil(ProcessRegistry().bundlePath(forBundleID: "com.weto.does.not.exist"))
    }
}
```

- [ ] **Step 2: Прогнать, убедиться что падает**

Run: `swift test --filter ProcessRegistryTests`
Expected: FAIL — `cannot find 'ProcessRegistry' in scope`.

- [ ] **Step 3: Реализовать реестр**

Файл `Sources/WetoSystem/ProcessRegistry.swift`:

```swift
import Foundation
import AppKit
import Darwin
import WetoCore

/// Граница системы: где живут приложения и какие процессы сейчас запущены.
public protocol ProcessLocating: Sendable {
    /// Путь к бандлу по bundle ID, либо `nil` если приложение не установлено.
    func bundlePath(forBundleID bundleID: String) -> String?
    /// Все процессы системы, видимые текущему пользователю.
    func allProcesses() -> [ProcessSnapshot]
}

/// Перечисление процессов через `libproc`.
///
/// `NSRunningApplication` намеренно не используется как единственный источник:
/// он отдаёт только GUI-приложения, а у современных целей рядом живут
/// хелперы-рендереры и XPC-сервисы внутри того же бандла.
public struct ProcessRegistry: ProcessLocating {

    /// `PROC_PIDPATHINFO_MAXSIZE` из `<sys/proc_info.h>` — 4 * MAXPATHLEN.
    private static let pathBufferSize = 4096

    public init() {}

    public func bundlePath(forBundleID bundleID: String) -> String? {
        NSWorkspace.shared
            .urlForApplication(withBundleIdentifier: bundleID)?
            .path
    }

    public func allProcesses() -> [ProcessSnapshot] {
        let capacity = Int(proc_listallpids(nil, 0))
        guard capacity > 0 else { return [] }

        // Запас на процессы, появившиеся между двумя вызовами.
        var pids = [pid_t](repeating: 0, count: capacity + 64)
        let byteCount = Int32(pids.count * MemoryLayout<pid_t>.size)
        let written = pids.withUnsafeMutableBufferPointer { buffer in
            proc_listallpids(buffer.baseAddress, byteCount)
        }
        guard written > 0 else { return [] }

        let count = Int(written) / MemoryLayout<pid_t>.size
        var result: [ProcessSnapshot] = []
        result.reserveCapacity(count)

        for pid in pids.prefix(count) where pid > 0 {
            guard let path = executablePath(pid: pid) else { continue }
            result.append(ProcessSnapshot(pid: pid, executablePath: path))
        }
        return result
    }

    // MARK: - Private

    private func executablePath(pid: pid_t) -> String? {
        var buffer = [CChar](repeating: 0, count: Self.pathBufferSize)
        let length = proc_pidpath(pid, &buffer, UInt32(Self.pathBufferSize))
        guard length > 0 else { return nil }
        return String(cString: buffer)
    }
}
```

- [ ] **Step 4: Прогнать, убедиться что проходят**

Run: `swift test --filter ProcessRegistryTests`
Expected: PASS, 5 тестов.

- [ ] **Step 5: Написать падающий тест на убийцу**

Файл `Tests/WetoSystemTests/ProcessKillerTests.swift`:

```swift
import XCTest
@testable import WetoSystem

final class ProcessKillerTests: XCTestCase {

    func test_kill_terminates_a_real_process() throws {
        let task = Process()
        task.executableURL = URL(fileURLWithPath: "/bin/sleep")
        task.arguments = ["30"]
        try task.run()
        XCTAssertTrue(task.isRunning)

        let results = ProcessKiller().kill(pids: [task.processIdentifier])

        XCTAssertEqual(results.count, 1)
        XCTAssertNil(results[0].errorCode, "kill вернул errno \(results[0].errorCode ?? -1)")

        task.waitUntilExit()
        XCTAssertFalse(task.isRunning)
    }

    func test_kill_of_nonexistent_pid_reports_esrch() {
        // PID заведомо не существует: ядро не выдаёт настолько большие значения.
        let results = ProcessKiller().kill(pids: [Int32.max - 1])
        XCTAssertEqual(results.count, 1)
        XCTAssertEqual(results[0].errorCode, ESRCH)
    }

    func test_kill_of_launchd_reports_eperm() {
        // PID 1 принадлежит root — ровно тот случай, который UI обязан показать
        // пользователю, а не проглотить.
        let results = ProcessKiller().kill(pids: [1])
        XCTAssertEqual(results[0].errorCode, EPERM)
    }

    func test_empty_pid_list_yields_empty_result() {
        XCTAssertTrue(ProcessKiller().kill(pids: []).isEmpty)
    }

    func test_results_preserve_input_order() throws {
        let first = Process()
        first.executableURL = URL(fileURLWithPath: "/bin/sleep")
        first.arguments = ["30"]
        try first.run()

        let results = ProcessKiller().kill(pids: [Int32.max - 1, first.processIdentifier])

        XCTAssertEqual(results.map(\.pid), [Int32.max - 1, first.processIdentifier])
        first.waitUntilExit()
    }
}
```

- [ ] **Step 6: Прогнать, убедиться что падает**

Run: `swift test --filter ProcessKillerTests`
Expected: FAIL — `cannot find 'ProcessKiller' in scope`.

- [ ] **Step 7: Реализовать убийцу**

Файл `Sources/WetoSystem/ProcessKiller.swift`:

```swift
import Foundation
import Darwin

/// Результат попытки завершить один процесс.
/// `errorCode == nil` — успех. `EPERM` — процесс принадлежит другому
/// пользователю или root: это не проглатывается, а показывается в UI как
/// отказ охраны. `ESRCH` — процесс уже умер сам, это не ошибка по существу.
public struct KillResult: Equatable, Sendable {
    public let pid: Int32
    public let errorCode: Int32?

    public init(pid: Int32, errorCode: Int32?) {
        self.pid = pid
        self.errorCode = errorCode
    }

    /// Процесс гарантированно не работает: либо убит нами, либо умер раньше.
    public var isTerminated: Bool { errorCode == nil || errorCode == ESRCH }
}

/// Граница системы: завершение процессов.
public protocol ProcessKilling: Sendable {
    func kill(pids: [Int32]) -> [KillResult]
}

/// Безусловный SIGKILL.
///
/// Мягкое завершение через `NSRunningApplication.terminate()` отклонено
/// осознанно: приложение может показать диалог сохранения и продолжить работать.
public struct ProcessKiller: ProcessKilling {

    public init() {}

    public func kill(pids: [Int32]) -> [KillResult] {
        pids.map { pid in
            let status = Darwin.kill(pid, SIGKILL)
            return KillResult(pid: pid, errorCode: status == 0 ? nil : errno)
        }
    }
}
```

- [ ] **Step 8: Прогнать, убедиться что проходят**

Run: `swift test --filter ProcessKillerTests`
Expected: PASS, 5 тестов.

- [ ] **Step 9: Коммит**

```bash
git add Sources/WetoSystem/ProcessRegistry.swift Sources/WetoSystem/ProcessKiller.swift \
        Tests/WetoSystemTests/ProcessRegistryTests.swift \
        Tests/WetoSystemTests/ProcessKillerTests.swift
git commit -m "$TASK_NO - Поиск и завершение процессов целей"
```

---

## Task 10: NetworkEventSource — триггеры пересчёта

Собирает в один поток все сигналы, по которым нужно пересчитать состояние: смену сетевого пути, изменения в SCDynamicStore, пробуждение из сна и запуск приложения-цели.

**Files:**
- Create: `Sources/WetoSystem/NetworkEventSource.swift`
- Test: `Tests/WetoSystemTests/NetworkEventSourceTests.swift`

**Interfaces:**
- Consumes: ничего из предыдущих задач.
- Produces: `enum GuardTrigger { case networkPath, dynamicStore, wake, appLaunched(bundleID: String), tick }`, протокол `NetworkEventSourcing { func start(handler: @escaping @Sendable (GuardTrigger) -> Void); func stop() }`, реализация `NetworkEventSource`.

- [ ] **Step 1: Написать падающий тест**

Файл `Tests/WetoSystemTests/NetworkEventSourceTests.swift`:

```swift
import XCTest
@testable import WetoSystem

final class NetworkEventSourceTests: XCTestCase {

    func test_start_delivers_initial_network_path_event() {
        // NWPathMonitor всегда отдаёт текущий путь сразу после старта —
        // на этом строится проверка первичного состояния при запуске приложения.
        let source = NetworkEventSource()
        let received = expectation(description: "получено событие о сетевом пути")
        received.assertForOverFulfill = false

        source.start { trigger in
            if case .networkPath = trigger { received.fulfill() }
        }
        defer { source.stop() }

        wait(for: [received], timeout: 5)
    }

    func test_app_launch_notification_produces_trigger() {
        let source = NetworkEventSource()
        let received = expectation(description: "получено событие о запуске приложения")
        received.assertForOverFulfill = false

        source.start { trigger in
            if case .appLaunched(let bundleID) = trigger, bundleID == "com.apple.TextEdit" {
                received.fulfill()
            }
        }
        defer { source.stop() }

        // Реальный запуск приложения: событие должно долететь через NSWorkspace.
        let textEdit = URL(fileURLWithPath: "/System/Applications/TextEdit.app")
        NSWorkspace.shared.openApplication(at: textEdit, configuration: .init())

        wait(for: [received], timeout: 15)

        for app in NSRunningApplication.runningApplications(
            withBundleIdentifier: "com.apple.TextEdit"
        ) {
            app.forceTerminate()
        }
    }

    func test_stop_silences_further_events() {
        let source = NetworkEventSource()
        var count = 0
        let lock = NSLock()

        source.start { _ in
            lock.lock(); count += 1; lock.unlock()
        }
        Thread.sleep(forTimeInterval: 1)
        source.stop()

        lock.lock(); let afterStop = count; lock.unlock()
        Thread.sleep(forTimeInterval: 1)
        lock.lock(); let final = count; lock.unlock()

        XCTAssertEqual(afterStop, final, "события продолжают приходить после stop()")
    }
}
```

Добавить `import AppKit` в начало файла теста.

- [ ] **Step 2: Прогнать, убедиться что падает**

Run: `swift test --filter NetworkEventSourceTests`
Expected: FAIL — `cannot find 'NetworkEventSource' in scope`.

- [ ] **Step 3: Реализовать источник событий**

Файл `Sources/WetoSystem/NetworkEventSource.swift`:

```swift
import Foundation
import Network
import SystemConfiguration
import AppKit
import WetoCore

/// Что заставило пересчитать состояние охраны.
public enum GuardTrigger: Equatable, Sendable {
    /// Сменился сетевой путь: Wi-Fi, кабель, сотовый.
    case networkPath
    /// Изменилась сетевая конфигурация: подъём или падение VPN, смена default route.
    case dynamicStore
    /// Пробуждение из сна.
    case wake
    /// Запустилось приложение — возможно, одна из целей.
    case appLaunched(bundleID: String)
    /// Фоновый такт по таймеру.
    case tick
}

/// Граница системы: источник триггеров пересчёта.
public protocol NetworkEventSourcing: AnyObject {
    func start(handler: @escaping @Sendable (GuardTrigger) -> Void)
    func stop()
}

/// Слушает `NWPathMonitor`, `SCDynamicStore` и `NSWorkspace`.
///
/// Наблюдатели `NSWorkspace` подписываются через `addObserver` со stored token,
/// а не через async sequence: `Notification` не `Sendable` под Swift 6
/// (тот же приём применён в `../blik/blik`, см. AGENTS.md проекта blik).
public final class NetworkEventSource: NetworkEventSourcing, @unchecked Sendable {

    private let queue = DispatchQueue(label: "com.weto.events")
    private var pathMonitor: NWPathMonitor?
    private var dynamicStore: SCDynamicStore?
    private var runLoopSource: CFRunLoopSource?
    private var observerTokens: [NSObjectProtocol] = []
    private var handler: (@Sendable (GuardTrigger) -> Void)?

    public init() {}

    deinit { stop() }

    public func start(handler: @escaping @Sendable (GuardTrigger) -> Void) {
        stop()
        self.handler = handler

        startPathMonitor()
        startDynamicStore()
        startWorkspaceObservers()
    }

    public func stop() {
        handler = nil

        pathMonitor?.cancel()
        pathMonitor = nil

        if let runLoopSource {
            CFRunLoopRemoveSource(CFRunLoopGetMain(), runLoopSource, .commonModes)
        }
        runLoopSource = nil
        dynamicStore = nil

        for token in observerTokens {
            NSWorkspace.shared.notificationCenter.removeObserver(token)
        }
        observerTokens.removeAll()
    }

    // MARK: - Private

    private func emit(_ trigger: GuardTrigger) {
        handler?(trigger)
    }

    private func startPathMonitor() {
        let monitor = NWPathMonitor()
        monitor.pathUpdateHandler = { [weak self] _ in
            self?.emit(.networkPath)
        }
        monitor.start(queue: queue)
        pathMonitor = monitor
    }

    private func startDynamicStore() {
        var context = SCDynamicStoreContext(
            version: 0,
            info: Unmanaged.passUnretained(self).toOpaque(),
            retain: nil,
            release: nil,
            copyDescription: nil
        )

        let callback: SCDynamicStoreCallBack = { _, _, info in
            guard let info else { return }
            let source = Unmanaged<NetworkEventSource>.fromOpaque(info).takeUnretainedValue()
            source.emit(.dynamicStore)
        }

        guard let store = SCDynamicStoreCreate(
            nil, "com.weto.events" as CFString, callback, &context
        ) else { return }

        // Глобальный IPv4 ловит смену default route (подъём и падение VPN),
        // ключи сервисов — появление и пропажу адреса, Link — кабель.
        let patterns = [
            "State:/Network/Global/IPv4",
            "State:/Network/Service/[^/]+/IPv4",
            "State:/Network/Interface/[^/]+/Link",
        ] as CFArray
        SCDynamicStoreSetNotificationKeys(store, nil, patterns)

        guard let source = SCDynamicStoreCreateRunLoopSource(nil, store, 0) else { return }
        CFRunLoopAddSource(CFRunLoopGetMain(), source, .commonModes)

        dynamicStore = store
        runLoopSource = source
    }

    private func startWorkspaceObservers() {
        let center = NSWorkspace.shared.notificationCenter

        observerTokens.append(center.addObserver(
            forName: NSWorkspace.didWakeNotification, object: nil, queue: nil
        ) { [weak self] _ in
            self?.emit(.wake)
        })

        observerTokens.append(center.addObserver(
            forName: NSWorkspace.didLaunchApplicationNotification, object: nil, queue: nil
        ) { [weak self] notification in
            guard let app = notification.userInfo?[NSWorkspace.applicationUserInfoKey]
                    as? NSRunningApplication,
                  let bundleID = app.bundleIdentifier
            else { return }
            self?.emit(.appLaunched(bundleID: bundleID))
        })
    }
}
```

- [ ] **Step 4: Прогнать, убедиться что проходят**

Run: `swift test --filter NetworkEventSourceTests`
Expected: PASS, 3 теста. Тест с TextEdit открывает и закрывает окно — это ожидаемо.

- [ ] **Step 5: Прогнать весь набор**

Run: `swift test`
Expected: PASS, 65 тестов.

- [ ] **Step 6: Коммит**

```bash
git add Sources/WetoSystem/NetworkEventSource.swift \
        Tests/WetoSystemTests/NetworkEventSourceTests.swift
git commit -m "$TASK_NO - Источник триггеров пересчёта состояния"
```

---

## Task 11: Хранилище настроек и токена

Токен `ipinfo` идёт в Keychain, всё остальное — в `UserDefaults` общего suite. Здесь появляется таргет `WetoShared`.

**Files:**
- Modify: `Package.swift` — добавить `WetoShared` и `WetoSharedTests`
- Create: `Sources/WetoSystem/KeychainStore.swift`
- Create: `Sources/WetoShared/SettingsStore.swift`
- Test: `Tests/WetoSystemTests/KeychainStoreTests.swift`
- Test: `Tests/WetoSharedTests/SettingsStoreTests.swift`

**Interfaces:**
- Consumes: `Constants`, `IPRange`, `GuardConfig` (Tasks 1–3).
- Produces:
  - `SecretStoring { func read(account: String) -> String?; @discardableResult func write(_ value: String?, account: String) -> Bool }`, реализация `KeychainStore(service:)`
  - `@Observable @MainActor final class SettingsStore` со свойствами `isEnabled`, `vpnServiceName`, `targetBundleIDs`, `blockedCountryCodes`, `blockedIPRangeTexts`, `pollIntervalSeconds`, `ipinfoToken`, и вычисляемым `guardConfig: GuardConfig`

- [ ] **Step 1: Добавить таргеты в `Package.swift`**

В `products` добавить `.library(name: "WetoShared", targets: ["WetoShared"])`, в `targets`:

```swift
        .target(
            name: "WetoShared",
            dependencies: ["WetoCore", "WetoSystem"],
            path: "Sources/WetoShared"
        ),
        .testTarget(
            name: "WetoSharedTests",
            dependencies: ["WetoShared", "WetoCore", "WetoSystem"],
            path: "Tests/WetoSharedTests"
        ),
```

- [ ] **Step 2: Написать падающий тест на Keychain**

Файл `Tests/WetoSystemTests/KeychainStoreTests.swift`:

```swift
import XCTest
@testable import WetoSystem

final class KeychainStoreTests: XCTestCase {

    private let service = "com.weto.tests.keychain"
    private let account = "ipinfo"

    private var store: KeychainStore { KeychainStore(service: service) }

    override func tearDown() {
        store.write(nil, account: account)
        super.tearDown()
    }

    func test_reading_absent_secret_returns_nil() {
        store.write(nil, account: account)
        XCTAssertNil(store.read(account: account))
    }

    func test_written_secret_is_read_back() {
        XCTAssertTrue(store.write("s3cret", account: account))
        XCTAssertEqual(store.read(account: account), "s3cret")
    }

    func test_writing_twice_overwrites_previous_value() {
        store.write("first", account: account)
        store.write("second", account: account)
        XCTAssertEqual(store.read(account: account), "second")
    }

    func test_writing_nil_deletes_secret() {
        store.write("to be removed", account: account)
        XCTAssertTrue(store.write(nil, account: account))
        XCTAssertNil(store.read(account: account))
    }

    func test_accounts_are_isolated() {
        store.write("a", account: "first")
        store.write("b", account: "second")
        XCTAssertEqual(store.read(account: "first"), "a")
        XCTAssertEqual(store.read(account: "second"), "b")
        store.write(nil, account: "first")
        store.write(nil, account: "second")
    }
}
```

- [ ] **Step 3: Прогнать, убедиться что падает**

Run: `swift test --filter KeychainStoreTests`
Expected: FAIL — `cannot find 'KeychainStore' in scope`.

- [ ] **Step 4: Реализовать Keychain**

Файл `Sources/WetoSystem/KeychainStore.swift`:

```swift
import Foundation
import Security

/// Граница системы: хранение секретов.
public protocol SecretStoring: Sendable {
    func read(account: String) -> String?
    @discardableResult
    func write(_ value: String?, account: String) -> Bool
}

/// Потокобезопасная коробка для токена.
///
/// `SettingsStore` изолирован главным актором, а `GeoProbe` — актор со своим
/// исполнительным контекстом. Читать `@MainActor`-свойство из него нельзя,
/// а `MainActor.assumeIsolated` в этой точке уронил бы процесс. Коробка —
/// самый дешёвый корректный способ передать значение через границу акторов.
public final class TokenBox: @unchecked Sendable {
    private let lock = NSLock()
    private var storage: String?

    public init(_ initial: String? = nil) { storage = initial }

    public var value: String? {
        get { lock.lock(); defer { lock.unlock() }; return storage }
        set { lock.lock(); storage = newValue; lock.unlock() }
    }
}

/// Обёртка над `kSecClassGenericPassword`.
///
/// Токен ipinfo лежит здесь, а не в `UserDefaults`: plist настроек читается
/// любым процессом пользователя и попадает в бэкапы открытым текстом.
public struct KeychainStore: SecretStoring {

    private let service: String

    public init(service: String) {
        self.service = service
    }

    public func read(account: String) -> String? {
        var query = baseQuery(account: account)
        query[kSecReturnData as String] = true
        query[kSecMatchLimit as String] = kSecMatchLimitOne

        var item: CFTypeRef?
        guard SecItemCopyMatching(query as CFDictionary, &item) == errSecSuccess,
              let data = item as? Data,
              let value = String(data: data, encoding: .utf8)
        else { return nil }
        return value
    }

    @discardableResult
    public func write(_ value: String?, account: String) -> Bool {
        let query = baseQuery(account: account)

        guard let value, !value.isEmpty else {
            let status = SecItemDelete(query as CFDictionary)
            return status == errSecSuccess || status == errSecItemNotFound
        }

        let data = Data(value.utf8)
        let attributes = [kSecValueData as String: data]

        let updateStatus = SecItemUpdate(query as CFDictionary, attributes as CFDictionary)
        if updateStatus == errSecSuccess { return true }
        guard updateStatus == errSecItemNotFound else { return false }

        var insert = query
        insert[kSecValueData as String] = data
        return SecItemAdd(insert as CFDictionary, nil) == errSecSuccess
    }

    // MARK: - Private

    private func baseQuery(account: String) -> [String: Any] {
        [
            kSecClass as String: kSecClassGenericPassword,
            kSecAttrService as String: service,
            kSecAttrAccount as String: account,
        ]
    }
}
```

- [ ] **Step 5: Прогнать, убедиться что проходят**

Run: `swift test --filter KeychainStoreTests`
Expected: PASS, 5 тестов.

- [ ] **Step 6: Написать падающий тест на настройки**

Файл `Tests/WetoSharedTests/SettingsStoreTests.swift`:

```swift
import XCTest
@testable import WetoShared
import WetoCore
import WetoSystem

/// Фейк секретного хранилища — Keychain не трогаем в тестах настроек.
private final class InMemorySecretStore: SecretStoring, @unchecked Sendable {
    private var storage: [String: String] = [:]
    private let lock = NSLock()

    func read(account: String) -> String? {
        lock.lock(); defer { lock.unlock() }
        return storage[account]
    }

    @discardableResult
    func write(_ value: String?, account: String) -> Bool {
        lock.lock(); defer { lock.unlock() }
        if let value { storage[account] = value } else { storage[account] = nil }
        return true
    }
}

@MainActor
final class SettingsStoreTests: XCTestCase {

    private var suiteName: String!
    private var defaults: UserDefaults!

    override func setUp() async throws {
        suiteName = "com.weto.tests.\(UUID().uuidString)"
        defaults = UserDefaults(suiteName: suiteName)
    }

    override func tearDown() async throws {
        UserDefaults.standard.removePersistentDomain(forName: suiteName)
    }

    private func makeStore() -> SettingsStore {
        SettingsStore(defaults: defaults, secrets: InMemorySecretStore())
    }

    func test_defaults_are_safe_out_of_the_box() {
        let store = makeStore()
        XCTAssertFalse(store.isEnabled, "охрана выключена до настройки")
        XCTAssertNil(store.vpnServiceName)
        XCTAssertTrue(store.targetBundleIDs.isEmpty)
        XCTAssertEqual(store.blockedCountryCodes, ["RU"])
        XCTAssertEqual(store.pollIntervalSeconds, 5)
    }

    func test_values_survive_a_new_store_over_same_defaults() {
        let first = makeStore()
        first.isEnabled = true
        first.vpnServiceName = "Happ"
        first.targetBundleIDs = ["com.example.a", "com.example.b"]
        first.blockedCountryCodes = ["RU", "BY"]
        first.blockedIPRangeTexts = ["198.51.100.0/22"]
        first.pollIntervalSeconds = 15

        let second = SettingsStore(defaults: defaults, secrets: InMemorySecretStore())
        XCTAssertTrue(second.isEnabled)
        XCTAssertEqual(second.vpnServiceName, "Happ")
        XCTAssertEqual(second.targetBundleIDs, ["com.example.a", "com.example.b"])
        XCTAssertEqual(second.blockedCountryCodes, ["BY", "RU"])
        XCTAssertEqual(second.blockedIPRangeTexts, ["198.51.100.0/22"])
        XCTAssertEqual(second.pollIntervalSeconds, 15)
    }

    func test_country_codes_are_normalized_to_uppercase() {
        let store = makeStore()
        store.blockedCountryCodes = ["ru", "by"]
        XCTAssertEqual(store.blockedCountryCodes, ["BY", "RU"])
    }

    func test_guard_config_reflects_current_settings() {
        let store = makeStore()
        store.vpnServiceName = "Happ"
        store.targetBundleIDs = ["com.example.a"]
        store.blockedCountryCodes = ["RU"]
        store.blockedIPRangeTexts = ["10.0.0.0/8", "мусор"]

        let config = store.guardConfig
        XCTAssertEqual(config.vpnServiceName, "Happ")
        XCTAssertEqual(config.targetBundleIDs, ["com.example.a"])
        XCTAssertEqual(config.blockedCountries, ["RU"])
        XCTAssertEqual(config.blockedIPRanges.count, 1, "неразобранные строки отбрасываются")
        XCTAssertTrue(config.blockedIPRanges[0].contains("10.1.2.3"))
    }

    func test_token_goes_to_secret_store_not_to_defaults() {
        let secrets = InMemorySecretStore()
        let store = SettingsStore(defaults: defaults, secrets: secrets)
        store.ipinfoToken = "s3cret"

        XCTAssertEqual(store.ipinfoToken, "s3cret")
        XCTAssertEqual(secrets.read(account: "token"), "s3cret")
        let plist = defaults.dictionaryRepresentation()
        XCTAssertFalse(
            plist.values.contains { ($0 as? String) == "s3cret" },
            "токен просочился в UserDefaults"
        )
    }

    func test_clearing_token_removes_it_from_secret_store() {
        let secrets = InMemorySecretStore()
        let store = SettingsStore(defaults: defaults, secrets: secrets)
        store.ipinfoToken = "s3cret"
        store.ipinfoToken = ""
        XCTAssertNil(secrets.read(account: "token"))
    }

    func test_token_box_mirrors_current_token() {
        // Коробку читает GeoProbe вне главного актора — она обязана быть
        // синхронизирована и при загрузке, и при изменении.
        let secrets = InMemorySecretStore()
        secrets.write("saved", account: "token")

        let store = SettingsStore(defaults: defaults, secrets: secrets)
        XCTAssertEqual(store.tokenBox.value, "saved")

        store.ipinfoToken = "updated"
        XCTAssertEqual(store.tokenBox.value, "updated")

        store.ipinfoToken = ""
        XCTAssertNil(store.tokenBox.value)
    }
}
```

- [ ] **Step 7: Прогнать, убедиться что падает**

Run: `swift test --filter SettingsStoreTests`
Expected: FAIL — `cannot find 'SettingsStore' in scope`.

- [ ] **Step 8: Реализовать настройки**

Файл `Sources/WetoShared/SettingsStore.swift`:

```swift
import Foundation
import Observation
import WetoCore
import WetoSystem

/// Настройки приложения.
///
/// Внутри `@Observable` используется `UserDefaults` напрямую, а не `@AppStorage`
/// — паттерн взят из `../blik/blik` (см. «Ключевые контракты» в его ARCHITECTURE.md):
/// `@AppStorage` не работает вне `View` и ломает наблюдение в VM-слое.
///
/// По умолчанию охрана выключена: приложение сразу после установки не должно
/// начать убивать процессы, пока пользователь не выбрал цели и VPN.
@Observable
@MainActor
public final class SettingsStore {

    private enum Key {
        static let isEnabled = "isEnabled"
        static let vpnServiceName = "vpnServiceName"
        static let targetBundleIDs = "targetBundleIDs"
        static let blockedCountryCodes = "blockedCountryCodes"
        static let blockedIPRangeTexts = "blockedIPRangeTexts"
        static let pollIntervalSeconds = "pollIntervalSeconds"
    }

    private static let tokenAccount = "token"

    @ObservationIgnored private let defaults: UserDefaults
    @ObservationIgnored private let secrets: SecretStoring

    public init(defaults: UserDefaults, secrets: SecretStoring) {
        self.defaults = defaults
        self.secrets = secrets
        self._isEnabled = defaults.bool(forKey: Key.isEnabled)
        self._vpnServiceName = defaults.string(forKey: Key.vpnServiceName)
        self._targetBundleIDs = defaults.stringArray(forKey: Key.targetBundleIDs) ?? []
        self._blockedCountryCodes = defaults.stringArray(forKey: Key.blockedCountryCodes)
            ?? Constants.defaultBlockedCountries.sorted()
        self._blockedIPRangeTexts = defaults.stringArray(forKey: Key.blockedIPRangeTexts) ?? []
        let interval = defaults.double(forKey: Key.pollIntervalSeconds)
        self._pollIntervalSeconds = interval > 0 ? interval : Constants.defaultPollIntervalSeconds
        self._ipinfoToken = secrets.read(account: Self.tokenAccount) ?? ""
        self.tokenBox.value = _ipinfoToken.isEmpty ? nil : _ipinfoToken
    }

    /// Удобный конструктор для приложения: общий suite и Keychain.
    public convenience init() {
        self.init(
            defaults: UserDefaults(suiteName: Constants.userDefaultsSuite) ?? .standard,
            secrets: KeychainStore(service: Constants.keychainService)
        )
    }

    // MARK: - Свойства

    private var _isEnabled: Bool
    public var isEnabled: Bool {
        get { _isEnabled }
        set { _isEnabled = newValue; defaults.set(newValue, forKey: Key.isEnabled) }
    }

    private var _vpnServiceName: String?
    public var vpnServiceName: String? {
        get { _vpnServiceName }
        set { _vpnServiceName = newValue; defaults.set(newValue, forKey: Key.vpnServiceName) }
    }

    private var _targetBundleIDs: [String]
    public var targetBundleIDs: [String] {
        get { _targetBundleIDs }
        set { _targetBundleIDs = newValue; defaults.set(newValue, forKey: Key.targetBundleIDs) }
    }

    private var _blockedCountryCodes: [String]
    /// Всегда в верхнем регистре и отсортированы — так их проще сравнивать и показывать.
    public var blockedCountryCodes: [String] {
        get { _blockedCountryCodes }
        set {
            let normalized = Array(Set(newValue.map { $0.uppercased() })).sorted()
            _blockedCountryCodes = normalized
            defaults.set(normalized, forKey: Key.blockedCountryCodes)
        }
    }

    private var _blockedIPRangeTexts: [String]
    public var blockedIPRangeTexts: [String] {
        get { _blockedIPRangeTexts }
        set { _blockedIPRangeTexts = newValue; defaults.set(newValue, forKey: Key.blockedIPRangeTexts) }
    }

    private var _pollIntervalSeconds: TimeInterval
    public var pollIntervalSeconds: TimeInterval {
        get { _pollIntervalSeconds }
        set { _pollIntervalSeconds = newValue; defaults.set(newValue, forKey: Key.pollIntervalSeconds) }
    }

    private var _ipinfoToken: String
    public var ipinfoToken: String {
        get { _ipinfoToken }
        set {
            _ipinfoToken = newValue
            secrets.write(newValue.isEmpty ? nil : newValue, account: Self.tokenAccount)
            tokenBox.value = newValue.isEmpty ? nil : newValue
        }
    }

    /// Токен нужен `GeoProbe`, который работает вне главного актора.
    /// Читать `@MainActor`-свойство оттуда нельзя, поэтому значение
    /// дублируется в потокобезопасную коробку.
    public let tokenBox = TokenBox()

    // MARK: - Производное

    /// Снимок настроек в том виде, в каком его потребляет `GuardPolicy`.
    /// Неразобранные строки CIDR молча отбрасываются: UI подсвечивает их
    /// при вводе, а политика обязана работать с тем, что есть.
    public var guardConfig: GuardConfig {
        GuardConfig(
            vpnServiceName: vpnServiceName,
            blockedCountries: Set(blockedCountryCodes),
            blockedIPRanges: blockedIPRangeTexts.compactMap(IPRange.init),
            targetBundleIDs: targetBundleIDs
        )
    }
}
```

- [ ] **Step 9: Прогнать, убедиться что проходят**

Run: `swift test --filter SettingsStoreTests`
Expected: PASS, 6 тестов.

- [ ] **Step 10: Прогнать весь набор**

Run: `swift test`
Expected: PASS, 76 тестов.

- [ ] **Step 11: Коммит**

```bash
git add Package.swift Sources/WetoSystem/KeychainStore.swift \
        Sources/WetoShared/SettingsStore.swift \
        Tests/WetoSystemTests/KeychainStoreTests.swift \
        Tests/WetoSharedTests/SettingsStoreTests.swift
git commit -m "$TASK_NO - Хранилище настроек и токена в Keychain"
```

---

## Task 12: Локальное решение и журнал срабатываний

Две вещи, которые нужны `GuardVM` из следующей задачи: способ принять решение без сетевой пробы и кольцевой журнал событий.

**Files:**
- Modify: `Sources/WetoCore/GuardPolicy.swift` — добавить `decideLocal`
- Modify: `Tests/WetoCoreTests/GuardPolicyTests.swift` — тесты на `decideLocal`
- Create: `Sources/WetoCore/Model/KillEvent.swift`
- Create: `Sources/WetoShared/EventLogStore.swift`
- Test: `Tests/WetoSharedTests/EventLogStoreTests.swift`

**Interfaces:**
- Consumes: `GuardPolicy`, `UnsafeReason`, `GuardConfig`, `VPNStatus` (Tasks 1–3), `Constants` (Task 1).
- Produces:
  - `GuardPolicy.decideLocal(isEnabled:vpn:config:) -> GuardDecision?` — `nil` означает «локальных оснований нет, нужен сетевой вердикт»
  - `KillEvent(date:reasonText:ip:country:killedPIDs:)`, `Codable`
  - `UnsafeReason.displayText: String` — человекочитаемая формулировка для попапа и журнала
  - `@Observable @MainActor final class EventLogStore` с `events: [KillEvent]` и `record(_:)`

- [ ] **Step 1: Написать падающий тест на `decideLocal`**

Дописать в `Tests/WetoCoreTests/GuardPolicyTests.swift`:

```swift
    // MARK: - decideLocal: решение без сетевой пробы

    func test_local_decision_is_nil_when_network_verdict_is_required() {
        let decision = GuardPolicy.decideLocal(
            isEnabled: true,
            vpn: .up(isPrimary: true),
            config: config()
        )
        XCTAssertNil(decision, "VPN в порядке — дальше решает только сеть")
    }

    func test_local_decision_short_circuits_when_guard_is_disabled() {
        let decision = GuardPolicy.decideLocal(
            isEnabled: false,
            vpn: .down,
            config: config()
        )
        XCTAssertEqual(decision, .safe)
    }

    func test_local_decision_kills_on_vpn_down_without_touching_network() {
        let decision = GuardPolicy.decideLocal(
            isEnabled: true,
            vpn: .down,
            config: config()
        )
        XCTAssertEqual(decision, .kill(.vpnDown))
    }

    func test_local_decision_kills_when_vpn_is_not_primary() {
        let decision = GuardPolicy.decideLocal(
            isEnabled: true,
            vpn: .up(isPrimary: false),
            config: config()
        )
        XCTAssertEqual(decision, .kill(.vpnNotPrimary))
    }

    func test_local_and_full_decisions_agree_on_local_reasons() {
        // Инвариант: если локальная проверка вынесла вердикт, полная обязана
        // вынести тот же самый. Иначе цикл вёл бы себя по-разному в зависимости
        // от того, дошли ли мы до сети.
        let cases: [(VPNStatus, GuardConfig)] = [
            (.down, config()),
            (.up(isPrimary: false), config()),
            (.up(isPrimary: true), config(vpn: nil)),
        ]
        for (vpn, configuration) in cases {
            let local = GuardPolicy.decideLocal(isEnabled: true, vpn: vpn, config: configuration)
            let full = GuardPolicy.decide(signals(vpn: vpn, config: configuration))
            if let local {
                XCTAssertEqual(local, full, "расхождение для vpn=\(vpn)")
            }
        }
    }
```

- [ ] **Step 2: Прогнать, убедиться что падает**

Run: `swift test --filter GuardPolicyTests`
Expected: FAIL — `type 'GuardPolicy' has no member 'decideLocal'`.

- [ ] **Step 3: Вынести локальную часть в отдельный метод**

Заменить в `Sources/WetoCore/GuardPolicy.swift` тело `decide` так, чтобы локальная часть жила отдельно:

```swift
    /// Решение, принимаемое без обращения к сети.
    ///
    /// Возвращает `nil`, если локальных оснований завершать цели нет и нужен
    /// сетевой вердикт. Отдельный метод существует потому, что падение VPN
    /// видно из SCDynamicStore мгновенно — самый частый сценарий отрабатывает
    /// за микросекунды, не дожидаясь HTTP-запроса.
    public static func decideLocal(
        isEnabled: Bool,
        vpn: VPNStatus,
        config: GuardConfig
    ) -> GuardDecision? {
        guard isEnabled, !config.targetBundleIDs.isEmpty else { return .safe }
        guard config.vpnServiceName != nil else { return .kill(.vpnNotConfigured) }

        switch vpn {
        case .notConfigured:
            return .kill(.vpnNotConfigured)
        case .down:
            return .kill(.vpnDown)
        case .up(let isPrimary):
            return isPrimary ? nil : .kill(.vpnNotPrimary)
        }
    }

    public static func decide(_ signals: GuardSignals) -> GuardDecision {
        if let local = decideLocal(
            isEnabled: signals.isEnabled,
            vpn: signals.vpn,
            config: signals.config
        ) {
            return local
        }

        // Шаги 4–9: требуют результата сетевой пробы.
        guard case .resolved(let reading) = signals.geo else {
            if case .unavailable(let detail) = signals.geo {
                return .kill(.geoUnavailable(detail))
            }
            return .kill(.geoUnavailable("нет данных"))
        }

        if signals.config.blockedIPRanges.contains(where: { $0.contains(reading.ip) }) {
            return .kill(.blacklistedIP(reading.ip))
        }

        let blocked = Set(signals.config.blockedCountries.map { $0.uppercased() })
        let primary = reading.primaryCountry.uppercased()

        if blocked.contains(primary) {
            return .kill(.blockedCountry(code: primary, source: "ipinfo"))
        }

        guard let confirmedRaw = reading.confirmedCountry else {
            return .kill(.confirmationUnavailable)
        }
        let confirmed = confirmedRaw.uppercased()

        if blocked.contains(confirmed) {
            let source = reading.confirmSource?.rawValue ?? "confirm"
            return .kill(.blockedCountry(code: confirmed, source: source))
        }

        if primary != confirmed {
            return .kill(.countryConflict(primary: primary, confirmed: confirmed))
        }

        return .safe
    }
```

- [ ] **Step 4: Прогнать, убедиться что проходят все тесты политики**

Run: `swift test --filter GuardPolicyTests`
Expected: PASS, 23 теста.

- [ ] **Step 5: Написать падающий тест на журнал**

Файл `Tests/WetoSharedTests/EventLogStoreTests.swift`:

```swift
import XCTest
@testable import WetoShared
import WetoCore

@MainActor
final class EventLogStoreTests: XCTestCase {

    private var suiteName: String!
    private var defaults: UserDefaults!

    override func setUp() async throws {
        suiteName = "com.weto.tests.\(UUID().uuidString)"
        defaults = UserDefaults(suiteName: suiteName)
    }

    override func tearDown() async throws {
        UserDefaults.standard.removePersistentDomain(forName: suiteName)
    }

    private func event(_ reason: String, pids: [Int32] = [42]) -> KillEvent {
        KillEvent(
            date: Date(timeIntervalSince1970: 1_700_000_000),
            reasonText: reason,
            ip: "203.0.113.28",
            country: "KZ",
            killedPIDs: pids
        )
    }

    func test_new_store_is_empty() {
        XCTAssertTrue(EventLogStore(defaults: defaults).events.isEmpty)
    }

    func test_recorded_event_appears_first() {
        let store = EventLogStore(defaults: defaults)
        store.record(event("первое"))
        store.record(event("второе"))
        XCTAssertEqual(store.events.map(\.reasonText), ["второе", "первое"])
    }

    func test_events_survive_a_new_store_over_same_defaults() {
        EventLogStore(defaults: defaults).record(event("сохранённое"))
        XCTAssertEqual(EventLogStore(defaults: defaults).events.map(\.reasonText), ["сохранённое"])
    }

    func test_ring_buffer_drops_oldest_beyond_capacity() {
        let store = EventLogStore(defaults: defaults)
        for index in 0..<(Constants.eventLogCapacity + 10) {
            store.record(event("событие \(index)"))
        }
        XCTAssertEqual(store.events.count, Constants.eventLogCapacity)
        XCTAssertEqual(store.events.first?.reasonText, "событие \(Constants.eventLogCapacity + 9)")
        XCTAssertEqual(store.events.last?.reasonText, "событие 10")
    }

    func test_preview_returns_at_most_configured_count() {
        let store = EventLogStore(defaults: defaults)
        for index in 0..<20 { store.record(event("событие \(index)")) }
        XCTAssertEqual(store.preview.count, Constants.eventLogPreviewCount)
        XCTAssertEqual(store.preview.first?.reasonText, "событие 19")
    }

    func test_clear_empties_the_log() {
        let store = EventLogStore(defaults: defaults)
        store.record(event("будет стёрто"))
        store.clear()
        XCTAssertTrue(store.events.isEmpty)
        XCTAssertTrue(EventLogStore(defaults: defaults).events.isEmpty)
    }
}
```

- [ ] **Step 6: Прогнать, убедиться что падает**

Run: `swift test --filter EventLogStoreTests`
Expected: FAIL — `cannot find 'KillEvent' in scope`.

- [ ] **Step 7: Создать модель события и человекочитаемые причины**

Файл `Sources/WetoCore/Model/KillEvent.swift`:

```swift
import Foundation

/// Запись о срабатывании охраны.
public struct KillEvent: Codable, Equatable, Identifiable, Sendable {
    public let id: UUID
    public let date: Date
    public let reasonText: String
    public let ip: String?
    public let country: String?
    public let killedPIDs: [Int32]

    public init(
        id: UUID = UUID(),
        date: Date,
        reasonText: String,
        ip: String?,
        country: String?,
        killedPIDs: [Int32]
    ) {
        self.id = id
        self.date = date
        self.reasonText = reasonText
        self.ip = ip
        self.country = country
        self.killedPIDs = killedPIDs
    }
}

extension UnsafeReason {
    /// Формулировка для попапа менюбара и журнала.
    public var displayText: String {
        switch self {
        case .vpnNotConfigured:
            return "VPN-сервис не выбран в настройках"
        case .vpnDown:
            return "VPN не поднят"
        case .vpnNotPrimary:
            return "VPN поднят, но трафик идёт мимо него"
        case .geoUnavailable(let detail):
            return "Не удалось определить внешний адрес: \(detail)"
        case .blacklistedIP(let ip):
            return "Адрес \(ip) в чёрном списке"
        case .blockedCountry(let code, let source):
            return "Обнаружена страна \(code) по данным \(source)"
        case .confirmationUnavailable:
            return "Подтверждающие сервисы недоступны"
        case .countryConflict(let primary, let confirmed):
            return "Расхождение стран: ipinfo — \(primary), подтверждение — \(confirmed)"
        }
    }

    /// Жёлтый статус выделен для единственной причины — отсутствия подтверждения.
    /// Цели убиты в обоих случаях, цвет лишь различает, какое звено сработало.
    public var isDegradedRatherThanBlocked: Bool {
        if case .confirmationUnavailable = self { return true }
        return false
    }
}
```

- [ ] **Step 8: Реализовать журнал**

Файл `Sources/WetoShared/EventLogStore.swift`:

```swift
import Foundation
import Observation
import WetoCore

/// Кольцевой журнал срабатываний в `UserDefaults`.
///
/// Базы данных в проекте нет: 200 записей — это единицы килобайт JSON,
/// SQLite ради такого объёма не нужен.
@Observable
@MainActor
public final class EventLogStore {

    private static let key = "eventLog"

    public private(set) var events: [KillEvent] = []

    @ObservationIgnored private let defaults: UserDefaults

    public init(defaults: UserDefaults) {
        self.defaults = defaults
        if let data = defaults.data(forKey: Self.key),
           let decoded = try? JSONDecoder().decode([KillEvent].self, from: data) {
            events = decoded
        }
    }

    public convenience init() {
        self.init(defaults: UserDefaults(suiteName: Constants.userDefaultsSuite) ?? .standard)
    }

    /// Последние события для попапа менюбара.
    public var preview: [KillEvent] {
        Array(events.prefix(Constants.eventLogPreviewCount))
    }

    /// Новое событие встаёт первым; хвост за пределами ёмкости отбрасывается.
    public func record(_ event: KillEvent) {
        events.insert(event, at: 0)
        if events.count > Constants.eventLogCapacity {
            events.removeLast(events.count - Constants.eventLogCapacity)
        }
        persist()
    }

    public func clear() {
        events.removeAll()
        persist()
    }

    // MARK: - Private

    private func persist() {
        guard let data = try? JSONEncoder().encode(events) else { return }
        defaults.set(data, forKey: Self.key)
    }
}
```

- [ ] **Step 9: Прогнать, убедиться что проходят**

Run: `swift test --filter EventLogStoreTests`
Expected: PASS, 6 тестов.

- [ ] **Step 10: Прогнать весь набор**

Run: `swift test`
Expected: PASS, 87 тестов.

- [ ] **Step 11: Коммит**

```bash
git add Sources/WetoCore/GuardPolicy.swift Sources/WetoCore/Model/KillEvent.swift \
        Sources/WetoShared/EventLogStore.swift \
        Tests/WetoCoreTests/GuardPolicyTests.swift \
        Tests/WetoSharedTests/EventLogStoreTests.swift
git commit -m "$TASK_NO - Локальное решение политики и журнал срабатываний"
```

---

## Task 13: GuardVM — цикл охраны

Собирает всё вместе: слушает триггеры, различает локальную оценку и сетевую пробу, применяет решение, добивает перезапуски. Тесты — на фейках границ.

**Files:**
- Create: `Sources/WetoShared/KillNotifying.swift`
- Create: `Sources/WetoShared/GuardVM.swift`
- Create: `Sources/WetoShared/AppCoordinator.swift`
- Test: `Tests/WetoSharedTests/GuardVMTests.swift`

**Interfaces:**
- Consumes: `GuardPolicy.decideLocal`, `GuardPolicy.decide`, `VPNStatusResolver`, `ProcessMatcher` (Core); `NetworkSnapshotReading`, `GeoProbing`, `ProcessLocating`, `ProcessKilling`, `NetworkEventSourcing`, `GuardTrigger`, `KillResult` (System); `SettingsStore`, `EventLogStore` (Tasks 11–12).
- Produces:
  - `enum GuardState { case disabled, safe(GeoReading?), unsafe(UnsafeReason) }` с `statusColor: GuardStatusColor` и `flag: String`
  - `enum GuardStatusColor { case green, yellow, red, grey }`
  - `protocol KillNotifying { func notify(reasonText: String, killedCount: Int) }`, реализация `UserNotificationKillNotifier`
  - `@Observable @MainActor final class GuardVM` — `state`, `lastReading`, `permissionFailure`, `availableVPNNames`, `start()`, `stop()`, `handle(_:)`, `runProbe() async`, `awaitPendingProbe() async`
  - `@Observable @MainActor final class AppCoordinator` — `settings`, `eventLog`, `guardVM`

- [ ] **Step 1: Написать падающий тест на локальный путь**

Файл `Tests/WetoSharedTests/GuardVMTests.swift`:

```swift
import XCTest
@testable import WetoShared
import WetoCore
import WetoSystem

// MARK: - Фейки границ

private struct StubSnapshotReader: NetworkSnapshotReading {
    let snapshotValue: NetworkSnapshot
    func snapshot() -> NetworkSnapshot { snapshotValue }
}

private actor StubGeoProbe: GeoProbing {
    private let outcome: GeoOutcome
    private(set) var callCount = 0

    init(_ outcome: GeoOutcome) { self.outcome = outcome }

    func probe() async -> GeoOutcome {
        callCount += 1
        return outcome
    }

    func calls() -> Int { callCount }
}

private struct StubLocator: ProcessLocating {
    let bundlePaths: [String: String]
    let processes: [ProcessSnapshot]

    func bundlePath(forBundleID bundleID: String) -> String? { bundlePaths[bundleID] }
    func allProcesses() -> [ProcessSnapshot] { processes }
}

private final class SpyKiller: ProcessKilling, @unchecked Sendable {
    private let lock = NSLock()
    private var _killedBatches: [[Int32]] = []
    var errorForPID: [Int32: Int32] = [:]

    var killedBatches: [[Int32]] {
        lock.lock(); defer { lock.unlock() }
        return _killedBatches
    }

    func kill(pids: [Int32]) -> [KillResult] {
        lock.lock()
        _killedBatches.append(pids)
        lock.unlock()
        return pids.map { KillResult(pid: $0, errorCode: errorForPID[$0]) }
    }
}

private final class SpyNotifier: KillNotifying, @unchecked Sendable {
    private let lock = NSLock()
    private var _messages: [String] = []
    var messages: [String] {
        lock.lock(); defer { lock.unlock() }
        return _messages
    }
    func notify(reasonText: String, killedCount: Int) {
        lock.lock(); _messages.append(reasonText); lock.unlock()
    }
}

private final class ManualEventSource: NetworkEventSourcing, @unchecked Sendable {
    private(set) var handler: (@Sendable (GuardTrigger) -> Void)?
    private(set) var stopped = false

    func start(handler: @escaping @Sendable (GuardTrigger) -> Void) { self.handler = handler }
    func stop() { stopped = true; handler = nil }
    func fire(_ trigger: GuardTrigger) { handler?(trigger) }
}

// MARK: - Тесты

@MainActor
final class GuardVMTests: XCTestCase {

    private let targetBundleID = "com.example.target"
    private let targetPath = "/Applications/Target.app"

    private var suiteName: String!
    private var defaults: UserDefaults!

    override func setUp() async throws {
        suiteName = "com.weto.tests.\(UUID().uuidString)"
        defaults = UserDefaults(suiteName: suiteName)
    }

    override func tearDown() async throws {
        UserDefaults.standard.removePersistentDomain(forName: suiteName)
    }

    /// Снимок, где Happ поднят и держит default route.
    private func healthySnapshot() -> NetworkSnapshot {
        NetworkSnapshot(
            services: [
                .init(uuid: "WIFI", name: "Wi-Fi", activeInterface: "en0"),
                .init(uuid: "HAPP", name: "Happ", activeInterface: "utun6"),
            ],
            primaryServiceUUID: "HAPP"
        )
    }

    /// Снимок, где Happ есть в системе, но туннель не поднят.
    private func vpnDownSnapshot() -> NetworkSnapshot {
        NetworkSnapshot(
            services: [
                .init(uuid: "WIFI", name: "Wi-Fi", activeInterface: "en0"),
                .init(uuid: "HAPP", name: "Happ", activeInterface: nil),
            ],
            primaryServiceUUID: "WIFI"
        )
    }

    private func geoOutcome(primary: String = "KZ", confirmed: String? = "KZ") -> GeoOutcome {
        .resolved(GeoReading(
            ip: "203.0.113.28",
            asn: "AS49791",
            primaryCountry: primary,
            confirmedCountry: confirmed,
            confirmSource: confirmed == nil ? nil : .ipwhois
        ))
    }

    private struct Harness {
        let vm: GuardVM
        let killer: SpyKiller
        let probe: StubGeoProbe
        let notifier: SpyNotifier
        let events: ManualEventSource
        let settings: SettingsStore
        let log: EventLogStore
    }

    private func makeHarness(
        snapshot: NetworkSnapshot,
        geo: GeoOutcome,
        enabled: Bool = true,
        processes: [ProcessSnapshot]? = nil
    ) -> Harness {
        let settings = SettingsStore(defaults: defaults, secrets: InMemorySecrets())
        settings.isEnabled = enabled
        settings.vpnServiceName = "Happ"
        settings.targetBundleIDs = [targetBundleID]

        let killer = SpyKiller()
        let probe = StubGeoProbe(geo)
        let notifier = SpyNotifier()
        let events = ManualEventSource()
        let log = EventLogStore(defaults: defaults)

        let vm = GuardVM(
            settings: settings,
            eventLog: log,
            snapshotReader: StubSnapshotReader(snapshotValue: snapshot),
            geoProbe: probe,
            locator: StubLocator(
                bundlePaths: [targetBundleID: targetPath],
                processes: processes ?? [
                    .init(pid: 500, executablePath: "\(targetPath)/Contents/MacOS/Target"),
                    .init(pid: 501, executablePath: "\(targetPath)/Contents/Frameworks/Helper.app/Contents/MacOS/Helper"),
                    .init(pid: 900, executablePath: "/Applications/Other.app/Contents/MacOS/Other"),
                ]
            ),
            killer: killer,
            notifier: notifier,
            events: events,
            debounceInterval: 0.01
        )

        return Harness(vm: vm, killer: killer, probe: probe, notifier: notifier,
                       events: events, settings: settings, log: log)
    }

    // MARK: - Локальный путь: сеть не трогаем

    func test_vpn_down_kills_targets_without_probing_network() async {
        let h = makeHarness(snapshot: vpnDownSnapshot(), geo: geoOutcome())

        h.vm.handle(.networkPath)

        XCTAssertEqual(h.vm.state, .unsafe(.vpnDown))
        XCTAssertEqual(h.killer.killedBatches, [[500, 501]], "убиты только процессы цели")
        let probeCalls = await h.probe.calls()
        XCTAssertEqual(probeCalls, 0, "сетевая проба не должна была запускаться")
    }

    func test_vpn_not_primary_kills() async {
        let snapshot = NetworkSnapshot(
            services: [
                .init(uuid: "WIFI", name: "Wi-Fi", activeInterface: "en0"),
                .init(uuid: "HAPP", name: "Happ", activeInterface: "utun6"),
            ],
            primaryServiceUUID: "WIFI"
        )
        let h = makeHarness(snapshot: snapshot, geo: geoOutcome())

        h.vm.handle(.networkPath)

        XCTAssertEqual(h.vm.state, .unsafe(.vpnNotPrimary))
    }

    func test_disabled_guard_never_kills() async {
        let h = makeHarness(snapshot: vpnDownSnapshot(), geo: geoOutcome(), enabled: false)

        h.vm.handle(.networkPath)

        XCTAssertEqual(h.vm.state, .disabled)
        XCTAssertTrue(h.killer.killedBatches.isEmpty)
    }
}

/// Локальный фейк секретов, чтобы не трогать Keychain в тестах VM.
private final class InMemorySecrets: SecretStoring, @unchecked Sendable {
    private var storage: [String: String] = [:]
    private let lock = NSLock()
    func read(account: String) -> String? {
        lock.lock(); defer { lock.unlock() }; return storage[account]
    }
    @discardableResult
    func write(_ value: String?, account: String) -> Bool {
        lock.lock(); defer { lock.unlock() }; storage[account] = value; return true
    }
}
```

- [ ] **Step 2: Прогнать, убедиться что падает**

Run: `swift test --filter GuardVMTests`
Expected: FAIL — `cannot find 'GuardVM' in scope`.

- [ ] **Step 3: Реализовать уведомления**

Файл `Sources/WetoShared/KillNotifying.swift`:

```swift
import Foundation
import UserNotifications

/// Граница системы: уведомление пользователя о срабатывании.
public protocol KillNotifying: Sendable {
    func notify(reasonText: String, killedCount: Int)
}

/// Системное уведомление.
///
/// Нужно потому, что цель исчезает молча и без объяснения это выглядит как краш.
public struct UserNotificationKillNotifier: KillNotifying {

    public init() {}

    public func notify(reasonText: String, killedCount: Int) {
        let content = UNMutableNotificationContent()
        content.title = "Weto: процессы завершены"
        content.body = "\(reasonText). Завершено процессов: \(killedCount)."
        content.sound = .default

        let request = UNNotificationRequest(
            identifier: UUID().uuidString,
            content: content,
            trigger: nil
        )
        UNUserNotificationCenter.current().add(request)
    }

    /// Запрашивается один раз при старте приложения.
    public static func requestAuthorization() {
        UNUserNotificationCenter.current().requestAuthorization(options: [.alert, .sound]) { _, _ in }
    }
}
```

- [ ] **Step 4: Реализовать `GuardVM` — локальная часть**

Файл `Sources/WetoShared/GuardVM.swift`:

```swift
import Foundation
import Observation
import WetoCore
import WetoSystem

/// Цвет индикатора в менюбаре.
///
/// Жёлтый и красный оба означают, что цели убиты; цвет различает причину.
public enum GuardStatusColor: Equatable, Sendable {
    case green, yellow, red, grey
}

public enum GuardState: Equatable, Sendable {
    case disabled
    case safe(GeoReading?)
    case unsafe(UnsafeReason)

    public var statusColor: GuardStatusColor {
        switch self {
        case .disabled: return .grey
        case .safe: return .green
        case .unsafe(let reason): return reason.isDegradedRatherThanBlocked ? .yellow : .red
        }
    }

    /// Флаг показывается только когда страна известна. При падении VPN сетевая
    /// проба не выполняется вовсе, показывать нечего.
    public func flag(lastReading: GeoReading?) -> String {
        switch self {
        case .disabled:
            return CountryFlag.unknown
        case .safe(let reading):
            return CountryFlag.emoji(for: reading?.primaryCountry ?? "")
        case .unsafe(let reason):
            switch reason {
            case .vpnNotConfigured, .vpnDown, .vpnNotPrimary, .geoUnavailable:
                return CountryFlag.unknown
            default:
                return CountryFlag.emoji(for: lastReading?.primaryCountry ?? "")
            }
        }
    }
}

/// Цикл охраны.
///
/// Ключевое разделение: `handle` сначала пробует принять решение локально
/// (`GuardPolicy.decideLocal`) — это бесплатно и мгновенно. В сеть идём только
/// когда локальных оснований нет, и с коалесценцией событий, потому что при
/// переключении Wi-Fi система выдаёт пачку уведомлений за секунду.
@Observable
@MainActor
public final class GuardVM {

    // MARK: - Наблюдаемое состояние

    public private(set) var state: GuardState = .disabled
    public private(set) var lastReading: GeoReading?
    public private(set) var lastEvaluation: Date?
    /// Заполняется, когда `kill` вернул EPERM: процесс принадлежит другому
    /// пользователю или root. Это не проглатывается, а показывается в UI.
    public private(set) var permissionFailure: String?
    public private(set) var availableVPNNames: [String] = []

    // MARK: - Зависимости

    @ObservationIgnored private let settings: SettingsStore
    @ObservationIgnored private let eventLog: EventLogStore
    @ObservationIgnored private let snapshotReader: NetworkSnapshotReading
    @ObservationIgnored private let geoProbe: GeoProbing
    @ObservationIgnored private let locator: ProcessLocating
    @ObservationIgnored private let killer: ProcessKilling
    @ObservationIgnored private let notifier: KillNotifying
    @ObservationIgnored private let events: NetworkEventSourcing
    @ObservationIgnored private let debounceInterval: TimeInterval

    @ObservationIgnored private var probeTask: Task<Void, Never>?
    @ObservationIgnored private var tickTask: Task<Void, Never>?
    @ObservationIgnored private var watchdogTask: Task<Void, Never>?

    public init(
        settings: SettingsStore,
        eventLog: EventLogStore,
        snapshotReader: NetworkSnapshotReading,
        geoProbe: GeoProbing,
        locator: ProcessLocating,
        killer: ProcessKilling,
        notifier: KillNotifying,
        events: NetworkEventSourcing,
        debounceInterval: TimeInterval = Constants.networkEventDebounceSeconds
    ) {
        self.settings = settings
        self.eventLog = eventLog
        self.snapshotReader = snapshotReader
        self.geoProbe = geoProbe
        self.locator = locator
        self.killer = killer
        self.notifier = notifier
        self.events = events
        self.debounceInterval = debounceInterval
    }

    deinit {
        probeTask?.cancel()
        tickTask?.cancel()
        watchdogTask?.cancel()
    }

    // MARK: - Жизненный цикл

    public func start() {
        refreshVPNNames()
        events.start { [weak self] trigger in
            Task { @MainActor [weak self] in self?.handle(trigger) }
        }
        startTicking()
    }

    public func stop() {
        events.stop()
        probeTask?.cancel(); probeTask = nil
        tickTask?.cancel(); tickTask = nil
        watchdogTask?.cancel(); watchdogTask = nil
    }

    /// Список имён сервисов для выпадающего списка в настройках.
    public func refreshVPNNames() {
        availableVPNNames = snapshotReader.snapshot().vpnCandidateNames
    }

    /// Сколько процессов цели найдено прямо сейчас — показывается в настройках,
    /// чтобы было видно, что охрана действительно видит приложение.
    public func runningProcessCount(forBundleID bundleID: String) -> Int {
        guard let path = locator.bundlePath(forBundleID: bundleID) else { return 0 }
        return ProcessMatcher.pids(in: locator.allProcesses(), belongingTo: [path]).count
    }

    // MARK: - Обработка триггеров

    public func handle(_ trigger: GuardTrigger) {
        if case .appLaunched(let bundleID) = trigger {
            // Запуск постороннего приложения нас не касается.
            guard settings.targetBundleIDs.contains(bundleID) else { return }
            // Цель поднялась при небезопасном состоянии — добиваем немедленно.
            if case .unsafe(let reason) = state {
                enforce(reason: reason)
                return
            }
        }

        let config = settings.guardConfig
        let vpn = VPNStatusResolver.status(
            serviceName: config.vpnServiceName,
            in: snapshotReader.snapshot()
        )

        if let local = GuardPolicy.decideLocal(
            isEnabled: settings.isEnabled,
            vpn: vpn,
            config: config
        ) {
            apply(local)
            return
        }

        scheduleProbe()
    }

    /// Тестовый и служебный крючок: дождаться отложенной сетевой пробы.
    public func awaitPendingProbe() async {
        await probeTask?.value
    }

    // MARK: - Private

    private func scheduleProbe() {
        probeTask?.cancel()
        probeTask = Task { [weak self] in
            guard let self else { return }
            let interval = await self.debounceInterval
            try? await Task.sleep(for: .seconds(interval))
            guard !Task.isCancelled else { return }
            await self.runProbe()
        }
    }

    public func runProbe() async {
        let config = settings.guardConfig
        let vpn = VPNStatusResolver.status(
            serviceName: config.vpnServiceName,
            in: snapshotReader.snapshot()
        )
        let geo = await geoProbe.probe()

        if case .resolved(let reading) = geo {
            lastReading = reading
        }

        apply(GuardPolicy.decide(GuardSignals(
            isEnabled: settings.isEnabled,
            vpn: vpn,
            geo: geo,
            config: config
        )))
    }

    private func apply(_ decision: GuardDecision) {
        lastEvaluation = Date()

        switch decision {
        case .safe:
            watchdogTask?.cancel(); watchdogTask = nil
            permissionFailure = nil
            state = settings.isEnabled && !settings.targetBundleIDs.isEmpty
                ? .safe(lastReading)
                : .disabled

        case .kill(let reason):
            let wasAlreadyUnsafe = state == .unsafe(reason)
            state = .unsafe(reason)
            enforce(reason: reason, silent: wasAlreadyUnsafe)
            startWatchdog()
        }
    }

    /// Находит и убивает процессы всех целей.
    /// `silent` подавляет запись в журнал и уведомление при повторных добиваниях.
    private func enforce(reason: UnsafeReason, silent: Bool = false) {
        let bundlePaths = settings.targetBundleIDs.compactMap(locator.bundlePath(forBundleID:))
        let pids = ProcessMatcher.pids(in: locator.allProcesses(), belongingTo: bundlePaths)
        guard !pids.isEmpty else { return }

        let results = killer.kill(pids: pids)
        let refused = results.filter { !$0.isTerminated }
        permissionFailure = refused.isEmpty
            ? nil
            : "Не удалось завершить процессы \(refused.map(\.pid)) — недостаточно прав"

        guard !silent else { return }

        eventLog.record(KillEvent(
            date: Date(),
            reasonText: reason.displayText,
            ip: lastReading?.ip,
            country: lastReading?.primaryCountry,
            killedPIDs: results.filter(\.isTerminated).map(\.pid)
        ))
        notifier.notify(reasonText: reason.displayText, killedCount: results.count)
    }

    private func startWatchdog() {
        guard watchdogTask == nil else { return }
        watchdogTask = Task { [weak self] in
            while !Task.isCancelled {
                try? await Task.sleep(for: .seconds(Constants.watchdogIntervalSeconds))
                guard !Task.isCancelled else { return }
                await MainActor.run { [weak self] in
                    guard let self, case .unsafe(let reason) = self.state else { return }
                    self.enforce(reason: reason, silent: true)
                }
            }
        }
    }

    private func startTicking() {
        tickTask?.cancel()
        tickTask = Task { [weak self] in
            while !Task.isCancelled {
                guard let self else { return }
                let interval = await self.settings.pollIntervalSeconds
                try? await Task.sleep(for: .seconds(interval))
                guard !Task.isCancelled else { return }
                await MainActor.run { [weak self] in self?.handle(.tick) }
            }
        }
    }
}
```

- [ ] **Step 5: Прогнать, убедиться что три первых теста проходят**

Run: `swift test --filter GuardVMTests`
Expected: PASS, 3 теста.

- [ ] **Step 6: Дописать тесты на сетевой путь, добивание и EPERM**

Дописать в `GuardVMTests`:

```swift
    // MARK: - Сетевой путь

    func test_healthy_network_leads_to_safe_state_without_killing() async {
        let h = makeHarness(snapshot: healthySnapshot(), geo: geoOutcome())

        h.vm.handle(.networkPath)
        await h.vm.awaitPendingProbe()

        XCTAssertEqual(h.vm.state, .safe(h.vm.lastReading))
        XCTAssertTrue(h.killer.killedBatches.isEmpty)
        XCTAssertEqual(h.vm.lastReading?.ip, "203.0.113.28")
    }

    func test_blocked_country_kills_and_records_event() async {
        let h = makeHarness(
            snapshot: healthySnapshot(),
            geo: geoOutcome(primary: "RU", confirmed: "RU")
        )

        h.vm.handle(.networkPath)
        await h.vm.awaitPendingProbe()

        XCTAssertEqual(h.vm.state, .unsafe(.blockedCountry(code: "RU", source: "ipinfo")))
        XCTAssertEqual(h.killer.killedBatches, [[500, 501]])
        XCTAssertEqual(h.log.events.count, 1)
        XCTAssertEqual(h.log.events.first?.country, "RU")
        XCTAssertEqual(h.notifier.messages.count, 1)
    }

    func test_missing_confirmation_kills_and_shows_yellow() async {
        let h = makeHarness(
            snapshot: healthySnapshot(),
            geo: geoOutcome(primary: "KZ", confirmed: nil)
        )

        h.vm.handle(.networkPath)
        await h.vm.awaitPendingProbe()

        XCTAssertEqual(h.vm.state, .unsafe(.confirmationUnavailable))
        XCTAssertEqual(h.vm.state.statusColor, .yellow)
        XCTAssertFalse(h.killer.killedBatches.isEmpty, "жёлтый — тоже убийство")
    }

    func test_geo_unavailable_kills_and_shows_red() async {
        let h = makeHarness(snapshot: healthySnapshot(), geo: .unavailable("timeout"))

        h.vm.handle(.networkPath)
        await h.vm.awaitPendingProbe()

        XCTAssertEqual(h.vm.state, .unsafe(.geoUnavailable("timeout")))
        XCTAssertEqual(h.vm.state.statusColor, .red)
    }

    func test_burst_of_events_collapses_into_single_probe() async {
        let h = makeHarness(snapshot: healthySnapshot(), geo: geoOutcome())

        for _ in 0..<10 { h.vm.handle(.networkPath) }
        await h.vm.awaitPendingProbe()

        let calls = await h.probe.calls()
        XCTAssertEqual(calls, 1, "пачка событий должна схлопнуться в одну пробу")
    }

    // MARK: - Добивание перезапусков

    func test_target_relaunch_while_unsafe_is_killed_again() async {
        let h = makeHarness(snapshot: vpnDownSnapshot(), geo: geoOutcome())
        h.vm.handle(.networkPath)
        XCTAssertEqual(h.killer.killedBatches.count, 1)

        h.vm.handle(.appLaunched(bundleID: targetBundleID))

        XCTAssertEqual(h.killer.killedBatches.count, 2, "перезапущенная цель должна быть добита")
        XCTAssertEqual(h.log.events.count, 1, "повторное добивание не засоряет журнал")
    }

    func test_launch_of_unrelated_app_is_ignored() async {
        let h = makeHarness(snapshot: vpnDownSnapshot(), geo: geoOutcome())
        h.vm.handle(.networkPath)

        h.vm.handle(.appLaunched(bundleID: "com.apple.TextEdit"))

        XCTAssertEqual(h.killer.killedBatches.count, 1)
    }

    // MARK: - Отказ прав

    func test_eperm_is_surfaced_instead_of_being_swallowed() async {
        let h = makeHarness(snapshot: vpnDownSnapshot(), geo: geoOutcome())
        h.killer.errorForPID = [500: EPERM]

        h.vm.handle(.networkPath)

        XCTAssertNotNil(h.vm.permissionFailure)
        XCTAssertTrue(h.vm.permissionFailure?.contains("500") == true)
    }

    func test_no_targets_running_does_not_produce_an_event() async {
        let h = makeHarness(
            snapshot: vpnDownSnapshot(),
            geo: geoOutcome(),
            processes: [.init(pid: 900, executablePath: "/Applications/Other.app/Contents/MacOS/Other")]
        )

        h.vm.handle(.networkPath)

        XCTAssertTrue(h.killer.killedBatches.isEmpty)
        XCTAssertTrue(h.log.events.isEmpty)
        XCTAssertEqual(h.vm.state, .unsafe(.vpnDown), "состояние всё равно небезопасное")
    }
```

- [ ] **Step 7: Прогнать, убедиться что все проходят**

Run: `swift test --filter GuardVMTests`
Expected: PASS, 12 тестов.

- [ ] **Step 8: Создать координатор**

Файл `Sources/WetoShared/AppCoordinator.swift`:

```swift
import Foundation
import Observation
import WetoCore
import WetoSystem

/// Единственный объект, который UI кладёт в `.environment(...)`.
/// Владеет всеми VM и собирает реальные реализации границ системы.
@Observable
@MainActor
public final class AppCoordinator {

    public let settings: SettingsStore
    public let eventLog: EventLogStore
    public let guardVM: GuardVM

    public init() {
        let settings = SettingsStore()
        let eventLog = EventLogStore()
        self.settings = settings
        self.eventLog = eventLog

        self.guardVM = GuardVM(
            settings: settings,
            eventLog: eventLog,
            snapshotReader: NetworkSnapshotReader(),
            geoProbe: GeoProbe(
                fetcher: URLSessionHTTPFetcher(),
                // Через коробку, а не через settings напрямую: GeoProbe —
                // отдельный актор, читать @MainActor-свойство оттуда нельзя.
                token: { [box = settings.tokenBox] in box.value }
            ),
            locator: ProcessRegistry(),
            killer: ProcessKiller(),
            notifier: UserNotificationKillNotifier(),
            events: NetworkEventSource()
        )
    }

    public func start() {
        UserNotificationKillNotifier.requestAuthorization()
        guardVM.start()
    }
}
```

- [ ] **Step 9: Прогнать весь набор**

Run: `swift test`
Expected: PASS, 99 тестов.

- [ ] **Step 10: Коммит**

```bash
git add Sources/WetoShared/KillNotifying.swift Sources/WetoShared/GuardVM.swift \
        Sources/WetoShared/AppCoordinator.swift \
        Tests/WetoSharedTests/GuardVMTests.swift
git commit -m "$TASK_NO - Цикл охраны и координатор приложения"
```

---

## Task 14: WetoDesign — порт токенов и компонентов

Копируем из `../blik/blik/Sources/BlikDesign` только нужное, с переименованием префикса `Blik` → `KS`. Панели вентиляторов, пресеты, поиск, логотип и температурные цвета не переносим — их нечем наполнить.

**Files:**
- Modify: `Package.swift` — добавить таргет `WetoDesign`
- Create: `Sources/WetoDesign/Tokens/Color+Hex.swift` ← порт `BlikDesign/Tokens/Color+Hex.swift` без изменений
- Create: `Sources/WetoDesign/Tokens/AdaptiveColor.swift` ← порт `BlikDesign/Tokens/AdaptiveColor.swift` без изменений
- Create: `Sources/WetoDesign/Tokens/KSPalette.swift` ← порт `BlikDesign/Tokens/BlikPalette.swift`, тип переименовать в `KSPalette`
- Create: `Sources/WetoDesign/Tokens/DesignTokens.swift` ← порт, ссылки `BlikPalette` → `KSPalette`, убрать `progressBar*` и `amberDark`
- Create: `Sources/WetoDesign/Components/KSBanner.swift` ← порт `BlikDesign/Components/BlikBanner.swift`
- Create: `Sources/WetoDesign/Components/KSPageContainer.swift` ← порт `BlikDesign/Components/BlikPageContainer.swift`
- Create: `Sources/WetoDesign/Components/KSStatusPill.swift` ← порт `BlikDesign/Components/BlikStatusPill.swift`
- Test: `Tests/WetoDesignTests/DesignTokensTests.swift`

**Interfaces:**
- Consumes: ничего.
- Produces: `DesignTokens` (`accent`, `green`, `amber`, `red`, `textSecondary`, `textTertiary`, `fontPrimary`, `fontPrimaryMedium`, `fontSecondary`), `AdaptiveColor.resolve(_:)`, `KSBanner(tone:systemImage:text:)`, `KSPageContainer`, `KSPageMetrics.rowInsets`, `KSStatusPill`.

- [ ] **Step 1: Добавить таргет в `Package.swift`**

В `products`: `.library(name: "WetoDesign", targets: ["WetoDesign"])`. В `targets`:

```swift
        .target(name: "WetoDesign", path: "Sources/WetoDesign"),
        .testTarget(
            name: "WetoDesignTests",
            dependencies: ["WetoDesign"],
            path: "Tests/WetoDesignTests"
        ),
```

- [ ] **Step 2: Скопировать файлы токенов и переименовать типы**

```bash
BLIK=../blik/blik/Sources/BlikDesign
mkdir -p Sources/WetoDesign/Tokens Sources/WetoDesign/Components

cp "$BLIK/Tokens/Color+Hex.swift"     Sources/WetoDesign/Tokens/
cp "$BLIK/Tokens/AdaptiveColor.swift" Sources/WetoDesign/Tokens/
cp "$BLIK/Tokens/BlikPalette.swift"   Sources/WetoDesign/Tokens/KSPalette.swift
cp "$BLIK/Tokens/DesignTokens.swift"  Sources/WetoDesign/Tokens/
cp "$BLIK/Components/BlikBanner.swift"        Sources/WetoDesign/Components/KSBanner.swift
cp "$BLIK/Components/BlikPageContainer.swift" Sources/WetoDesign/Components/KSPageContainer.swift
cp "$BLIK/Components/BlikStatusPill.swift"    Sources/WetoDesign/Components/KSStatusPill.swift

# Переименование префикса во всех скопированных файлах
grep -rl 'Blik' Sources/WetoDesign | xargs sed -i '' 's/Blik/KS/g'
```

- [ ] **Step 2a: Добавить в `KSBanner` тон `.success`**

У blik в перечислении тонов есть `.error`, `.warn`, `.info`, `.accent`, но нет `.success` — зелёного состояния у него не бывает. Нам он нужен для «Всё в порядке». Добавить в enum тонов внутри `KSBanner.swift`:

```swift
    case success
```

и ветку в вычислении цвета:

```swift
        case .success: return DesignTokens.green
```

После `sed` перечисление тонов получит имя вида `KSBannerTone` (в blik оно называется `BlikBannerTone`). Если после переименования имя оказалось другим — привести к `KSBannerTone`, потому что на него ссылается `StatusPopupView` из Task 16.

- [ ] **Step 3: Убрать из `DesignTokens.swift` то, чему нет применения**

Удалить блоки `// MARK: - Progress bar` (`progressBarHeight`, `progressBarBg`) и константу `amberDark`. Заменить комментарий-ссылку на `icon/blik/palette/palette.json` на «Палитра унаследована от проекта blik».

- [ ] **Step 4: Написать тест на разрешение цветов**

Файл `Tests/WetoDesignTests/DesignTokensTests.swift`:

```swift
import XCTest
import SwiftUI
@testable import WetoDesign

final class DesignTokensTests: XCTestCase {

    func test_adaptive_color_differs_between_schemes() {
        let light = DesignTokens.textSecondary.resolve(.light)
        let dark = DesignTokens.textSecondary.resolve(.dark)
        XCTAssertNotEqual(light, dark)
    }

    func test_status_colors_are_distinct() {
        XCTAssertNotEqual(DesignTokens.green, DesignTokens.red)
        XCTAssertNotEqual(DesignTokens.green, DesignTokens.amber)
        XCTAssertNotEqual(DesignTokens.amber, DesignTokens.red)
    }

    func test_accent_resolves_in_both_schemes() {
        XCTAssertNotNil(DesignTokens.accent.resolve(.light))
        XCTAssertNotNil(DesignTokens.accent.resolve(.dark))
    }
}
```

- [ ] **Step 5: Собрать и прогнать**

Run: `swift build && swift test --filter DesignTokensTests`
Expected: PASS, 3 теста. Если сборка ругается на оставшиеся упоминания `Blik` — доправить вручную: `grep -rn 'Blik' Sources/WetoDesign`.

- [ ] **Step 6: Коммит**

```bash
git add Package.swift Sources/WetoDesign Tests/WetoDesignTests
git commit -m "$TASK_NO - Порт дизайн-токенов и компонентов"
```

---

## Task 15: Лейбл менюбара и приложение

Появляется исполняемый таргет и первый запускаемый бинарник.

**Files:**
- Modify: `Package.swift` — добавить `WetoMenuBar`
- Create: `Sources/WetoDesign/Components/MenuBarImageRenderer.swift`
- Create: `Sources/WetoMenuBar/WetoMenuBarApp.swift`
- Create: `Sources/WetoMenuBar/MenuBarLabel.swift`
- Test: `Tests/WetoDesignTests/MenuBarImageRendererTests.swift`

**Interfaces:**
- Consumes: `GuardState`, `GuardStatusColor`, `CountryFlag`, `AppCoordinator` (Tasks 1, 13).
- Produces: `MenuBarImageRenderer.image(flag:color:) -> NSImage`, `MenuBarLabel`, `@main struct WetoMenuBarApp`.

- [ ] **Step 1: Добавить таргет в `Package.swift`**

```swift
        .executableTarget(
            name: "WetoMenuBar",
            dependencies: [
                "WetoCore", "WetoSystem", "WetoShared", "WetoDesign",
            ],
            path: "Sources/WetoMenuBar"
        ),
```

- [ ] **Step 2: Написать падающий тест на рендерер**

Файл `Tests/WetoDesignTests/MenuBarImageRendererTests.swift`:

```swift
import XCTest
import AppKit
@testable import WetoDesign

final class MenuBarImageRendererTests: XCTestCase {

    func test_image_has_menu_bar_height() {
        let image = MenuBarImageRenderer.image(flag: "🇰🇿", color: .systemGreen)
        XCTAssertEqual(image.size.height, 22)
        XCTAssertGreaterThan(image.size.width, 0)
    }

    func test_image_is_not_a_template() {
        // Шаблонные изображения перекрашиваются системой в монохром,
        // а нам нужны и цвет флага, и цвет кружка статуса.
        let image = MenuBarImageRenderer.image(flag: "🇰🇿", color: .systemGreen)
        XCTAssertFalse(image.isTemplate)
    }

    func test_identical_input_returns_cached_instance() {
        let first = MenuBarImageRenderer.image(flag: "🇰🇿", color: .systemGreen)
        let second = MenuBarImageRenderer.image(flag: "🇰🇿", color: .systemGreen)
        XCTAssertTrue(first === second)
    }

    func test_different_color_produces_different_instance() {
        let green = MenuBarImageRenderer.image(flag: "🇰🇿", color: .systemGreen)
        let red = MenuBarImageRenderer.image(flag: "🇰🇿", color: .systemRed)
        XCTAssertFalse(green === red)
    }

    func test_white_flag_is_rendered_without_crashing() {
        XCTAssertGreaterThan(
            MenuBarImageRenderer.image(flag: "🏳️", color: .systemGray).size.width,
            0
        )
    }
}
```

- [ ] **Step 3: Прогнать, убедиться что падает**

Run: `swift test --filter MenuBarImageRendererTests`
Expected: FAIL — `cannot find 'MenuBarImageRenderer' in scope`.

- [ ] **Step 4: Реализовать рендерер**

Файл `Sources/WetoDesign/Components/MenuBarImageRenderer.swift`:

```swift
import AppKit

/// Рендер лейбла менюбара: флаг страны и кружок статуса.
///
/// Рисуем в `NSImage`, а не собираем `HStack` в лейбле `MenuBarExtra`:
/// вёрстка лейбла там жёстко ограничена, а изображение даёт полный контроль
/// над отступами. Приём унаследован от `../blik/blik`.
///
/// `isTemplate` остаётся `false` — в отличие от blik, где лейбл монохромный.
/// Шаблонный режим перекрасил бы и флаг, и кружок в цвет системного текста,
/// а именно цвет здесь и несёт смысл.
public enum MenuBarImageRenderer {

    private static let cacheLimit = 32
    private static let lock = NSLock()
    private static var cache: [String: NSImage] = [:]
    private static var order: [String] = []

    public static func image(flag: String, color: NSColor) -> NSImage {
        let key = "\(flag)|\(color.description)"

        lock.lock()
        if let hit = cache[key] {
            lock.unlock()
            return hit
        }
        lock.unlock()

        let image = render(flag: flag, color: color)

        lock.lock()
        cache[key] = image
        order.append(key)
        if order.count > cacheLimit {
            cache.removeValue(forKey: order.removeFirst())
        }
        lock.unlock()

        return image
    }

    // MARK: - Private

    private static func render(flag: String, color: NSColor) -> NSImage {
        let height: CGFloat = 22
        let dotDiameter: CGFloat = 7
        let gap: CGFloat = 4

        let font = NSFont.systemFont(ofSize: 14)
        let attributes: [NSAttributedString.Key: Any] = [.font: font]
        let flagSize = (flag as NSString).size(withAttributes: attributes)

        let width = flagSize.width + gap + dotDiameter + 2
        let image = NSImage(size: NSSize(width: width, height: height))

        image.lockFocus()

        (flag as NSString).draw(
            at: NSPoint(x: 0, y: (height - flagSize.height) / 2),
            withAttributes: attributes
        )

        let dotRect = NSRect(
            x: flagSize.width + gap,
            y: (height - dotDiameter) / 2,
            width: dotDiameter,
            height: dotDiameter
        )
        color.setFill()
        NSBezierPath(ovalIn: dotRect).fill()

        image.unlockFocus()
        image.isTemplate = false
        return image
    }
}
```

- [ ] **Step 5: Прогнать, убедиться что проходят**

Run: `swift test --filter MenuBarImageRendererTests`
Expected: PASS, 5 тестов.

- [ ] **Step 6: Создать лейбл**

Файл `Sources/WetoMenuBar/MenuBarLabel.swift`:

```swift
import SwiftUI
import AppKit
import WetoCore
import WetoShared
import WetoDesign

struct MenuBarLabel: View {
    @Environment(AppCoordinator.self) private var coordinator

    var body: some View {
        let state = coordinator.guardVM.state
        Image(nsImage: MenuBarImageRenderer.image(
            flag: state.flag(lastReading: coordinator.guardVM.lastReading),
            color: Self.color(for: state.statusColor)
        ))
    }

    private static func color(for status: GuardStatusColor) -> NSColor {
        switch status {
        case .green:  return .systemGreen
        case .yellow: return .systemYellow
        case .red:    return .systemRed
        case .grey:   return .systemGray
        }
    }
}
```

- [ ] **Step 7: Создать точку входа**

Файл `Sources/WetoMenuBar/WetoMenuBarApp.swift` (имя файла по имени приложения, не `main.swift` — иначе конфликт с `@main`):

```swift
import SwiftUI
import AppKit
import WetoShared

/// Гейт от повторного запуска бинарника напрямую из терминала или отладчика.
/// Приём унаследован от `../blik/blik/Sources/BlikMenuBar/BlikMenuBarApp.swift`.
private final class SingleInstanceGuard {
    static let portName = "com.weto.app.singleton" as CFString
    private var port: CFMessagePort?

    func acquire() -> Bool {
        var context = CFMessagePortContext()
        var isRemote: DarwinBoolean = false
        port = CFMessagePortCreateLocal(nil, Self.portName, { _, _, _, _ in nil }, &context, &isRemote)
        return port != nil
    }
}

@main
struct WetoMenuBarApp: App {

    @State private var coordinator = AppCoordinator()
    private static let instanceGuard = SingleInstanceGuard()

    init() {
        if !Self.instanceGuard.acquire() {
            DispatchQueue.main.async { NSApplication.shared.terminate(nil) }
            return
        }
        // Приложение живёт только в менюбаре, иконки в Dock у него нет.
        NSApplication.shared.setActivationPolicy(.accessory)
    }

    var body: some Scene {
        MenuBarExtra {
            StatusPopupView()
                .environment(coordinator)
                .frame(width: 340)
                .task { coordinator.start() }
        } label: {
            MenuBarLabel()
                .environment(coordinator)
        }
        .menuBarExtraStyle(.window)

        Window("Настройки Weto", id: SettingsWindow.identifier) {
            SettingsWindow()
                .environment(coordinator)
        }
        .defaultSize(width: 560, height: 620)
    }
}
```

Собираться это будет только после Task 16, где появятся `StatusPopupView` и `SettingsWindow`.

- [ ] **Step 8: Коммит (сборка исполняемого таргета ещё красная, это ожидаемо)**

```bash
git add Package.swift Sources/WetoDesign/Components/MenuBarImageRenderer.swift \
        Sources/WetoMenuBar Tests/WetoDesignTests/MenuBarImageRendererTests.swift
git commit -m "$TASK_NO - Лейбл менюбара и точка входа приложения"
```

Проверка `swift build --target WetoDesign && swift test --filter WetoDesignTests` обязана быть зелёной — исполняемый таргет достраивается в следующей задаче.

---

## Task 16: Попап и окно настроек

Достраивает исполняемый таргет до запускаемого состояния. После этой задачи приложение впервые можно запустить и увидеть в менюбаре.

**Files:**
- Create: `Sources/WetoShared/StatusPresentation.swift`
- Create: `Sources/WetoMenuBar/StatusPopupView.swift`
- Create: `Sources/WetoMenuBar/Settings/SettingsWindow.swift`
- Create: `Sources/WetoMenuBar/Settings/TargetsSection.swift`
- Test: `Tests/WetoSharedTests/StatusPresentationTests.swift`

**Interfaces:**
- Consumes: `GuardState`, `UnsafeReason.displayText`, `AppCoordinator`, `SettingsStore`, `EventLogStore`, `Constants.pollIntervalOptions`, `DesignTokens`, `KSBanner`, `KSPageContainer`.
- Produces: `StatusPresentation.headline(for:) -> String`, `StatusPresentation.detail(for:reading:) -> String?`, `StatusPresentation.bannerTone(for:) -> BannerTone`, `SettingsWindow.identifier: String`, вьюхи `StatusPopupView`, `SettingsWindow`, `TargetsSection`.

- [ ] **Step 1: Написать падающий тест на тексты состояния**

Логика формулировок отделена от вьюх намеренно: тексты — это то, что пользователь читает в момент инцидента, и они должны быть под тестом.

Файл `Tests/WetoSharedTests/StatusPresentationTests.swift`:

```swift
import XCTest
@testable import WetoShared
import WetoCore

final class StatusPresentationTests: XCTestCase {

    private let reading = GeoReading(
        ip: "203.0.113.28",
        asn: "AS49791",
        primaryCountry: "KZ",
        confirmedCountry: "KZ",
        confirmSource: .ipwhois
    )

    func test_disabled_state_says_guard_is_off() {
        XCTAssertEqual(StatusPresentation.headline(for: .disabled), "Охрана выключена")
    }

    func test_safe_state_says_everything_is_fine() {
        XCTAssertEqual(StatusPresentation.headline(for: .safe(reading)), "Всё в порядке")
    }

    func test_unsafe_headline_uses_reason_text() {
        XCTAssertEqual(
            StatusPresentation.headline(for: .unsafe(.vpnDown)),
            "VPN не поднят"
        )
        XCTAssertEqual(
            StatusPresentation.headline(for: .unsafe(.confirmationUnavailable)),
            "Подтверждающие сервисы недоступны"
        )
    }

    func test_detail_shows_ip_asn_and_both_countries() {
        let detail = StatusPresentation.detail(for: .safe(reading), reading: reading)
        XCTAssertEqual(detail, "203.0.113.28 · AS49791 · ipinfo: KZ · ipwhois: KZ")
    }

    func test_detail_marks_missing_confirmation() {
        let degraded = GeoReading(
            ip: "203.0.113.28", asn: "AS49791", primaryCountry: "KZ",
            confirmedCountry: nil, confirmSource: nil
        )
        let detail = StatusPresentation.detail(for: .unsafe(.confirmationUnavailable), reading: degraded)
        XCTAssertEqual(detail, "203.0.113.28 · AS49791 · ipinfo: KZ · подтверждение: нет")
    }

    func test_detail_is_nil_when_nothing_is_known() {
        XCTAssertNil(StatusPresentation.detail(for: .unsafe(.vpnDown), reading: nil))
    }

    func test_banner_tone_matches_status_color() {
        XCTAssertEqual(StatusPresentation.bannerTone(for: .disabled), .info)
        XCTAssertEqual(StatusPresentation.bannerTone(for: .safe(reading)), .success)
        XCTAssertEqual(StatusPresentation.bannerTone(for: .unsafe(.confirmationUnavailable)), .warn)
        XCTAssertEqual(StatusPresentation.bannerTone(for: .unsafe(.vpnDown)), .error)
    }
}
```

- [ ] **Step 2: Прогнать, убедиться что падает**

Run: `swift test --filter StatusPresentationTests`
Expected: FAIL — `cannot find 'StatusPresentation' in scope`.

- [ ] **Step 3: Реализовать формулировки**

Файл `Sources/WetoShared/StatusPresentation.swift`:

```swift
import Foundation
import WetoCore

/// Тон плашки в попапе. Совпадает по смыслу с `GuardStatusColor`,
/// но живёт отдельно, чтобы `WetoShared` не зависел от дизайн-модуля.
public enum BannerTone: Equatable, Sendable {
    case info, success, warn, error
}

/// Тексты состояния для попапа менюбара.
///
/// Вынесено из вьюх: это то, что пользователь читает в момент инцидента,
/// и оно должно быть под тестом.
public enum StatusPresentation {

    public static func headline(for state: GuardState) -> String {
        switch state {
        case .disabled: return "Охрана выключена"
        case .safe: return "Всё в порядке"
        case .unsafe(let reason): return reason.displayText
        }
    }

    /// Строка с деталями показания. `nil`, когда показания ещё нет —
    /// например, при падении VPN сетевая проба не выполняется вовсе.
    public static func detail(for state: GuardState, reading: GeoReading?) -> String? {
        guard let reading else { return nil }

        var parts = [reading.ip]
        if let asn = reading.asn { parts.append(asn) }
        parts.append("ipinfo: \(reading.primaryCountry)")

        if let confirmed = reading.confirmedCountry, let source = reading.confirmSource {
            parts.append("\(source.rawValue): \(confirmed)")
        } else {
            parts.append("подтверждение: нет")
        }

        return parts.joined(separator: " · ")
    }

    public static func bannerTone(for state: GuardState) -> BannerTone {
        switch state.statusColor {
        case .grey: return .info
        case .green: return .success
        case .yellow: return .warn
        case .red: return .error
        }
    }
}
```

- [ ] **Step 4: Прогнать, убедиться что проходят**

Run: `swift test --filter StatusPresentationTests`
Expected: PASS, 7 тестов.

- [ ] **Step 5: Создать попап**

Файл `Sources/WetoMenuBar/StatusPopupView.swift`:

```swift
import SwiftUI
import AppKit
import WetoCore
import WetoShared
import WetoDesign

struct StatusPopupView: View {
    @Environment(AppCoordinator.self) private var coordinator
    @Environment(\.openWindow) private var openWindow
    @Environment(\.colorScheme) private var colorScheme

    var body: some View {
        VStack(alignment: .leading, spacing: 8) {
            header
            statusBanner

            if let detail = StatusPresentation.detail(
                for: coordinator.guardVM.state,
                reading: coordinator.guardVM.lastReading
            ) {
                Text(detail)
                    .font(DesignTokens.fontSecondary)
                    .foregroundStyle(DesignTokens.textSecondary.resolve(colorScheme))
                    .textSelection(.enabled)
            }

            if let failure = coordinator.guardVM.permissionFailure {
                KSBanner(tone: .error, systemImage: "lock.slash", text: failure)
            }

            Divider()
            journal
            Divider()
            footer
        }
        .padding(12)
    }

    private var header: some View {
        HStack {
            Text("Weto").font(.headline)
            Spacer()
            Toggle("", isOn: Binding(
                get: { coordinator.settings.isEnabled },
                set: { newValue in
                    coordinator.settings.isEnabled = newValue
                    coordinator.guardVM.handle(.tick)
                }
            ))
            .toggleStyle(.switch)
            .labelsHidden()
            .tint(DesignTokens.accent.resolve(colorScheme))
        }
    }

    private var statusBanner: some View {
        KSBanner(
            tone: Self.bannerTone(StatusPresentation.bannerTone(for: coordinator.guardVM.state)),
            systemImage: "shield",
            text: StatusPresentation.headline(for: coordinator.guardVM.state)
        )
    }

    /// `BannerTone` живёт в `WetoShared`, тон компонента — в `WetoDesign`.
    /// Слой VM намеренно не зависит от дизайн-модуля, поэтому маппинг здесь.
    private static func bannerTone(_ tone: BannerTone) -> KSBannerTone {
        switch tone {
        case .info:    return .info
        case .success: return .success
        case .warn:    return .warn
        case .error:   return .error
        }
    }

    @ViewBuilder
    private var journal: some View {
        if coordinator.eventLog.preview.isEmpty {
            Text("Срабатываний не было")
                .font(DesignTokens.fontSecondary)
                .foregroundStyle(DesignTokens.textTertiary.resolve(colorScheme))
        } else {
            ForEach(coordinator.eventLog.preview) { event in
                HStack(alignment: .firstTextBaseline, spacing: 6) {
                    Text(event.date, format: .dateTime.hour().minute().second())
                        .font(DesignTokens.fontSecondary)
                        .foregroundStyle(DesignTokens.textTertiary.resolve(colorScheme))
                    Text(event.reasonText)
                        .font(DesignTokens.fontSecondary)
                        .lineLimit(1)
                }
            }
        }
    }

    private var footer: some View {
        HStack(spacing: 6) {
            Button("Настройки") {
                openWindow(id: SettingsWindow.identifier)
                NSApplication.shared.activate(ignoringOtherApps: true)
            }
            .buttonStyle(.borderedProminent)
            .tint(DesignTokens.accent.resolve(colorScheme))

            Spacer()

            Button("Выход") { NSApplication.shared.terminate(nil) }
                .foregroundStyle(DesignTokens.textSecondary.resolve(colorScheme))
        }
    }
}
```

- [ ] **Step 6: Создать секцию целей**

Файл `Sources/WetoMenuBar/Settings/TargetsSection.swift`:

```swift
import SwiftUI
import AppKit
import WetoShared
import WetoDesign

/// Список охраняемых приложений. Целей может быть несколько.
struct TargetsSection: View {
    @Environment(AppCoordinator.self) private var coordinator
    @Environment(\.colorScheme) private var colorScheme

    var body: some View {
        Section("Цели") {
            ForEach(coordinator.settings.targetBundleIDs, id: \.self) { bundleID in
                LabeledContent(displayName(for: bundleID)) {
                    VStack(alignment: .trailing, spacing: 2) {
                        Text(bundleID).font(DesignTokens.fontSecondary)
                        Text("процессов сейчас: \(coordinator.guardVM.runningProcessCount(forBundleID: bundleID))")
                            .font(DesignTokens.fontSecondary)
                            .foregroundStyle(DesignTokens.textTertiary.resolve(colorScheme))
                    }
                }
            }
            .onDelete { offsets in
                var ids = coordinator.settings.targetBundleIDs
                ids.remove(atOffsets: offsets)
                coordinator.settings.targetBundleIDs = ids
            }

            Button("Добавить приложение…") { addTarget() }
        }
        .listRowInsets(KSPageMetrics.rowInsets)
    }

    private func displayName(for bundleID: String) -> String {
        guard let url = NSWorkspace.shared.urlForApplication(withBundleIdentifier: bundleID)
        else { return bundleID }
        return url.deletingPathExtension().lastPathComponent
    }

    private func addTarget() {
        let panel = NSOpenPanel()
        panel.directoryURL = URL(fileURLWithPath: "/Applications")
        panel.allowedContentTypes = [.application]
        panel.allowsMultipleSelection = true
        panel.canChooseDirectories = false

        guard panel.runModal() == .OK else { return }

        let added = panel.urls.compactMap { Bundle(url: $0)?.bundleIdentifier }
        var ids = coordinator.settings.targetBundleIDs
        for id in added where !ids.contains(id) { ids.append(id) }
        coordinator.settings.targetBundleIDs = ids
    }
}
```

- [ ] **Step 7: Создать окно настроек**

Файл `Sources/WetoMenuBar/Settings/SettingsWindow.swift`:

```swift
import SwiftUI
import AppKit
import WetoCore
import WetoShared
import WetoDesign

struct SettingsWindow: View {
    static let identifier = "settings"

    @Environment(AppCoordinator.self) private var coordinator
    @Environment(\.colorScheme) private var colorScheme

    @State private var newCountryCode = ""
    @State private var newIPRange = ""

    var body: some View {
        KSPageContainer {
            List {
                TargetsSection()
                vpnSection
                geoSection
                blacklistSection
                journalSection
            }
        }
        .onAppear { coordinator.guardVM.refreshVPNNames() }
    }

    private var vpnSection: some View {
        Section("VPN") {
            Picker("Сервис", selection: Binding(
                get: { coordinator.settings.vpnServiceName ?? "" },
                set: { coordinator.settings.vpnServiceName = $0.isEmpty ? nil : $0 }
            )) {
                Text("Не выбран").tag("")
                ForEach(coordinator.guardVM.availableVPNNames, id: \.self) { name in
                    Text(name).tag(name)
                }
            }
            Text("Безопасно, когда туннель поднят и держит default route.")
                .font(DesignTokens.fontSecondary)
                .foregroundStyle(DesignTokens.textSecondary.resolve(colorScheme))
        }
        .listRowInsets(KSPageMetrics.rowInsets)
    }

    private var geoSection: some View {
        Section("Гео") {
            SecureField("Токен ipinfo", text: Binding(
                get: { coordinator.settings.ipinfoToken },
                set: { coordinator.settings.ipinfoToken = $0 }
            ))

            Picker("Интервал опроса", selection: Binding(
                get: { coordinator.settings.pollIntervalSeconds },
                set: { coordinator.settings.pollIntervalSeconds = $0 }
            )) {
                ForEach(Constants.pollIntervalOptions, id: \.self) { value in
                    Text("\(Int(value)) с").tag(value)
                }
            }
            .pickerStyle(.segmented)

            ForEach(coordinator.settings.blockedCountryCodes, id: \.self) { code in
                Text("\(CountryFlag.emoji(for: code))  \(code)")
            }
            .onDelete { offsets in
                var codes = coordinator.settings.blockedCountryCodes
                codes.remove(atOffsets: offsets)
                coordinator.settings.blockedCountryCodes = codes
            }

            HStack {
                TextField("Код страны, например RU", text: $newCountryCode)
                Button("Добавить") {
                    guard !newCountryCode.isEmpty else { return }
                    coordinator.settings.blockedCountryCodes += [newCountryCode]
                    newCountryCode = ""
                }
            }
        }
        .listRowInsets(KSPageMetrics.rowInsets)
    }

    private var blacklistSection: some View {
        Section("Чёрный список IP") {
            ForEach(coordinator.settings.blockedIPRangeTexts, id: \.self) { text in
                HStack {
                    Text(text)
                    if IPRange(text) == nil {
                        Spacer()
                        Label("не разобран", systemImage: "exclamationmark.triangle")
                            .font(DesignTokens.fontSecondary)
                            .foregroundStyle(DesignTokens.red)
                    }
                }
            }
            .onDelete { offsets in
                var ranges = coordinator.settings.blockedIPRangeTexts
                ranges.remove(atOffsets: offsets)
                coordinator.settings.blockedIPRangeTexts = ranges
            }

            HStack {
                TextField("IP или CIDR, например 198.51.100.0/22", text: $newIPRange)
                Button("Добавить") {
                    guard !newIPRange.isEmpty else { return }
                    coordinator.settings.blockedIPRangeTexts += [newIPRange]
                    newIPRange = ""
                }
            }
        }
        .listRowInsets(KSPageMetrics.rowInsets)
    }

    private var journalSection: some View {
        Section("Журнал") {
            if coordinator.eventLog.events.isEmpty {
                Text("Срабатываний не было")
                    .foregroundStyle(DesignTokens.textTertiary.resolve(colorScheme))
            } else {
                ForEach(coordinator.eventLog.events) { event in
                    VStack(alignment: .leading, spacing: 2) {
                        Text(event.reasonText)
                        Text("\(event.date.formatted()) · \(event.ip ?? "адрес неизвестен") · процессов: \(event.killedPIDs.count)")
                            .font(DesignTokens.fontSecondary)
                            .foregroundStyle(DesignTokens.textSecondary.resolve(colorScheme))
                    }
                }
                Button("Очистить журнал", role: .destructive) {
                    coordinator.eventLog.clear()
                }
            }
        }
        .listRowInsets(KSPageMetrics.rowInsets)
    }
}
```

- [ ] **Step 8: Собрать и запустить**

Run: `swift build && swift run WetoMenuBar`
Expected: в менюбаре появляется 🏳️ с серым кружком (охрана выключена по умолчанию). Клик открывает попап, кнопка «Настройки» открывает окно.

Проверить руками:
- добавить любое подопытное приложение в «Цели»,
- выбрать `Happ` в разделе VPN,
- вставить токен ipinfo,
- включить тумблер — статус должен стать зелёным с флагом 🇰🇿,
- выключить Happ — цели должны быть завершены, кружок красный, в попапе «VPN не поднят».

- [ ] **Step 9: Прогнать весь набор**

Run: `swift test`
Expected: PASS, 114 тестов.

- [ ] **Step 10: Коммит**

```bash
git add Sources/WetoShared/StatusPresentation.swift Sources/WetoMenuBar \
        Tests/WetoSharedTests/StatusPresentationTests.swift
git commit -m "$TASK_NO - Попап статуса и окно настроек"
```

---

## Task 17: Версии и проверка обновлений

Чистая часть автообновления: сравнение версий и разбор ответа GitHub Releases API. Демон и XPC — в следующей задаче.

**Files:**
- Create: `Sources/WetoCore/SemanticVersion.swift`
- Create: `Sources/WetoCore/Model/UpdateInfo.swift`
- Create: `Sources/WetoCore/ReleaseParser.swift`
- Test: `Tests/WetoCoreTests/SemanticVersionTests.swift`
- Test: `Tests/WetoCoreTests/ReleaseParserTests.swift`

**Interfaces:**
- Consumes: `Constants.appVersion`, `Constants.githubOwner`, `Constants.githubRepo`.
- Produces: `SemanticVersion(string:)` с `Comparable`, `UpdateInfo(currentVersion:latestVersion:downloadURL:releaseNotes:isNewer:)` `Codable`, `ReleaseParser.latestReleaseURL: String`, `ReleaseParser.parse(_:currentVersion:) -> Result<UpdateInfo, Error>`.

- [ ] **Step 1: Написать падающий тест на версии**

Файл `Tests/WetoCoreTests/SemanticVersionTests.swift`:

```swift
import XCTest
@testable import WetoCore

final class SemanticVersionTests: XCTestCase {

    func test_parses_three_component_version() {
        let version = SemanticVersion(string: "1.2.3")
        XCTAssertEqual(version?.major, 1)
        XCTAssertEqual(version?.minor, 2)
        XCTAssertEqual(version?.patch, 3)
    }

    func test_missing_components_default_to_zero() {
        XCTAssertEqual(SemanticVersion(string: "2"), SemanticVersion(string: "2.0.0"))
        XCTAssertEqual(SemanticVersion(string: "2.5"), SemanticVersion(string: "2.5.0"))
    }

    func test_ordering_is_by_component_significance() {
        XCTAssertTrue(SemanticVersion(string: "1.0.0")! < SemanticVersion(string: "1.0.1")!)
        XCTAssertTrue(SemanticVersion(string: "1.9.9")! < SemanticVersion(string: "1.10.0")!)
        XCTAssertTrue(SemanticVersion(string: "1.10.0")! < SemanticVersion(string: "2.0.0")!)
    }

    func test_equal_versions_are_not_less_than_each_other() {
        let a = SemanticVersion(string: "1.2.3")!
        let b = SemanticVersion(string: "1.2.3")!
        XCTAssertFalse(a < b)
        XCTAssertFalse(b < a)
        XCTAssertEqual(a, b)
    }

    func test_malformed_input_returns_nil() {
        XCTAssertNil(SemanticVersion(string: ""))
        XCTAssertNil(SemanticVersion(string: "версия"))
        XCTAssertNil(SemanticVersion(string: "1.x.3"))
        XCTAssertNil(SemanticVersion(string: "1.2.3.4"))
    }
}
```

- [ ] **Step 2: Прогнать, убедиться что падает**

Run: `swift test --filter SemanticVersionTests`
Expected: FAIL — `cannot find 'SemanticVersion' in scope`.

- [ ] **Step 3: Реализовать версии**

Файл `Sources/WetoCore/SemanticVersion.swift`:

```swift
import Foundation

/// Версия вида `major.minor.patch`. Недостающие компоненты считаются нулями,
/// чтобы тег `v2` сравнивался корректно.
public struct SemanticVersion: Equatable, Comparable, Sendable {
    public let major: Int
    public let minor: Int
    public let patch: Int

    public init?(string: String) {
        let trimmed = string.trimmingCharacters(in: .whitespaces)
        guard !trimmed.isEmpty else { return nil }

        let parts = trimmed.split(separator: ".", omittingEmptySubsequences: false)
        guard (1...3).contains(parts.count) else { return nil }

        var numbers: [Int] = []
        for part in parts {
            guard let value = Int(part), value >= 0 else { return nil }
            numbers.append(value)
        }
        while numbers.count < 3 { numbers.append(0) }

        major = numbers[0]
        minor = numbers[1]
        patch = numbers[2]
    }

    public static func < (lhs: SemanticVersion, rhs: SemanticVersion) -> Bool {
        (lhs.major, lhs.minor, lhs.patch) < (rhs.major, rhs.minor, rhs.patch)
    }
}
```

- [ ] **Step 4: Прогнать, убедиться что проходят**

Run: `swift test --filter SemanticVersionTests`
Expected: PASS, 5 тестов.

- [ ] **Step 5: Написать падающий тест на разбор релиза**

Файл `Tests/WetoCoreTests/ReleaseParserTests.swift`:

```swift
import XCTest
@testable import WetoCore

final class ReleaseParserTests: XCTestCase {

    private func releaseJSON(tag: String, assetName: String = "Weto-1.1.0.pkg") -> Data {
        Data("""
        {"tag_name":"\(tag)","body":"Исправлены ошибки",
         "assets":[{"name":"\(assetName)",
         "browser_download_url":"https://example.com/\(assetName)","size":1024}]}
        """.utf8)
    }

    func test_newer_tag_is_reported_as_update() throws {
        let result = try ReleaseParser.parse(releaseJSON(tag: "v1.1.0"), currentVersion: "1.0.0").get()
        XCTAssertTrue(result.isNewer)
        XCTAssertEqual(result.latestVersion, "1.1.0")
        XCTAssertEqual(result.currentVersion, "1.0.0")
        XCTAssertEqual(result.downloadURL, "https://example.com/Weto-1.1.0.pkg")
        XCTAssertEqual(result.releaseNotes, "Исправлены ошибки")
    }

    func test_tag_without_v_prefix_is_accepted() throws {
        let result = try ReleaseParser.parse(releaseJSON(tag: "1.1.0"), currentVersion: "1.0.0").get()
        XCTAssertEqual(result.latestVersion, "1.1.0")
    }

    func test_same_version_is_not_an_update() throws {
        let result = try ReleaseParser.parse(releaseJSON(tag: "v1.0.0"), currentVersion: "1.0.0").get()
        XCTAssertFalse(result.isNewer)
    }

    func test_older_release_is_not_an_update() throws {
        let result = try ReleaseParser.parse(releaseJSON(tag: "v0.9.0"), currentVersion: "1.0.0").get()
        XCTAssertFalse(result.isNewer)
    }

    func test_release_without_pkg_asset_fails() {
        let json = releaseJSON(tag: "v1.1.0", assetName: "Weto-1.1.0.zip")
        guard case .failure = ReleaseParser.parse(json, currentVersion: "1.0.0") else {
            return XCTFail("релиз без PKG не должен считаться пригодным")
        }
    }

    func test_malformed_tag_fails() {
        let json = releaseJSON(tag: "релиз")
        guard case .failure = ReleaseParser.parse(json, currentVersion: "1.0.0") else {
            return XCTFail("нечитаемый тег должен приводить к ошибке")
        }
    }

    func test_latest_release_url_points_at_configured_repository() {
        XCTAssertEqual(
            ReleaseParser.latestReleaseURL,
            "https://api.github.com/repos/\(Constants.githubOwner)/\(Constants.githubRepo)/releases/latest"
        )
    }
}
```

- [ ] **Step 6: Прогнать, убедиться что падает**

Run: `swift test --filter ReleaseParserTests`
Expected: FAIL — `cannot find 'ReleaseParser' in scope`.

- [ ] **Step 7: Реализовать модель и разбор**

Файл `Sources/WetoCore/Model/UpdateInfo.swift`:

```swift
import Foundation

public struct UpdateInfo: Codable, Equatable, Sendable {
    public let currentVersion: String
    public let latestVersion: String
    public let downloadURL: String
    public let releaseNotes: String?
    public let isNewer: Bool

    public init(
        currentVersion: String,
        latestVersion: String,
        downloadURL: String,
        releaseNotes: String?,
        isNewer: Bool
    ) {
        self.currentVersion = currentVersion
        self.latestVersion = latestVersion
        self.downloadURL = downloadURL
        self.releaseNotes = releaseNotes
        self.isNewer = isNewer
    }
}
```

Файл `Sources/WetoCore/ReleaseParser.swift`:

```swift
import Foundation

/// Разбор ответа GitHub Releases API. Порт `UpdateChecker.parseRelease`
/// из `../blik/blik/Sources/BlikHelper/UpdateChecker.swift`, выделенный
/// в чистую функцию ради тестируемости.
public enum ReleaseParser {

    public enum ParseError: LocalizedError, Equatable {
        case invalidVersion(String)
        case noPKGAsset

        public var errorDescription: String? {
            switch self {
            case .invalidVersion(let value): return "Некорректная версия: \(value)"
            case .noPKGAsset: return "PKG-файл не найден в релизе"
            }
        }
    }

    private struct Release: Decodable {
        let tag_name: String
        let body: String?
        let assets: [Asset]
    }

    private struct Asset: Decodable {
        let name: String
        let browser_download_url: String
    }

    public static var latestReleaseURL: String {
        "https://api.github.com/repos/\(Constants.githubOwner)/\(Constants.githubRepo)/releases/latest"
    }

    public static func parse(_ data: Data, currentVersion: String) -> Result<UpdateInfo, Error> {
        let release: Release
        do {
            release = try JSONDecoder().decode(Release.self, from: data)
        } catch {
            return .failure(error)
        }

        let versionString = release.tag_name.hasPrefix("v")
            ? String(release.tag_name.dropFirst())
            : release.tag_name

        guard let latest = SemanticVersion(string: versionString) else {
            return .failure(ParseError.invalidVersion(release.tag_name))
        }
        guard let current = SemanticVersion(string: currentVersion) else {
            return .failure(ParseError.invalidVersion(currentVersion))
        }
        guard let pkg = release.assets.first(where: { $0.name.hasSuffix(".pkg") }) else {
            return .failure(ParseError.noPKGAsset)
        }

        return .success(UpdateInfo(
            currentVersion: currentVersion,
            latestVersion: versionString,
            downloadURL: pkg.browser_download_url,
            releaseNotes: release.body,
            isNewer: current < latest
        ))
    }
}
```

- [ ] **Step 8: Прогнать, убедиться что проходят**

Run: `swift test --filter ReleaseParserTests`
Expected: PASS, 7 тестов.

- [ ] **Step 9: Прогнать весь набор**

Run: `swift test`
Expected: PASS, 126 тестов.

- [ ] **Step 10: Коммит**

```bash
git add Sources/WetoCore/SemanticVersion.swift Sources/WetoCore/Model/UpdateInfo.swift \
        Sources/WetoCore/ReleaseParser.swift Tests/WetoCoreTests/SemanticVersionTests.swift \
        Tests/WetoCoreTests/ReleaseParserTests.swift
git commit -m "$TASK_NO - Сравнение версий и разбор релиза GitHub"
```

---

## Task 18: XPC-демон обновлений

Демон существует ровно для одного: `installer -pkg` без root спрашивает пароль администратора при каждом обновлении. Мониторинг и убийство процессов root не требуют.

**Files:**
- Modify: `Package.swift` — добавить `WetoXPC` и `WetoHelper`, добавить `WetoXPC` в зависимости `WetoShared`
- Create: `Sources/WetoXPC/HelperProtocol.swift`
- Create: `Sources/WetoXPC/XPCConstants.swift`
- Create: `Sources/WetoXPC/HelperClient.swift`
- Create: `Sources/WetoHelper/UpdateInstaller.swift`
- Create: `Sources/WetoHelper/HelperDelegate.swift`
- Create: `Sources/WetoHelper/main.swift`
- Create: `Sources/WetoShared/UpdateVM.swift`
- Test: `Tests/WetoXPCTests/XPCContractTests.swift`

**Interfaces:**
- Consumes: `ReleaseParser`, `UpdateInfo`, `Constants` (Task 17).
- Produces:
  - `@objc protocol WetoHelperProtocol { func checkForUpdate(reply: @escaping (Data?, String?) -> Void); func performUpdate(reply: @escaping (String?) -> Void) }`
  - `XPCConstants.machServiceName = "com.weto.helper"`, `XPCConstants.protocolVersion = "1.0.0"`
  - `HelperClient` — `isConnected: Bool`, `helper() -> WetoHelperProtocol?`
  - `@Observable @MainActor final class UpdateVM` — `availableUpdate`, `isInstalling`, `manualResult`, `shouldTerminate`, `checkForUpdate()`, `installUpdate()`

- [ ] **Step 1: Добавить таргеты в `Package.swift`**

```swift
        .target(name: "WetoXPC", dependencies: ["WetoCore"], path: "Sources/WetoXPC"),
        .executableTarget(
            name: "WetoHelper",
            dependencies: ["WetoCore", "WetoXPC"],
            path: "Sources/WetoHelper"
        ),
        .testTarget(
            name: "WetoXPCTests",
            dependencies: ["WetoXPC", "WetoCore"],
            path: "Tests/WetoXPCTests"
        ),
```

В таргет `WetoShared` добавить `"WetoXPC"` в `dependencies`.

- [ ] **Step 2: Написать тест на контракт XPC**

Файл `Tests/WetoXPCTests/XPCContractTests.swift`:

```swift
import XCTest
@testable import WetoXPC
import WetoCore

final class XPCContractTests: XCTestCase {

    func test_mach_service_name_matches_launch_daemon_label() {
        // Значение обязано совпадать с Label и MachServices в
        // Resources/com.weto.helper.plist — иначе клиент не подключится.
        XCTAssertEqual(XPCConstants.machServiceName, "com.weto.helper")
    }

    func test_protocol_version_is_a_valid_semantic_version() {
        XCTAssertNotNil(SemanticVersion(string: XPCConstants.protocolVersion))
    }

    func test_update_info_survives_json_roundtrip() throws {
        // По XPC данные ходят JSON-кодированными Data, как в blik.
        let original = UpdateInfo(
            currentVersion: "1.0.0",
            latestVersion: "1.1.0",
            downloadURL: "https://example.com/Weto-1.1.0.pkg",
            releaseNotes: "Заметки",
            isNewer: true
        )
        let data = try JSONEncoder().encode(original)
        XCTAssertEqual(try JSONDecoder().decode(UpdateInfo.self, from: data), original)
    }
}
```

- [ ] **Step 3: Прогнать, убедиться что падает**

Run: `swift test --filter XPCContractTests`
Expected: FAIL — `cannot find 'XPCConstants' in scope`.

- [ ] **Step 4: Создать протокол и константы**

Файл `Sources/WetoXPC/XPCConstants.swift`:

```swift
import Foundation

public enum XPCConstants {
    /// Совпадает с `Label` и ключом `MachServices` в
    /// `Resources/com.weto.helper.plist`.
    public static let machServiceName = "com.weto.helper"

    /// Версия XPC-поверхности. Развязана с релизной версией приложения:
    /// `build.sh` подставляет только `Constants.appVersion`.
    /// Бампается вручную при изменении протокола.
    public static let protocolVersion = "1.0.0"
}
```

Файл `Sources/WetoXPC/HelperProtocol.swift`:

```swift
import Foundation

/// XPC-поверхность демона. Ровно два метода: проверить и поставить.
/// Данные ходят JSON-кодированными `Data` — `@objc` не пропускает Swift-структуры.
@objc public protocol WetoHelperProtocol {
    /// `Data` — JSON `UpdateInfo`, `String` — текст ошибки. Ровно одно из двух не `nil`.
    func checkForUpdate(reply: @escaping (Data?, String?) -> Void)

    /// Скачивает и ставит PKG. `String` — текст ошибки, `nil` — успех.
    /// При успехе демон умирает вместе с переустановкой, поэтому ответ может не прийти.
    func performUpdate(reply: @escaping (String?) -> Void)
}
```

Файл `Sources/WetoXPC/HelperClient.swift`:

```swift
import Foundation

/// Клиент XPC-соединения с демоном.
public final class HelperClient: @unchecked Sendable {

    private var connection: NSXPCConnection?
    private let lock = NSLock()

    public init() {}

    public var isConnected: Bool {
        lock.lock(); defer { lock.unlock() }
        return connection != nil
    }

    public func helper() -> WetoHelperProtocol? {
        lock.lock(); defer { lock.unlock() }

        if connection == nil {
            let newConnection = NSXPCConnection(
                machServiceName: XPCConstants.machServiceName,
                options: .privileged
            )
            newConnection.remoteObjectInterface =
                NSXPCInterface(with: WetoHelperProtocol.self)
            newConnection.invalidationHandler = { [weak self] in
                self?.lock.lock()
                self?.connection = nil
                self?.lock.unlock()
            }
            newConnection.resume()
            connection = newConnection
        }

        return connection?.remoteObjectProxyWithErrorHandler { _ in } as? WetoHelperProtocol
    }
}
```

- [ ] **Step 5: Прогнать, убедиться что проходят**

Run: `swift test --filter XPCContractTests`
Expected: PASS, 3 теста.

- [ ] **Step 6: Реализовать установщик в демоне**

Файл `Sources/WetoHelper/UpdateInstaller.swift`:

```swift
import Foundation
import WetoCore

/// Скачивание и установка PKG. Работает от root внутри демона.
enum UpdateInstaller {

    enum InstallError: LocalizedError {
        case invalidURL
        case httpError(Int)
        case installFailed(Int32)

        var errorDescription: String? {
            switch self {
            case .invalidURL: return "Некорректный URL"
            case .httpError(let code): return "HTTP \(code)"
            case .installFailed(let code): return "Установка завершилась с кодом \(code)"
            }
        }
    }

    /// Каталог создаётся под root с правами 0700, чтобы исключить подмену
    /// файла из user-процессов между скачиванием и `installer -pkg`.
    /// Приём унаследован от `../blik/blik/Sources/BlikHelper/UpdateChecker.swift`.
    static func ensureUpdatesDirectory() throws {
        let fm = FileManager.default
        let path = Constants.updatesDirectory
        if fm.fileExists(atPath: path) {
            try fm.setAttributes([.posixPermissions: 0o700], ofItemAtPath: path)
        } else {
            try fm.createDirectory(
                at: URL(fileURLWithPath: path),
                withIntermediateDirectories: true,
                attributes: [.posixPermissions: 0o700]
            )
        }
    }

    static func checkForUpdate() async throws -> UpdateInfo {
        guard let url = URL(string: ReleaseParser.latestReleaseURL) else {
            throw InstallError.invalidURL
        }
        var request = URLRequest(url: url)
        request.setValue("application/vnd.github+json", forHTTPHeaderField: "Accept")
        request.timeoutInterval = 30

        let (data, response) = try await URLSession.shared.data(for: request)
        if let http = response as? HTTPURLResponse, http.statusCode != 200 {
            throw InstallError.httpError(http.statusCode)
        }
        return try ReleaseParser.parse(data, currentVersion: Constants.appVersion).get()
    }

    static func downloadAndInstall(from urlString: String) async throws {
        guard let url = URL(string: urlString) else { throw InstallError.invalidURL }
        try ensureUpdatesDirectory()

        let destination = URL(fileURLWithPath: Constants.updatesDirectory)
            .appendingPathComponent("weto-update.pkg")
        try? FileManager.default.removeItem(at: destination)

        let (temporary, response) = try await URLSession.shared.download(from: url)
        if let http = response as? HTTPURLResponse, http.statusCode != 200 {
            throw InstallError.httpError(http.statusCode)
        }

        try FileManager.default.moveItem(at: temporary, to: destination)
        try FileManager.default.setAttributes(
            [.posixPermissions: 0o600],
            ofItemAtPath: destination.path
        )

        let process = Process()
        process.executableURL = URL(fileURLWithPath: "/usr/sbin/installer")
        process.arguments = ["-pkg", destination.path, "-target", "/"]
        try process.run()
        process.waitUntilExit()

        try? FileManager.default.removeItem(at: destination)

        guard process.terminationStatus == 0 else {
            throw InstallError.installFailed(process.terminationStatus)
        }
    }
}
```

- [ ] **Step 7: Реализовать делегат и точку входа демона**

Файл `Sources/WetoHelper/HelperDelegate.swift`:

```swift
import Foundation
import WetoXPC

final class HelperDelegate: NSObject, NSXPCListenerDelegate, WetoHelperProtocol {

    func listener(
        _ listener: NSXPCListener,
        shouldAcceptNewConnection connection: NSXPCConnection
    ) -> Bool {
        connection.exportedInterface = NSXPCInterface(with: WetoHelperProtocol.self)
        connection.exportedObject = self
        connection.resume()
        return true
    }

    func checkForUpdate(reply: @escaping (Data?, String?) -> Void) {
        Task {
            do {
                let info = try await UpdateInstaller.checkForUpdate()
                reply(try JSONEncoder().encode(info), nil)
            } catch {
                reply(nil, error.localizedDescription)
            }
        }
    }

    func performUpdate(reply: @escaping (String?) -> Void) {
        Task {
            do {
                let info = try await UpdateInstaller.checkForUpdate()
                guard info.isNewer else {
                    reply("Установлена последняя версия")
                    return
                }
                try await UpdateInstaller.downloadAndInstall(from: info.downloadURL)
                reply(nil)
            } catch {
                reply(error.localizedDescription)
            }
        }
    }
}
```

Файл `Sources/WetoHelper/main.swift` (без `@main`, как `BlikHelper/main.swift` в blik — демон живёт на `dispatchMain()`):

```swift
import Foundation
import WetoXPC

let delegate = HelperDelegate()
let listener = NSXPCListener(machServiceName: XPCConstants.machServiceName)
listener.delegate = delegate
listener.resume()

dispatchMain()
```

- [ ] **Step 8: Реализовать `UpdateVM`**

Файл `Sources/WetoShared/UpdateVM.swift`:

```swift
import Foundation
import Observation
import WetoCore
import WetoXPC

/// Проверка и установка обновлений через демон.
///
/// Демон при успешной установке умирает вместе с переустановкой, поэтому
/// ответ на `performUpdate` может не прийти — отсюда мониторинг разрыва
/// соединения и таймаут. Схема унаследована от
/// `../blik/blik/Sources/BlikShared/UpdateVM.swift`.
@Observable
@MainActor
public final class UpdateVM {

    public private(set) var availableUpdate: UpdateInfo?
    public private(set) var isInstalling = false
    public private(set) var manualResult: String?
    /// Приложение должно завершиться, чтобы перезапуститься уже обновлённым.
    public private(set) var shouldTerminate = false

    @ObservationIgnored private let client: HelperClient
    @ObservationIgnored private var checkTask: Task<Void, Never>?
    @ObservationIgnored private var monitorTask: Task<Void, Never>?
    @ObservationIgnored private let installTimeout: TimeInterval

    public init(client: HelperClient = HelperClient(), installTimeout: TimeInterval = 300) {
        self.client = client
        self.installTimeout = installTimeout
    }

    deinit {
        checkTask?.cancel()
        monitorTask?.cancel()
    }

    public var helperAvailable: Bool { client.helper() != nil }

    public func startPeriodicCheck() {
        checkForUpdate()
        checkTask?.cancel()
        checkTask = Task { [weak self] in
            while !Task.isCancelled {
                try? await Task.sleep(for: .seconds(Constants.updateCheckInterval))
                guard !Task.isCancelled else { return }
                await MainActor.run { [weak self] in self?.checkForUpdate() }
            }
        }
    }

    public func checkForUpdate() {
        guard let helper = client.helper() else {
            manualResult = "Демон обновлений недоступен"
            return
        }
        helper.checkForUpdate { [weak self] data, error in
            Task { @MainActor [weak self] in
                guard let self else { return }
                if let error {
                    self.manualResult = "Ошибка: \(error)"
                    return
                }
                guard let data,
                      let info = try? JSONDecoder().decode(UpdateInfo.self, from: data)
                else { return }
                self.availableUpdate = info.isNewer ? info : nil
                self.manualResult = info.isNewer
                    ? nil
                    : "Установлена последняя версия \(info.currentVersion)"
            }
        }
    }

    public func installUpdate() {
        guard let helper = client.helper() else { return }
        isInstalling = true

        helper.performUpdate { [weak self] error in
            Task { @MainActor [weak self] in
                guard let self, self.isInstalling else { return }
                if let error {
                    self.isInstalling = false
                    self.manualResult = "Ошибка: \(error)"
                } else {
                    self.shouldTerminate = true
                }
            }
        }

        monitorTask?.cancel()
        monitorTask = Task { [weak self] in
            var elapsed: TimeInterval = 0
            while !Task.isCancelled {
                try? await Task.sleep(for: .seconds(3))
                elapsed += 3
                let finished = await MainActor.run { [weak self] () -> Bool in
                    guard let self, self.isInstalling else { return true }
                    if !self.client.isConnected {
                        self.shouldTerminate = true
                        return true
                    }
                    if elapsed >= self.installTimeout {
                        self.isInstalling = false
                        self.manualResult = "Установка не завершилась. Попробуйте позже."
                        return true
                    }
                    return false
                }
                if finished { return }
            }
        }
    }
}
```

- [ ] **Step 9: Подключить `UpdateVM` к координатору**

В `Sources/WetoShared/AppCoordinator.swift` добавить свойство `public let update = UpdateVM()`, а в `start()` — вызов `update.startPeriodicCheck()`.

- [ ] **Step 10: Добавить секцию обновлений в настройки**

В `Sources/WetoMenuBar/Settings/SettingsWindow.swift` добавить в `List` секцию:

```swift
    private var updatesSection: some View {
        Section("Обновления") {
            LabeledContent("Версия", value: Constants.appVersion)
            LabeledContent("Демон") {
                Text(coordinator.update.helperAvailable ? "Доступен" : "Не установлен")
                    .foregroundStyle(coordinator.update.helperAvailable
                                     ? DesignTokens.green : DesignTokens.red)
            }
            if let update = coordinator.update.availableUpdate {
                KSBanner(
                    tone: .info,
                    systemImage: "arrow.down.circle.fill",
                    text: "Доступна версия \(update.latestVersion)"
                )
                Button("Установить обновление") { coordinator.update.installUpdate() }
                    .disabled(coordinator.update.isInstalling)
            }
            if let result = coordinator.update.manualResult {
                Text(result).font(DesignTokens.fontSecondary)
            }
        }
        .listRowInsets(KSPageMetrics.rowInsets)
    }
```

и вставить `updatesSection` в `List` перед `journalSection`.

- [ ] **Step 11: Собрать и прогнать весь набор**

Run: `swift build && swift test`
Expected: сборка всех семи таргетов зелёная, PASS, 129 тестов.

- [ ] **Step 12: Коммит**

```bash
git add Package.swift Sources/WetoXPC Sources/WetoHelper \
        Sources/WetoShared/UpdateVM.swift Sources/WetoShared/AppCoordinator.swift \
        Sources/WetoMenuBar/Settings/SettingsWindow.swift Tests/WetoXPCTests
git commit -m "$TASK_NO - XPC-демон обновлений"
```

---

## Task 19: Упаковка в PKG, автозапуск и удаление

**Files:**
- Create: `Sources/WetoShared/LaunchAgentController.swift`
- Create: `Resources/Weto-Info.plist`
- Create: `Resources/com.weto.app.plist`
- Create: `Resources/com.weto.helper.plist`
- Create: `Resources/uninstall-helper.sh`
- Create: `Resources/welcome.html`, `Resources/conclusion.html`
- Create: `scripts/build.sh`, `scripts/preinstall`, `scripts/postinstall`
- Modify: `Sources/WetoMenuBar/Settings/SettingsWindow.swift` — секции «Запуск» и «Удаление»
- Test: `Tests/WetoSharedTests/LaunchAgentControllerTests.swift`

**Interfaces:**
- Consumes: `XPCConstants.machServiceName` (Task 18).
- Produces: `LaunchAgentController` — `plistPath`, `serviceName`, `isInstalled`, `isEnabled`, `enable()`, `disable()`, `uninstallScriptURL`.

- [ ] **Step 1: Написать тест на контроллер автозапуска**

Файл `Tests/WetoSharedTests/LaunchAgentControllerTests.swift`:

```swift
import XCTest
@testable import WetoShared

final class LaunchAgentControllerTests: XCTestCase {

    func test_paths_match_pkg_layout() {
        // Должно совпадать с тем, что кладёт scripts/build.sh.
        XCTAssertEqual(
            LaunchAgentController.plistPath,
            "/Library/LaunchAgents/com.weto.app.plist"
        )
        XCTAssertEqual(LaunchAgentController.serviceName, "com.weto.app")
    }

    func test_is_installed_reflects_file_presence() {
        let expected = FileManager.default.fileExists(atPath: LaunchAgentController.plistPath)
        XCTAssertEqual(LaunchAgentController.isInstalled, expected)
    }

    func test_is_enabled_is_false_when_agent_is_not_installed() {
        guard !LaunchAgentController.isInstalled else {
            throw XCTSkip("агент установлен на этой машине — проверка неприменима")
        }
        XCTAssertFalse(LaunchAgentController.isEnabled)
    }

    func test_enable_fails_gracefully_when_not_installed() {
        guard !LaunchAgentController.isInstalled else {
            throw XCTSkip("агент установлен на этой машине — проверка неприменима")
        }
        XCTAssertFalse(LaunchAgentController.enable())
    }
}
```

Пометить методы `throws`, чтобы работал `XCTSkip`.

- [ ] **Step 2: Прогнать, убедиться что падает**

Run: `swift test --filter LaunchAgentControllerTests`
Expected: FAIL — `cannot find 'LaunchAgentController' in scope`.

- [ ] **Step 3: Реализовать контроллер**

Файл `Sources/WetoShared/LaunchAgentController.swift` — порт `../blik/blik/Sources/BlikShared/LaunchAgentController.swift` с заменой идентификаторов:

```swift
import Foundation
import Darwin

/// Управление LaunchAgent пользователя.
/// plist кладёт PKG в `/Library/LaunchAgents/`, загрузка и выгрузка идут
/// через `launchctl` в user-домене и не требуют sudo.
public enum LaunchAgentController {

    public static let plistPath = "/Library/LaunchAgents/com.weto.app.plist"
    public static let serviceName = "com.weto.app"

    /// Скрипт полного удаления лежит внутри бандла приложения.
    public static var uninstallScriptURL: URL? {
        Bundle.main.url(forResource: "uninstall-helper", withExtension: "sh")
    }

    public static var isInstalled: Bool {
        FileManager.default.fileExists(atPath: plistPath)
    }

    public static var isEnabled: Bool {
        guard isInstalled else { return false }
        return runLaunchctl(["print", "gui/\(getuid())/\(serviceName)"])
    }

    @discardableResult
    public static func enable() -> Bool {
        guard isInstalled else { return false }
        return runLaunchctl(["bootstrap", "gui/\(getuid())", plistPath])
    }

    @discardableResult
    public static func disable() -> Bool {
        runLaunchctl(["bootout", "gui/\(getuid())/\(serviceName)"])
    }

    // MARK: - Private

    private static func runLaunchctl(_ arguments: [String]) -> Bool {
        let process = Process()
        process.executableURL = URL(fileURLWithPath: "/bin/launchctl")
        process.arguments = arguments
        process.standardOutput = Pipe()
        process.standardError = Pipe()
        do {
            try process.run()
            process.waitUntilExit()
            return process.terminationStatus == 0
        } catch {
            return false
        }
    }
}
```

- [ ] **Step 4: Прогнать, убедиться что проходят**

Run: `swift test --filter LaunchAgentControllerTests`
Expected: PASS, 4 теста (часть может быть пропущена, если агент уже установлен).

- [ ] **Step 5: Создать plist-файлы**

Файл `Resources/com.weto.helper.plist`:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.weto.helper</string>
    <key>Program</key>
    <string>/Library/PrivilegedHelperTools/com.weto.helper</string>
    <key>MachServices</key>
    <dict>
        <key>com.weto.helper</key>
        <true/>
    </dict>
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>
</dict>
</plist>
```

Файл `Resources/com.weto.app.plist` — `KeepAlive` здесь `true`, в отличие от blik: падение менюбара blik означает потерю удобства, падение нашего — потерю охраны.

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.weto.app</string>
    <key>Program</key>
    <string>/Applications/Weto.app/Contents/MacOS/WetoMenuBar</string>
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>
</dict>
</plist>
```

Файл `Resources/Weto-Info.plist`:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key>
    <string>Weto</string>
    <key>CFBundleDisplayName</key>
    <string>Weto</string>
    <key>CFBundleIdentifier</key>
    <string>com.weto.app</string>
    <key>CFBundleExecutable</key>
    <string>WetoMenuBar</string>
    <key>CFBundleIconFile</key>
    <string>AppIcon</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>0.1.0</string>
    <key>CFBundleVersion</key>
    <string>0.1.0</string>
    <key>LSMinimumSystemVersion</key>
    <string>26.0</string>
    <key>LSUIElement</key>
    <true/>
</dict>
</plist>
```

- [ ] **Step 6: Создать скрипты установки**

Файл `scripts/preinstall`:

```bash
#!/bin/bash
# Остановить старые агент и демон при обновлении.

CONSOLE_USER=$(/usr/bin/stat -f '%Su' /dev/console 2>/dev/null || echo "")
if [ -n "$CONSOLE_USER" ] && [ "$CONSOLE_USER" != "root" ]; then
    CURRENT_UID=$(id -u "$CONSOLE_USER")
else
    CURRENT_UID=$(id -u "$USER")
fi

killall WetoMenuBar 2>/dev/null || true
launchctl bootout gui/"$CURRENT_UID"/com.weto.app 2>/dev/null || true
launchctl bootout system/com.weto.helper 2>/dev/null || true
exit 0
```

Файл `scripts/postinstall`:

```bash
#!/bin/bash
set -e

chown root:wheel /Library/PrivilegedHelperTools/com.weto.helper
chmod 755 /Library/PrivilegedHelperTools/com.weto.helper
chown root:wheel /Library/LaunchDaemons/com.weto.helper.plist
chmod 644 /Library/LaunchDaemons/com.weto.helper.plist
chown root:wheel /Library/LaunchAgents/com.weto.app.plist
chmod 644 /Library/LaunchAgents/com.weto.app.plist

launchctl bootstrap system /Library/LaunchDaemons/com.weto.helper.plist

CONSOLE_USER=$(/usr/bin/stat -f '%Su' /dev/console 2>/dev/null || echo "")
if [ -n "$CONSOLE_USER" ] && [ "$CONSOLE_USER" != "root" ]; then
    CURRENT_UID=$(id -u "$CONSOLE_USER")
else
    CURRENT_UID=$(id -u "$USER")
fi

launchctl bootstrap gui/"$CURRENT_UID" /Library/LaunchAgents/com.weto.app.plist 2>/dev/null || true

sleep 1
if ! pgrep -x WetoMenuBar > /dev/null 2>&1; then
    sudo -u "$CONSOLE_USER" open /Applications/Weto.app 2>/dev/null || true
fi
exit 0
```

Файл `Resources/uninstall-helper.sh`:

```bash
#!/bin/bash
echo "Полное удаление Weto..."

killall WetoMenuBar 2>/dev/null
sleep 1

sudo launchctl bootout system/com.weto.helper 2>/dev/null
launchctl bootout gui/"$(id -u "$USER")"/com.weto.app 2>/dev/null

sudo rm -f /Library/PrivilegedHelperTools/com.weto.helper
sudo rm -f /Library/LaunchDaemons/com.weto.helper.plist
sudo rm -f /Library/LaunchAgents/com.weto.app.plist
sudo rm -rf /Applications/Weto.app
sudo rm -rf /var/db/weto

rm -f ~/Library/Preferences/com.weto.*
rm -rf ~/Library/Caches/com.weto.*
security delete-generic-password -s com.weto.ipinfo 2>/dev/null

tccutil reset All com.weto.app 2>/dev/null
sudo pkgutil --forget com.weto.pkg 2>/dev/null

echo "Weto полностью удалён."
```

Файл `Resources/welcome.html`:

```html
<!DOCTYPE html>
<html><head><meta charset="utf-8">
<style>body{font:13px -apple-system;margin:16px;color:#222}h2{font-size:15px}</style>
</head><body>
<h2>Weto</h2>
<p>Приложение следит за тем, через какую сеть выходит ваш компьютер, и немедленно
завершает процессы выбранных приложений, как только выход перестаёт быть безопасным:
упал корпоративный VPN, внешний адрес оказался в заблокированной стране или в чёрном
списке, либо определить положение не удалось вовсе.</p>
<p>Устанавливаются приложение в менюбаре и небольшая служба, которая ставит обновления
без запроса пароля.</p>
</body></html>
```

Файл `Resources/conclusion.html`:

```html
<!DOCTYPE html>
<html><head><meta charset="utf-8">
<style>body{font:13px -apple-system;margin:16px;color:#222}h2{font-size:15px}li{margin:4px 0}</style>
</head><body>
<h2>Установлено</h2>
<p>Значок появился в менюбаре. Охрана выключена — включите её после настройки.</p>
<p>Откройте «Настройки» и заполните:</p>
<ul>
  <li>приложения, которые нужно контролировать;</li>
  <li>VPN-сервис, который считается корпоративным;</li>
  <li>токен ipinfo для определения внешнего адреса.</li>
</ul>
</body></html>
```

- [ ] **Step 7: Создать `scripts/build.sh`**

Порт `../blik/blik/scripts/build.sh` с заменой имён. Ключевые отличия: собираются два продукта вместо трёх, CLI-бинарника в `/usr/local/bin` нет.

```bash
#!/bin/bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"
cd "$PROJECT_DIR"

VERSION="${1:-0.1.0}"
PKG_ID="com.weto.pkg"
OUT=".build/release_build"

echo "=== Weto $VERSION — сборка ==="

# Подстановка версии. XPCConstants.protocolVersion НЕ трогаем:
# это версия XPC-поверхности, а не релиза.
sed -i '' "s/public static let appVersion = \".*\"/public static let appVersion = \"$VERSION\"/" \
    Sources/WetoCore/Constants.swift
sed -i '' "s/<string>[0-9]*\.[0-9]*\.[0-9]*<\/string>/<string>$VERSION<\/string>/g" \
    Resources/Weto-Info.plist

swift build -c release --product WetoMenuBar
swift build -c release --product WetoHelper

rm -rf "$OUT"; mkdir -p "$OUT"

APP="$OUT/_app/Weto.app/Contents"
mkdir -p "$APP/MacOS" "$APP/Resources"
cp .build/release/WetoMenuBar "$APP/MacOS/"
cp Resources/Weto-Info.plist "$APP/Info.plist"
cp Resources/uninstall-helper.sh "$APP/Resources/"
chmod +x "$APP/Resources/uninstall-helper.sh"
cp Resources/AppIcon.icns "$APP/Resources/" 2>/dev/null || true

codesign --force --sign - --deep "$OUT/_app/Weto.app" 2>&1 || true

ROOT="$OUT/_pkg-root"; SCRIPTS="$OUT/_pkg-scripts"
mkdir -p "$ROOT/Applications" "$ROOT/Library/PrivilegedHelperTools" \
         "$ROOT/Library/LaunchDaemons" "$ROOT/Library/LaunchAgents" "$SCRIPTS"

cp -R "$OUT/_app/Weto.app" "$ROOT/Applications/"
cp .build/release/WetoHelper "$ROOT/Library/PrivilegedHelperTools/com.weto.helper"
cp Resources/com.weto.helper.plist "$ROOT/Library/LaunchDaemons/"
cp Resources/com.weto.app.plist "$ROOT/Library/LaunchAgents/"
cp scripts/preinstall scripts/postinstall "$SCRIPTS/"
chmod +x "$SCRIPTS/preinstall" "$SCRIPTS/postinstall"

pkgbuild --root "$ROOT" --scripts "$SCRIPTS" --identifier "$PKG_ID" \
         --version "$VERSION" --install-location "/" "$OUT/_component.pkg"

cat > "$OUT/_distribution.xml" << DIST
<?xml version="1.0" encoding="utf-8"?>
<installer-gui-script minSpecVersion="2">
    <title>Weto</title>
    <welcome file="welcome.html"/>
    <conclusion file="conclusion.html"/>
    <options customize="never" require-scripts="false" hostArchitectures="arm64"/>
    <volume-check>
        <allowed-os-versions><os-version min="26.0"/></allowed-os-versions>
    </volume-check>
    <pkg-ref id="$PKG_ID"/>
    <choices-outline><line choice="default"><line choice="$PKG_ID"/></line></choices-outline>
    <choice id="default"/>
    <choice id="$PKG_ID" visible="false"><pkg-ref id="$PKG_ID"/></choice>
    <pkg-ref id="$PKG_ID" version="$VERSION" onConclusion="none">_component.pkg</pkg-ref>
</installer-gui-script>
DIST

mkdir -p "$OUT/_pkg-resources"
cp Resources/welcome.html Resources/conclusion.html "$OUT/_pkg-resources/"

productbuild --distribution "$OUT/_distribution.xml" --resources "$OUT/_pkg-resources" \
             --package-path "$OUT/" "$OUT/Weto-$VERSION.pkg"

rm -rf "$OUT/_app" "$OUT/_pkg-root" "$OUT/_pkg-scripts" "$OUT/_pkg-resources" \
       "$OUT/_distribution.xml" "$OUT/_component.pkg"

echo "✓ Готово: $OUT/Weto-$VERSION.pkg"
```

- [ ] **Step 8: Добавить секции «Запуск» и «Удаление» в настройки**

В `SettingsWindow.swift` добавить и вставить в `List` перед `journalSection`:

```swift
    private var launchSection: some View {
        Section("Запуск") {
            Toggle("Автозапуск при входе в систему", isOn: Binding(
                get: { LaunchAgentController.isEnabled },
                set: { $0 ? LaunchAgentController.enable() : LaunchAgentController.disable() }
            ))
            .toggleStyle(.switch)
            .tint(DesignTokens.accent.resolve(colorScheme))
            .disabled(!LaunchAgentController.isInstalled)
            .help(LaunchAgentController.isInstalled
                  ? "Запускать Weto при входе в систему"
                  : "Доступно после установки из PKG")

            Button("Удалить Weto…", role: .destructive) { confirmUninstall() }
        }
        .listRowInsets(KSPageMetrics.rowInsets)
    }

    /// NSAlert, а не SwiftUI `.alert`: в приложении с MenuBarExtra
    /// SwiftUI-алерт закрывает popover (баг, задокументирован в blik).
    private func confirmUninstall() {
        let alert = NSAlert()
        alert.messageText = "Удалить Weto?"
        alert.informativeText = "Будут удалены приложение, демон обновлений, настройки и токен."
        alert.alertStyle = .warning
        alert.addButton(withTitle: "Удалить")
        alert.addButton(withTitle: "Отмена")

        guard alert.runModal() == .alertFirstButtonReturn,
              let script = LaunchAgentController.uninstallScriptURL
        else { return }

        let process = Process()
        process.executableURL = URL(fileURLWithPath: "/bin/bash")
        process.arguments = [script.path]
        try? process.run()
    }
```

Добавить `import AppKit` и `import WetoShared`, если их ещё нет.

- [ ] **Step 9: Собрать PKG и установить**

```bash
chmod +x scripts/build.sh scripts/preinstall scripts/postinstall Resources/uninstall-helper.sh
scripts/build.sh 0.1.0
open .build/release_build/Weto-0.1.0.pkg
```

Проверить после установки:
- `launchctl print system/com.weto.helper` показывает загруженный демон,
- иконка появилась в менюбаре,
- в настройках «Демон» — «Доступен»,
- тумблер автозапуска активен.

- [ ] **Step 10: Прогнать весь набор**

Run: `swift test`
Expected: PASS, 133 теста.

- [ ] **Step 11: Коммит**

```bash
git add Resources scripts Sources/WetoShared/LaunchAgentController.swift \
        Sources/WetoMenuBar/Settings/SettingsWindow.swift \
        Tests/WetoSharedTests/LaunchAgentControllerTests.swift
git commit -m "$TASK_NO - PKG-установщик, автозапуск и удаление"
```

---

## Task 20: GitHub-флоу и документация проекта

**Files:**
- Create: `.github/workflows/pr-checks.yml`
- Create: `.github/workflows/release.yml`
- Create: `AGENTS.md`
- Create: `.claude/rules/ARCHITECTURE.md`
- Create: `.claude/CLAUDE.md` — симлинк на `../AGENTS.md`
- Create: `.claude/docs/overview.md`, `.claude/docs/modules/*.md`
- Modify: `Sources/WetoCore/Constants.swift` — реальные `githubOwner` и `githubRepo`

**Interfaces:**
- Consumes: `scripts/build.sh` (Task 19).
- Produces: рабочий релизный конвейер и документацию проекта.

- [ ] **Step 1: Уточнить у владельца адрес репозитория**

`Constants.githubOwner` и `Constants.githubRepo` определяют, откуда приложение тянет обновления. Значения по умолчанию — заглушка; подставить реальные и убедиться, что `ReleaseParserTests.test_latest_release_url_points_at_configured_repository` проходит.

- [ ] **Step 2: Создать проверку PR**

Файл `.github/workflows/pr-checks.yml`:

```yaml
name: PR Checks

on:
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    name: Build & Test
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: swift build
      - name: Test
        run: swift test
```

- [ ] **Step 3: Создать релизный воркфлоу**

Файл `.github/workflows/release.yml`:

```yaml
name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-and-release:
    name: Build PKG & Publish Release
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Build release PKG
        run: |
          chmod +x scripts/build.sh scripts/preinstall scripts/postinstall
          scripts/build.sh "${{ steps.version.outputs.VERSION }}"

      # Релиз в этот же репозиторий — источник автообновления:
      # WetoHelper опрашивает Releases API этого репо каждые 6 часов.
      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: .build/release_build/Weto-${{ steps.version.outputs.VERSION }}.pkg
          name: Weto ${{ steps.version.outputs.VERSION }}
          generate_release_notes: true
```

- [ ] **Step 4: Создать `AGENTS.md`**

```markdown
# Weto

Приложение в менюбаре macOS: завершает по SIGKILL все процессы заданных приложений,
как только сетевое положение машины перестаёт быть безопасным — упал корпоративный VPN,
гео стало заблокированным, внешний IP попал в чёрный список либо вердикт не получен вовсе.
Swift 5.9, SwiftUI, Swift Package Manager, macOS 26+.

## Запуск

```bash
swift build                     # Debug-сборка
swift test                      # Тесты
swift run WetoMenuBar     # Запуск без установки
scripts/build.sh 0.1.0          # PKG → .build/release_build/Weto-0.1.0.pkg
```

## Документация

- Карта проекта: `.claude/rules/ARCHITECTURE.md`
- Операционная память: `.claude/docs/`
- Дизайн и обоснования решений: `docs/superpowers/specs/2026-07-27-weto-design.md`

@.claude/rules/ARCHITECTURE.md

## Соглашения

- Ветки: `feature/<имя>`, `fix/<имя>`. Коммиты: `{номер задачи} - {описание}`.
- `WetoCore` не импортирует `Network`, `SystemConfiguration`, `AppKit`, `SwiftUI`
  и не использует `URLSession`. Инвариант держит основную массу тестов синхронной.
- Мокаются только границы системы. Внутренние типы не подменяются.
- Файл с `@main` называется по имени приложения, не `main.swift`. Исключение —
  `WetoHelper/main.swift` (без `@main`, живёт на `dispatchMain()`).
- `NSAlert` вместо SwiftUI `.alert`: в приложении с `MenuBarExtra` SwiftUI-алерт
  закрывает popover.
- Наблюдатели `NSWorkspace` — через `addObserver` со stored token, не через
  async sequence: `Notification` не `Sendable` под Swift 6.
- Внутри `@Observable` — `UserDefaults` напрямую, не `@AppStorage`.
- Паттерны унаследованы от `../blik/blik` — при сомнениях сверяться с ним.
```

- [ ] **Step 5: Создать `.claude/rules/ARCHITECTURE.md`**

По контракту из `~/.claude/CLAUDE.md`: стек (5–10 строк), однострочники команд, tree верхнего уровня, индекс модулей со ссылками на `.claude/docs/modules/<slug>.md`, секция `### Базовый образ` со значением «Отсутствует — нативное macOS-приложение SPM + PKG, без Docker». Лимит ~150 строк. Раздел «Персистентность» должен зафиксировать: доменной БД нет, правило UUID-PK неприменимо, хранилища — `UserDefaults(suiteName: "com.weto.shared")` и Keychain `com.weto.ipinfo`.

- [ ] **Step 6: Создать симлинк и модульную документацию**

```bash
mkdir -p .claude/rules .claude/docs/modules
ln -sf ../AGENTS.md .claude/CLAUDE.md
```

Файлы `.claude/docs/modules/`: `weto-core.md`, `weto-system.md`, `weto-shared.md`, `weto-design.md`, `weto-xpc.md`, `weto-helper.md`, `weto-menubar.md`. В каждом — назначение, `Key files`, ключевые контракты. `.claude/docs/overview.md` — только потоки данных: триггер → локальная оценка → сетевая проба → решение → завершение процессов → журнал и уведомление.

- [ ] **Step 7: Проверить релизный конвейер**

```bash
git tag v0.1.0 && git push origin v0.1.0
```

Убедиться, что воркфлоу собрал PKG и опубликовал релиз, а в настройках приложения раздел «Обновления» видит версию.

- [ ] **Step 8: Финальный прогон**

Run: `swift build && swift test`
Expected: PASS, 133 теста.

- [ ] **Step 9: Коммит**

```bash
git add .github AGENTS.md .claude Sources/WetoCore/Constants.swift
git commit -m "$TASK_NO - GitHub-флоу и документация проекта"
```

---

## Task 21: Подключение реальных целей

Выполняется, когда владелец назовёт приложения, которые нужно контролировать.

**Files:**
- Modify: настройки приложения (через UI, не через код)
- Modify: `.claude/docs/` — записать выявленные особенности целей

- [ ] **Step 1: Получить список приложений от владельца**

- [ ] **Step 2: Для каждой цели проверить владельца процессов**

```bash
BUNDLE_PATH="/Applications/<Цель>.app"
ps -axo pid,user,comm | grep -i "<имя процесса>"
```

Если хоть один процесс цели работает под `root`, `kill` вернёт `EPERM`, и приложение честно покажет это в `permissionFailure`. В этом случае потребуется расширить полномочия демона — отдельная задача, в текущий объём не входит.

- [ ] **Step 3: Проверить, что находятся все процессы, а не только главный**

Запустить цель, открыть настройки Weto и убедиться, что число найденных процессов соответствует выводу:

```bash
ps -axo pid,comm | grep "^ *[0-9]* /Applications/<Цель>.app"
```

- [ ] **Step 4: Проверить сценарий целиком**

Включить охрану, выключить Happ, убедиться что все цели завершены за доли секунды и в журнале появилась запись «VPN не поднят». Затем поднять Happ, запустить цели заново, убедиться что статус зелёный с флагом.

- [ ] **Step 5: Записать особенности целей в документацию**

Вызвать субагента `docs-keeper` (`subagent_type: DocsKeeper`, `MODE: incremental`) с описанием задачи и `git diff`.

- [ ] **Step 6: Коммит**

```bash
git add .claude/docs
git commit -m "$TASK_NO - Подключение целевых приложений"
```

